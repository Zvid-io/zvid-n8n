{
  "name": "Webinar / demo no-show → personal re-book video (Zvid)",
  "nodes": [
    {
      "parameters": {},
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        -880,
        120
      ],
      "id": "zvid-ns-0001",
      "name": "Test manually"
    },
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "minutes",
              "minutesInterval": 15
            }
          ]
        }
      },
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        -880,
        300
      ],
      "id": "zvid-ns-0002",
      "name": "Every 15 minutes"
    },
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "no-show",
        "responseMode": "responseNode",
        "options": {}
      },
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        -880,
        500
      ],
      "id": "zvid-ns-0003",
      "name": "No-show webhook",
      "webhookId": "a41d6c2f-8b57-4e19-9d30-6c2f7a1e5b84"
    },
    {
      "parameters": {
        "mode": "raw",
        "jsonOutput": "{\n  \"apiUrl\": \"https://api.zvid.io\",\n  \"editorUrl\": \"https://editor.zvid.io\",\n  \"source\": \"sheet\",\n  \"brandName\": \"Flowdesk\",\n  \"presenterName\": \"Dana Whitfield\",\n  \"presenterRole\": \"Head of Growth, Flowdesk\",\n  \"presenterImageUrl\": \"\",\n  \"paperColor\": \"#FBF3EC\",\n  \"inkColor\": \"#241C15\",\n  \"mutedColor\": \"#786557\",\n  \"brandAccent\": \"#B0472A\",\n  \"headingFont\": \"Fraunces\",\n  \"bodyFont\": \"Manrope\",\n  \"kickerText\": \"A note from {presenter}\",\n  \"hookLine\": \"We missed you, {firstName}.\",\n  \"hookSub\": \"{eventName} ran on {originalTime}. No stress — here is the short version, and a new slot whenever you like.\",\n  \"sessionLabel\": \"The session you booked\",\n  \"highlightsTitle\": \"Here's what we covered\",\n  \"highlights\": [\n    \"The three-step content system that replaced our editing contractor\",\n    \"A live build: brief to finished video in under four minutes\",\n    \"The two automations that gave us back eleven hours a week\"\n  ],\n  \"highlightsFootnote\": \"Same session, new time — there is nothing to catch up on.\",\n  \"ctaHeadline\": \"Ready when you are, {firstName}.\",\n  \"ctaSub\": \"Pick a time that actually works for you — it takes about 30 seconds.\",\n  \"ctaText\": \"Grab another slot\",\n  \"signOff\": \"{presenter} · {brand}\",\n  \"rebookUrl\": \"https://calendly.com/your-team/30min\",\n  \"timeZone\": \"UTC\",\n  \"musicUrl\": \"https://cdn.pixabay.com/audio/2026/02/24/audio_3f375fdf97.mp3\",\n  \"musicVolume\": 0.14,\n  \"maxMusicBytes\": 5242880,\n  \"frameRate\": 30,\n  \"statusDoneValue\": \"sent\",\n  \"sendEmail\": false,\n  \"emailFrom\": \"hello@yourcompany.example\",\n  \"emailSubject\": \"We missed you at {eventName}, {firstName}\",\n  \"emailBody\": \"<p>Hi {firstName},</p><p>Sorry we missed you at <strong>{eventName}</strong>. {presenter} recorded a 16-second recap of what you missed — no catching up required.</p><p><a href=\\\"{videoUrl}\\\">▶ Watch the 16-second recap</a></p><p><a href=\\\"{rebookUrl}\\\">Grab another slot</a> whenever suits you — it takes about 30 seconds.</p><p>— {presenter}, {brand}</p>\",\n  \"samplePayload\": {\n    \"inviteeFirstName\": \"Sarah\",\n    \"inviteeEmail\": \"sarah@northwind.example\",\n    \"eventName\": \"Scaling Content Ops Without Hiring\",\n    \"originalTime\": \"2026-08-13T18:00:00Z\",\n    \"rebookUrl\": \"https://calendly.com/your-team/30min\"\n  },\n  \"dryRun\": false,\n  \"pollSeconds\": 10,\n  \"timeoutMinutes\": 20\n}",
        "options": {}
      },
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [
        -660,
        300
      ],
      "id": "zvid-ns-0004",
      "name": "Config"
    },
    {
      "parameters": {
        "method": "HEAD",
        "url": "={{ $('Config').first().json.musicUrl }}",
        "options": {
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": true
            }
          },
          "redirect": {
            "redirect": {}
          },
          "timeout": 15000
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        -440,
        300
      ],
      "id": "zvid-ns-0005",
      "name": "Check music",
      "onError": "continueRegularOutput",
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "// The music bed is optional. An unreachable URL, an HTTP error or a file over\n// the plan's 5 MB audio cap renders the video WITHOUT music instead of failing\n// the run — a silent re-book video still books meetings, a failed run does not.\nconst cfg = $('Config').first().json;\nconst res = $input.first().json;\n\nconst maxBytes = Number(cfg.maxMusicBytes) || 5242880;\nconst url = String(cfg.musicUrl || '').trim();\n\nlet music = null;\nlet musicNote = 'ok';\nlet bytes = null;\n\nif (!url) {\n  musicNote = 'skipped: musicUrl is empty in Config';\n} else {\n  const status = Number(res && res.statusCode);\n  const headers = (res && res.headers) || {};\n  const len = Number(headers['content-length'] || headers['Content-Length'] || 0);\n  bytes = Number.isFinite(len) && len > 0 ? len : null;\n\n  if (!Number.isFinite(status) || status === 0) {\n    musicNote = 'skipped: the music URL was unreachable';\n  } else if (status >= 400) {\n    musicNote = 'skipped: HEAD answered HTTP ' + status;\n  } else if (bytes && bytes > maxBytes) {\n    musicNote = 'skipped: ' + Math.round(bytes / 1024) + ' KB is over the ' + Math.round(maxBytes / 1024) + ' KB cap';\n  } else {\n    music = { src: url };\n    musicNote = bytes ? 'ok (' + bytes + ' bytes)' : 'ok';\n  }\n}\n\nreturn [{ json: { music, musicNote, musicBytes: bytes } }];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -220,
        300
      ],
      "id": "zvid-ns-0006",
      "name": "Music guard"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "source-webhook--cond",
              "leftValue": "={{ $('Config').first().json.source === 'webhook' }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        0,
        300
      ],
      "id": "zvid-ns-0007",
      "name": "Source?"
    },
    {
      "parameters": {
        "jsCode": "// Normalizes one no-show posted to the webhook. A forwarded Calendly invitee\n// record (or anything your CRM sends) is mapped to a flat shape here — see the\n// blue note for the exact contract. Field names are accepted in both the\n// Calendly spelling (name / email / reschedule_url / scheduled_event.*) and the\n// plain one (firstName / eventName / originalTime / rebookUrl).\n//\n// When the workflow is executed BY HAND the webhook node has not run, so the\n// sample in Config.samplePayload is used instead: 'Execute workflow' always\n// produces a real video, which is what makes the template testable.\n//\n// The SCHEDULE is a different story. With source: \"webhook\" the schedule has\n// nothing to send — the no-shows arrive by POST. Without the trigger gate\n// below it would fall through to samplePayload and render (and charge for) the\n// same sample video every 15 minutes, unattended. A scheduled run in webhook\n// mode therefore returns found:false and ends in 'Nothing to send'.\nconst cfg = $('Config').first().json;\n\nconst didRun = (name) => {\n  try {\n    return Boolean($(name).isExecuted);\n  } catch (e) {\n    return false;\n  }\n};\nconst hookRan = didRun('No-show webhook');\nconst byHand = didRun('Test manually');\n\nlet body = null;\nlet via = 'sample';\nif (hookRan) {\n  try {\n    const hook = $('No-show webhook').first().json;\n    if (hook && typeof hook === 'object') {\n      body = hook.body && typeof hook.body === 'object' ? hook.body : hook;\n      via = 'webhook';\n    }\n  } catch (e) {\n    body = null;\n  }\n}\n\nif (!body && !byHand) {\n  return [{\n    json: {\n      found: false,\n      checked: 0,\n      source: 'webhook',\n      reason:\n        'Scheduled run while Config.source is \"webhook\": no-shows arrive by POST, so there is nothing ' +\n        'to render on a timer. Post one to the webhook URL, or set source: \"sheet\" to let the schedule ' +\n        'poll the sheet.',\n    },\n  }];\n}\n\nif (!body || typeof body !== 'object') {\n  body = cfg.samplePayload || {};\n  via = 'sample';\n}\n\n// Calendly nests the invitee; accept that shape too.\nconst invitee = body.payload && typeof body.payload === 'object' ? body.payload : body;\n\nconst clean = (v, max) => String(v == null ? '' : v).replace(/\\s+/g, ' ').trim().slice(0, max);\nconst firstOf = (...vals) => {\n  for (const v of vals) {\n    const s = clean(v, 400);\n    if (s) return s;\n  }\n  return '';\n};\n\nconst fullName = firstOf(invitee.name, body.name);\nconst firstName = clean(\n  firstOf(invitee.inviteeFirstName, invitee.firstName, invitee.first_name, fullName.split(' ')[0]),\n  60\n);\nconst email = clean(firstOf(invitee.inviteeEmail, invitee.email), 160);\nconst eventName = clean(\n  firstOf(\n    invitee.eventName,\n    invitee.event_name,\n    body.eventName,\n    (body.scheduled_event && body.scheduled_event.name)\n  ),\n  120\n);\nconst originalTime = clean(\n  firstOf(\n    invitee.originalTime,\n    invitee.original_time,\n    body.originalTime,\n    (body.scheduled_event && body.scheduled_event.start_time)\n  ),\n  80\n);\nconst rebookUrl = firstOf(invitee.rebookUrl, invitee.reschedule_url, body.rebookUrl, cfg.rebookUrl);\n\nif (!firstName) {\n  throw new Error(\n    'The no-show payload needs at least a first name. Expected POST body: ' +\n      '{ \"inviteeFirstName\": \"Sarah\", \"inviteeEmail\": \"sarah@example.com\", \"eventName\": \"Your webinar\", ' +\n      '\"originalTime\": \"2026-08-13T18:00:00Z\", \"rebookUrl\": \"https://calendly.com/...\" }'\n  );\n}\n\nreturn [{\n  json: {\n    found: true,\n    firstName,\n    email,\n    eventName,\n    originalTime,\n    rebookUrl,\n    rowNumber: null,\n    source: 'webhook',\n    receivedVia: via,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        220,
        140
      ],
      "id": "zvid-ns-0008",
      "name": "Webhook payload"
    },
    {
      "parameters": {
        "resource": "sheet",
        "operation": "read",
        "documentId": {
          "__rl": true,
          "mode": "list",
          "value": ""
        },
        "sheetName": {
          "__rl": true,
          "mode": "list",
          "value": ""
        },
        "options": {}
      },
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.5,
      "position": [
        220,
        480
      ],
      "id": "zvid-ns-0009",
      "name": "Read no-shows sheet",
      "alwaysOutputData": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "jsCode": "// The Google Sheets node emits one item per row plus a row_number field. Take\n// the FIRST row whose Status column is still empty — row_number is what lets the\n// write-back at the end mark exactly this row as sent.\nconst cfg = $('Config').first().json;\nconst isEmpty = (v) => v === undefined || v === null || String(v).trim() === '';\n\n// A POST landed on the webhook while Config still says source: \"sheet\". The\n// posted body was routed away from this branch, so rendering whatever row\n// happens to be next would answer the caller with a stranger's video. Say so\n// instead — the caller gets this message back from 'Respond ok'.\nlet hookRan = false;\ntry {\n  hookRan = Boolean($('No-show webhook').isExecuted);\n} catch (e) { /* not a webhook run */ }\nif (hookRan) {\n  return [{\n    json: {\n      found: false,\n      checked: 0,\n      source: 'sheet',\n      reason:\n        'A no-show was POSTed to the webhook, but Config.source is still \"sheet\", so the posted body ' +\n        'was ignored rather than rendering an unrelated sheet row. Set source: \"webhook\" in Config.',\n    },\n  }];\n}\n\nconst COLUMNS = ['FirstName', 'Email', 'EventName', 'OriginalTime', 'RebookUrl', 'Status', 'VideoUrl'];\n// 'Read no-shows sheet' has alwaysOutputData on, so an empty sheet still emits\n// one (empty) item and this guard runs instead of the branch dead-ending. Blank\n// spacer rows — the trailing rows a formatted sheet emits — are skipped rather\n// than picked, otherwise the same empty row would be chosen on every run and\n// the queue behind it would never advance.\nconst rows = $input\n  .all()\n  .map((i) => i.json || {})\n  .filter((r) => COLUMNS.some((c) => !isEmpty(r[c])));\nconst row = rows.find((r) => isEmpty(r.Status));\n\nif (!row) {\n  return [{\n    json: {\n      found: false,\n      checked: rows.length,\n      source: 'sheet',\n      reason: rows.length\n        ? 'No row with an empty Status column. Add fresh no-show rows to the sheet.'\n        : 'The sheet has no data rows yet — add a no-show under the header row (FirstName and EventName are the required columns).',\n    },\n  }];\n}\n\nconst clean = (v, max) => String(v == null ? '' : v).replace(/\\s+/g, ' ').trim().slice(0, max);\nconst firstName = clean(row.FirstName, 60);\nconst eventName = clean(row.EventName, 120);\n\nif (!firstName || !eventName) {\n  throw new Error(\n    'Row ' + row.row_number + ' is missing FirstName or EventName. Fill both columns, ' +\n      'or put anything in Status to skip the row.'\n  );\n}\n\nreturn [{\n  json: {\n    found: true,\n    firstName,\n    email: clean(row.Email, 160),\n    eventName,\n    originalTime: clean(row.OriginalTime, 80),\n    rebookUrl: clean(row.RebookUrl, 300) || clean(cfg.rebookUrl, 300),\n    rowNumber: row.row_number,\n    checked: rows.length,\n    source: 'sheet',\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        440,
        480
      ],
      "id": "zvid-ns-0010",
      "name": "Pick next no-show"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "no-show-found--cond",
              "leftValue": "={{ $json.found }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        660,
        300
      ],
      "id": "zvid-ns-0011",
      "name": "No-show found?"
    },
    {
      "parameters": {
        "jsCode": "// Nobody is waiting on a re-book video right now. That is a normal,\n// successful outcome — not an error. The upstream node says why: every row\n// already has a Status, the sheet is empty, the schedule fired while the\n// workflow is in webhook mode, or a POST arrived while it is in sheet mode.\nconst picked = ($input.first() || {}).json || {};\nreturn [{\n  json: {\n    rendered: false,\n    reason: picked.reason || 'No row with an empty Status column. Add fresh no-show rows to the sheet.',\n    rowsChecked: Number(picked.checked) || 0,\n    source: picked.source || null,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        880,
        500
      ],
      "id": "zvid-ns-0012",
      "name": "Nothing to send"
    },
    {
      "parameters": {
        "jsCode": "// === buildProject — identical to the frame-reviewed standalone builder ===\n// Turns one no-show (a webhook body or a sheet row) into a warm, zero-pressure\n// 1080x1920 re-book video: \"we missed you\" hook, a recap of what the session\n// covered with the presenter's face, then a friendly \"grab another slot\" card.\n// Every string that came from outside is length-capped and HTML-escaped before\n// it reaches the renderer.\nfunction buildProject(config, data, music) {\n  const cfg = config || {};\n  const d = data || {};\n\n  // ---- tiny helpers --------------------------------------------------------\n  const clean = (v, max) => String(v == null ? '' : v).replace(/\\s+/g, ' ').trim().slice(0, max || 200);\n  const esc = (s) =>\n    String(s == null ? '' : s).replace(/[&<>\"']/g, (c) => ({\n      '&': '&amp;', '<': '&lt;', '>': '&gt;', '\"': '&quot;', \"'\": '&#39;',\n    }[c]));\n  const round = (n) => Math.round(n * 100) / 100;\n\n  const hex = (v, fallback) => {\n    const m = /^#?([0-9a-fA-F]{6})$/.exec(String(v == null ? '' : v).trim());\n    return m ? '#' + m[1].toUpperCase() : fallback;\n  };\n  const rgb = (h) => {\n    const n = parseInt(h.slice(1), 16);\n    return [(n >> 16) & 255, (n >> 8) & 255, n & 255];\n  };\n  const rgba = (h, a) => {\n    const c = rgb(h);\n    return 'rgba(' + c[0] + ',' + c[1] + ',' + c[2] + ',' + a + ')';\n  };\n  // Blend two hexes; t = how much of `b` to mix in.\n  const mix = (a, b, t) => {\n    const x = rgb(a);\n    const y = rgb(b);\n    const p = (i) => Math.max(0, Math.min(255, Math.round(x[i] + (y[i] - x[i]) * t)));\n    return '#' + [p(0), p(1), p(2)].map((v) => v.toString(16).padStart(2, '0')).join('').toUpperCase();\n  };\n\n  // Greedy word-wrap estimate. `avgEm` is the average glyph advance as a\n  // fraction of the font size — measured against real renders of this layout\n  // (~0.50em for Fraunces, ~0.53em for Manrope).\n  const wrapLines = (text, fontPx, boxPx, avgEm) => {\n    const perLine = Math.max(6, Math.floor(boxPx / (fontPx * avgEm)));\n    const words = String(text).split(' ').filter(Boolean);\n    if (!words.length) return 1;\n    let lines = 1;\n    let used = 0;\n    for (const w of words) {\n      const need = used === 0 ? w.length : used + 1 + w.length;\n      if (need <= perLine) { used = need; continue; }\n      lines += 1;\n      used = Math.min(w.length, perLine);\n    }\n    return lines;\n  };\n\n  // Wrapping only helps where there is a space. A single unbreakable token —\n  // a 26-character first name, a hyphen-free compound word — can still be\n  // wider than its box and would run into the frame border. Step the size\n  // down (never below `minPx`) until the longest word fits.\n  const fitLongestWord = (text, fontPx, boxPx, avgEm, minPx) => {\n    const longest = String(text).split(' ').reduce((m, w) => Math.max(m, w.length), 0);\n    let size = fontPx;\n    while (size > minPx && longest * size * avgEm > boxPx) size -= 2;\n    return size;\n  };\n\n  // ---- palette + type ------------------------------------------------------\n  const paper = hex(cfg.paperColor, '#FBF3EC');\n  const ink = hex(cfg.inkColor, '#241C15');\n  const muted = hex(cfg.mutedColor, '#786557');\n  const accent = hex(cfg.brandAccent, '#B0472A');\n  const surface = mix(paper, '#FFFFFF', 0.7);      // card fill on the paper scenes\n  const accentSoft = mix(accent, paper, 0.45);      // accent that still reads on ink\n  const inkGlow = mix(ink, accent, 0.22);           // warm centre of the CTA backdrop\n  const paperDim = mix(paper, ink, 0.32);           // secondary text on ink\n  const headFont = clean(cfg.headingFont, 40) || 'Fraunces';\n  const bodyFont = clean(cfg.bodyFont, 40) || 'Manrope';\n\n  // ---- the no-show ---------------------------------------------------------\n  const firstName = clean(d.firstName, 32) || 'there';\n  const eventName = clean(d.eventName, 78) || 'the session';\n  const rebookUrl = clean(d.rebookUrl, 200) || clean(cfg.rebookUrl, 200);\n  const brand = clean(cfg.brandName, 40) || 'Us';\n  const presenter = clean(cfg.presenterName, 42) || brand;\n  const presenterRole = clean(cfg.presenterRole, 52);\n\n  // `originalTime` is formatted, never computed. An ISO timestamp (what\n  // Calendly posts) becomes \"Thu, Aug 13 . 6:00 PM UTC\" in the configured zone;\n  // anything else — including a hand-typed sheet cell — is passed through as is.\n  const formatWhen = (raw, tz) => {\n    const s = clean(raw, 80);\n    if (!s) return '';\n    if (!/^\\d{4}-\\d{2}-\\d{2}[T ]/.test(s)) return s;\n    const when = new Date(s.replace(' ', 'T'));\n    if (isNaN(when.getTime())) return s;\n    const zone = clean(tz, 60) || 'UTC';\n    try {\n      const day = when.toLocaleDateString('en-US', { timeZone: zone, weekday: 'short', month: 'short', day: 'numeric' });\n      const time = when.toLocaleTimeString('en-US', { timeZone: zone, hour: 'numeric', minute: '2-digit', timeZoneName: 'short' });\n      return day + ' · ' + time;\n    } catch (e) {\n      // An unknown IANA zone must never fail the render — fall back to UTC.\n      const day = when.toLocaleDateString('en-US', { timeZone: 'UTC', weekday: 'short', month: 'short', day: 'numeric' });\n      const time = when.toLocaleTimeString('en-US', { timeZone: 'UTC', hour: 'numeric', minute: '2-digit' });\n      return day + ' · ' + time + ' UTC';\n    }\n  };\n  const originalTime = formatWhen(d.originalTime, cfg.timeZone);\n\n  // Config copy may carry {firstName} / {eventName} / {originalTime} /\n  // {presenter} / {brand} tokens. Substitute the raw values first, escape the\n  // whole string afterwards, so a name containing \"&\" can never break markup.\n  const fill = (s) =>\n    String(s == null ? '' : s)\n      .replace(/\\{firstName\\}/g, firstName)\n      .replace(/\\{eventName\\}/g, eventName)\n      .replace(/\\{originalTime\\}/g, originalTime || 'the other day')\n      .replace(/\\{presenter\\}/g, presenter)\n      .replace(/\\{brand\\}/g, brand);\n  const tok = (s, max) => clean(fill(s), max);\n\n  // Anything that has to fit on ONE line gets cut on a whole word, never\n  // mid-word, and never silently overflows its box.\n  const ellipsize = (s, max) => {\n    const t = String(s);\n    if (t.length <= max) return t;\n    const cut = t.slice(0, max - 1);\n    const sp = cut.lastIndexOf(' ');\n    return (sp > max * 0.6 ? cut.slice(0, sp) : cut).replace(/[\\s.,;:]+$/, '') + '…';\n  };\n\n  // The booking link is shown as a bare domain (plus the path when it is short\n  // enough to read at a glance), never as a raw URL and never cut mid-word.\n  const bareUrl = rebookUrl\n    .replace(/^https?:\\/\\//i, '')\n    .replace(/^www\\./i, '')\n    .replace(/[?#].*$/, '')\n    .replace(/\\/+$/, '');\n  const rebookLabel = bareUrl.length <= 34 ? bareUrl : bareUrl.split('/')[0].slice(0, 34);\n\n  // ---- copy ----------------------------------------------------------------\n  const kicker = ellipsize(tok(cfg.kickerText || 'A note from {presenter}', 46), 40).toUpperCase();\n  const hookLine = tok(cfg.hookLine || 'We missed you, {firstName}.', 58);\n  const hookSub = ellipsize(\n    tok(cfg.hookSub || '{eventName} ran on {originalTime}. No stress — here is the short version, and a new slot whenever you like.', 220),\n    168\n  );\n  const sessionLabel = tok(cfg.sessionLabel || 'The session you booked', 34).toUpperCase();\n  const highlightsTitle = tok(cfg.highlightsTitle || \"Here's what we covered\", 46);\n  const highlights = (Array.isArray(cfg.highlights) ? cfg.highlights : [])\n    .map((h) => tok(h, 96))\n    .filter(Boolean)\n    .slice(0, 3);\n  if (!highlights.length) highlights.push('A walk-through of the whole thing, start to finish.');\n  const footnote = ellipsize(tok(cfg.highlightsFootnote || 'Same session, new time — there is nothing to catch up on.', 140), 100);\n  const ctaHeadline = tok(cfg.ctaHeadline || 'Ready when you are, {firstName}.', 58);\n  const ctaSub = ellipsize(tok(cfg.ctaSub || 'Pick a time that actually works for you — it takes about 30 seconds.', 160), 116);\n  const ctaText = tok(cfg.ctaText || 'Grab another slot', 30);\n  const signOff = ellipsize(tok(cfg.signOff || '{presenter} · {brand}', 60), 52);\n  const brandMark = ellipsize(brand, 30);\n\n  // ================= scene 1 — \"we missed you\" ==============================\n  const S1_TR = 0.6;\n  const hookLen = hookLine.length;\n  const headSize = fitLongestWord(hookLine, hookLen <= 22 ? 96 : hookLen <= 32 ? 84 : hookLen <= 46 ? 70 : 58, 900, 0.52, 34);\n  const headLineH = Math.round(headSize * 1.08);\n  const headLines = wrapLines(hookLine, headSize, 900, 0.52);\n  const subSize = hookSub.length <= 150 ? 34 : 31;\n  const subLineH = Math.round(subSize * 1.42);\n  const subLines = wrapLines(hookSub, subSize, 840, 0.53);\n\n  const evSize = fitLongestWord(eventName, eventName.length <= 34 ? 46 : eventName.length <= 56 ? 40 : 35, 776, 0.53, 22);\n  const evLineH = Math.round(evSize * 1.24);\n  const evLines = wrapLines(eventName, evSize, 776, 0.53);\n  const cardH = 88 + 139 + evLines * evLineH;\n  const cardY = 1330;\n\n  // Centre the kicker + headline + subline block in the space above the card.\n  const midH = 32 + 30 + headLines * headLineH + 46 + subLines * subLineH;\n  const kickerY = Math.round(250 + Math.max(0, (cardY - 90 - 250 - midH) / 2));\n  const headY = kickerY + 62;\n  const subY = headY + headLines * headLineH + 46;\n\n  const s1Content = round(Math.min(Math.max(4.2, 3.4 + subLines * 0.35 + headLines * 0.2), 5.4));\n  const s1 = round(s1Content + S1_TR);\n\n  const s1Svg =\n    \"<svg width='1080' height='1920' viewBox='0 0 1080 1920' xmlns='http://www.w3.org/2000/svg'>\" +\n    \"<defs><radialGradient id='nsrHalo' cx='0.5' cy='0.42' r='0.62'>\" +\n    \"<stop offset='0' stop-color='\" + accent + \"' stop-opacity='0.13'/>\" +\n    \"<stop offset='1' stop-color='\" + accent + \"' stop-opacity='0'/></radialGradient></defs>\" +\n    \"<rect width='1080' height='1920' fill='\" + paper + \"'/>\" +\n    \"<circle cx='1064' cy='120' r='250' fill='\" + rgba(accent, 0.055) + \"'/>\" +\n    \"<circle cx='24' cy='1836' r='300' fill='\" + rgba(accent, 0.05) + \"'/>\" +\n    \"<rect width='1080' height='1920' fill='url(#nsrHalo)'/>\" +\n    \"<rect x='48' y='48' width='984' height='1824' rx='48' fill='none' stroke='\" + rgba(ink, 0.1) + \"' stroke-width='1.5'/>\" +\n    \"<rect x='506' y='\" + (kickerY - 36) + \"' width='68' height='5' rx='2.5' fill='\" + accent + \"'/>\" +\n    '</svg>';\n\n  const monogram = (s) => esc((String(s).replace(/[^A-Za-z0-9]/g, '')[0] || 'Z').toUpperCase());\n\n  const s1Visuals = [\n    { type: 'SVG', width: 1080, height: 1920, track: 0, enterBegin: 0, exitEnd: s1, svg: s1Svg },\n    {\n      // Brand, kicker and headline carry NO enter animation on purpose: frame 0\n      // is the poster an email client shows before anyone presses play, so it\n      // has to read as a finished title card, not an empty page.\n      // It also leaves 0.15 s BEFORE the scene transition starts: scene 2's\n      // header sits at the same height, and a wipe with two headers on screen\n      // reads as a glitch.\n      // NOTE: TEXT elements never declare `height` anywhere in this builder. Every\n      // card's box is set on its inner div instead. The layout linter can only\n      // recognise the full-frame SVG as a backdrop when it knows the canvas size,\n      // and it does NOT when `resolution` is a named preset — so a TEXT with a\n      // resolvable height would be reported as \"overlapping\" the backdrop on\n      // every single element. Height-less TEXT keeps that lint clean and renders\n      // identically, because the inner div carries the real box.\n      type: 'TEXT', x: 104, y: 124, width: 872, anchor: 'top-left', track: 1,\n      enterBegin: 0, exitBegin: round(s1 - 1.0), exitEnd: round(s1 - S1_TR - 0.02), exitAnimation: 'fade',\n      style: { fontFamily: bodyFont, color: ink },\n      html:\n        \"<div style='display:flex; align-items:center; gap:20px;'>\" +\n        \"<div style='width:72px; height:72px; border-radius:22px; background:\" + accent + '; color:' + paper +\n        \"; font-size:36px; font-weight:800; display:flex; align-items:center; justify-content:center;'>\" + monogram(brand) + '</div>' +\n        \"<div style='color:\" + ink + \"; font-size:34px; font-weight:800; letter-spacing:1px;'>\" + esc(brandMark) + '</div></div>',\n    },\n    {\n      type: 'TEXT', x: 540, y: kickerY, width: 900, anchor: 'top-center', track: 2,\n      enterBegin: 0, exitEnd: s1,\n      style: { fontFamily: bodyFont, color: accent },\n      html: \"<div style='text-align:center; color:\" + accent + \"; font-size:25px; font-weight:800; letter-spacing:7px;'>\" + esc(kicker) + '</div>',\n    },\n    {\n      type: 'TEXT', x: 540, y: headY, width: 900, anchor: 'top-center', track: 3,\n      enterBegin: 0, exitEnd: s1,\n      style: { fontFamily: headFont, color: ink },\n      html:\n        \"<div style='text-align:center; color:\" + ink + '; font-size:' + headSize + 'px; line-height:' + headLineH +\n        \"px; font-weight:700; letter-spacing:-1.5px;'>\" + esc(hookLine) + '</div>',\n    },\n    {\n      type: 'TEXT', x: 540, y: subY, width: 840, anchor: 'top-center', track: 4,\n      enterBegin: 0.45, enterEnd: 1.0, enterAnimation: 'fade', exitEnd: s1,\n      style: { fontFamily: bodyFont, color: muted },\n      html: \"<div style='text-align:center; color:\" + muted + '; font-size:' + subSize + 'px; line-height:' + subLineH +\n        \"px; font-weight:500;'>\" + esc(hookSub) + '</div>',\n    },\n    {\n      type: 'TEXT', x: 540, y: cardY, width: 872, anchor: 'top-center', track: 5,\n      enterBegin: 1.05, enterEnd: 1.65, enterAnimation: 'smoothup', exitEnd: s1,\n      style: { fontFamily: bodyFont, color: ink },\n      html:\n        \"<div style='box-sizing:border-box; width:872px; height:\" + cardH + 'px; padding:44px 48px; background:' + surface +\n        '; border:1.5px solid ' + rgba(accent, 0.32) + \"; border-radius:40px;'>\" +\n        \"<div style='color:\" + accent + \"; font-size:22px; font-weight:800; letter-spacing:5px;'>\" + esc(sessionLabel) + '</div>' +\n        \"<div style='margin-top:18px; color:\" + ink + '; font-size:' + evSize + 'px; line-height:' + evLineH +\n        \"px; font-weight:800; letter-spacing:-0.5px;'>\" + esc(eventName) + '</div>' +\n        \"<div style='margin-top:28px; height:1px; background:\" + rgba(ink, 0.14) + \"'></div>\" +\n        \"<div style='margin-top:27px; color:\" + muted + \"; font-size:30px; font-weight:600;'>\" +\n        (originalTime ? esc(originalTime) : 'Your original slot') + '</div></div>',\n    },\n  ];\n\n  // ================= scene 2 — what we covered ==============================\n  const S2_TR = 0.6;\n  const titleLen = highlightsTitle.length;\n  const titleSize = fitLongestWord(highlightsTitle, titleLen <= 26 ? 74 : titleLen <= 44 ? 62 : 52, 880, 0.52, 32);\n  const titleLineH = Math.round(titleSize * 1.1);\n  const titleLines = wrapLines(highlightsTitle, titleSize, 880, 0.52);\n\n  const hlMaxLen = highlights.reduce((m, h) => Math.max(m, h.length), 0);\n  const hlSize = fitLongestWord(highlights.join(' '), hlMaxLen <= 44 ? 34 : hlMaxLen <= 70 ? 30 : hlMaxLen <= 96 ? 27 : 24, 688, 0.53, 18);\n  const hlLineH = Math.round(hlSize * 1.44);\n  const hlLines = highlights.map((h) => wrapLines(h, hlSize, 688, 0.53));\n  const hlCardH = Math.max(196, 96 + Math.max.apply(null, hlLines) * hlLineH);\n\n  const kicker2Y = 176;\n  const title2Y = 238;\n  const cardsY = title2Y + titleLines * titleLineH + 118;\n  const hlGap = 44;\n  const stackH = highlights.length * hlCardH + (highlights.length - 1) * hlGap;\n\n  const presLabelY = cardsY + stackH + 104;\n  const avatarSize = 156;\n  const avatarY = presLabelY + 56;\n  const presNameY = avatarY + avatarSize + 32;\n  const presRoleY = presNameY + 56;\n  const footY = presRoleY + (presenterRole ? 46 : 0) + 62;\n\n  const totalHlChars = highlights.reduce((m, h) => m + h.length, 0);\n  const s2Content = round(Math.min(Math.max(4.8, 3.0 + highlights.length * 0.8 + totalHlChars * 0.004), 7.2));\n  const s2 = round(s2Content + S2_TR);\n\n  const s2Svg =\n    \"<svg width='1080' height='1920' viewBox='0 0 1080 1920' xmlns='http://www.w3.org/2000/svg'>\" +\n    \"<defs><radialGradient id='nsrPaper2' cx='0.22' cy='0.12' r='1.05'>\" +\n    \"<stop offset='0' stop-color='\" + mix(paper, accent, 0.09) + \"'/>\" +\n    \"<stop offset='1' stop-color='\" + paper + \"'/></radialGradient></defs>\" +\n    \"<rect width='1080' height='1920' fill='url(#nsrPaper2)'/>\" +\n    \"<circle cx='1040' cy='1880' r='260' fill='\" + rgba(accent, 0.05) + \"'/>\" +\n    \"<rect x='48' y='48' width='984' height='1824' rx='48' fill='none' stroke='\" + rgba(ink, 0.1) + \"' stroke-width='1.5'/>\" +\n    \"<rect x='506' y='\" + (kicker2Y - 34) + \"' width='68' height='5' rx='2.5' fill='\" + accent + \"'/>\" +\n    '</svg>';\n\n  const s2Visuals = [\n    { type: 'SVG', width: 1080, height: 1920, track: 0, enterBegin: 0, exitEnd: s2, svg: s2Svg },\n    {\n      type: 'TEXT', x: 540, y: kicker2Y, width: 880, anchor: 'top-center', track: 1,\n      enterBegin: 0.1, enterEnd: 0.5, enterAnimation: 'fade', exitEnd: s2,\n      style: { fontFamily: bodyFont, color: accent },\n      html: \"<div style='text-align:center; color:\" + accent + \"; font-size:25px; font-weight:800; letter-spacing:7px;'>\" +\n        esc(ellipsize(eventName, 38).toUpperCase()) + '</div>',\n    },\n    {\n      type: 'TEXT', x: 540, y: title2Y, width: 880, anchor: 'top-center', track: 2,\n      enterBegin: 0.25, enterEnd: 0.75, enterAnimation: 'smoothup', exitEnd: s2,\n      style: { fontFamily: headFont, color: ink },\n      html: \"<div style='text-align:center; color:\" + ink + '; font-size:' + titleSize + 'px; line-height:' + titleLineH +\n        \"px; font-weight:700; letter-spacing:-1px;'>\" + esc(highlightsTitle) + '</div>',\n    },\n  ];\n\n  highlights.forEach((h, i) => {\n    s2Visuals.push({\n      type: 'TEXT', x: 540, y: cardsY + i * (hlCardH + hlGap), width: 872, anchor: 'top-center', track: 3 + i,\n      enterBegin: round(0.65 + i * 0.26), enterEnd: round(1.15 + i * 0.26), enterAnimation: 'smoothleft', exitEnd: s2,\n      style: { fontFamily: bodyFont, color: ink },\n      html:\n        \"<div style='box-sizing:border-box; width:872px; height:\" + hlCardH +\n        'px; padding:0 44px; background:' + surface + '; border:1.5px solid ' + rgba(accent, 0.28) +\n        \"; border-radius:34px; display:flex; align-items:center; gap:28px;'>\" +\n        \"<div style='flex:0 0 auto; width:68px; height:68px; border-radius:999px; background:\" + rgba(accent, 0.14) +\n        '; border:2px solid ' + accent + '; color:' + accent +\n        \"; font-size:30px; font-weight:800; display:flex; align-items:center; justify-content:center;'>\" + (i + 1) + '</div>' +\n        \"<div style='flex:1 1 auto; color:\" + ink + '; font-size:' + hlSize + 'px; line-height:' + hlLineH +\n        \"px; font-weight:600;'>\" + esc(h) + '</div></div>',\n    });\n  });\n\n  s2Visuals.push({\n    type: 'TEXT', x: 540, y: presLabelY, width: 800, anchor: 'top-center', track: 7,\n    enterBegin: 1.5, enterEnd: 1.9, enterAnimation: 'fade', exitEnd: s2,\n    style: { fontFamily: bodyFont, color: muted },\n    html: \"<div style='text-align:center; color:\" + muted + \"; font-size:22px; font-weight:800; letter-spacing:6px;'>HOSTED BY</div>\",\n  });\n\n  const presenterImage = clean(cfg.presenterImageUrl, 400);\n  const hasPhoto = /^https:\\/\\//i.test(presenterImage);\n  if (hasPhoto) {\n    // The halo sits on its own element (not in the scene SVG) so it appears\n    // with the photo instead of hanging there empty for the first 1.6 s.\n    s2Visuals.push({\n      type: 'TEXT', x: 540, y: avatarY - 11, width: avatarSize + 22, anchor: 'top-center', track: 8,\n      enterBegin: 1.6, enterEnd: 2.05, enterAnimation: 'circleopen', exitEnd: s2,\n      style: { fontFamily: bodyFont, color: accent },\n      html:\n        \"<div style='box-sizing:border-box; width:\" + (avatarSize + 22) + 'px; height:' + (avatarSize + 22) +\n        'px; border-radius:999px; background:' + surface + '; border:2px solid ' + rgba(accent, 0.34) + \";'></div>\",\n    });\n    // NOTE: no `resize` here. The renderer's `resize` recomputes width/height\n    // against the whole CANVAS, which blows a 156 px avatar up to full-frame.\n    // Without it, width/height are honoured and `radius` masks a clean circle —\n    // so the presenter photo must be square (documented in the setup notes).\n    s2Visuals.push({\n      type: 'IMAGE', src: presenterImage,\n      x: 540 - avatarSize / 2, y: avatarY, width: avatarSize, height: avatarSize,\n      radius: { tl: avatarSize / 2, tr: avatarSize / 2, bl: avatarSize / 2, br: avatarSize / 2 },\n      anchor: 'top-left', track: 9, enterBegin: 1.6, enterEnd: 2.05, enterAnimation: 'circleopen', exitEnd: s2,\n    });\n  } else {\n    s2Visuals.push({\n      type: 'TEXT', x: 540, y: avatarY, width: avatarSize, anchor: 'top-center', track: 9,\n      enterBegin: 1.6, enterEnd: 2.05, enterAnimation: 'circleopen', exitEnd: s2,\n      style: { fontFamily: bodyFont, color: accent },\n      html:\n        \"<div style='box-sizing:border-box; width:\" + avatarSize + 'px; height:' + avatarSize +\n        'px; border-radius:999px; background:' + rgba(accent, 0.14) + '; border:2.5px solid ' + accent + '; color:' + accent +\n        \"; font-size:62px; font-weight:800; display:flex; align-items:center; justify-content:center;'>\" + monogram(presenter) + '</div>',\n    });\n  }\n\n  s2Visuals.push({\n    type: 'TEXT', x: 540, y: presNameY, width: 860, anchor: 'top-center', track: 10,\n    enterBegin: 1.8, enterEnd: 2.2, enterAnimation: 'smoothup', exitEnd: s2,\n    style: { fontFamily: bodyFont, color: ink },\n    html: \"<div style='text-align:center; color:\" + ink + \"; font-size:42px; line-height:50px; font-weight:800; letter-spacing:-0.5px;'>\" +\n      esc(presenter) + '</div>',\n  });\n\n  if (presenterRole) {\n    s2Visuals.push({\n      type: 'TEXT', x: 540, y: presRoleY, width: 860, anchor: 'top-center', track: 11,\n      enterBegin: 1.95, enterEnd: 2.35, enterAnimation: 'fade', exitEnd: s2,\n      style: { fontFamily: bodyFont, color: muted },\n      html: \"<div style='text-align:center; color:\" + muted + \"; font-size:28px; line-height:38px; font-weight:600;'>\" + esc(presenterRole) + '</div>',\n    });\n  }\n\n  s2Visuals.push({\n    type: 'TEXT', x: 540, y: footY, width: 820, anchor: 'top-center', track: 12,\n    enterBegin: 2.2, enterEnd: 2.6, enterAnimation: 'fade', exitEnd: s2,\n    style: { fontFamily: bodyFont, color: muted },\n    html: \"<div style='text-align:center; color:\" + muted + \"; font-size:26px; line-height:38px; font-weight:500;'>\" + esc(footnote) + '</div>',\n  });\n\n  // ================= scene 3 — grab another slot ============================\n  const ctaLen = ctaHeadline.length;\n  const ctaSize = fitLongestWord(ctaHeadline, ctaLen <= 24 ? 88 : ctaLen <= 34 ? 76 : ctaLen <= 46 ? 64 : 54, 900, 0.52, 34);\n  const ctaLineH = Math.round(ctaSize * 1.08);\n  const ctaLines = wrapLines(ctaHeadline, ctaSize, 900, 0.52);\n  const ctaSubLines = wrapLines(ctaSub, 32, 820, 0.53);\n\n  const ctaHeadY = 830;\n  const ctaSubY = ctaHeadY + ctaLines * ctaLineH + 44;\n  const pillY = ctaSubY + ctaSubLines * 46 + 92;\n  const domainY = pillY + 142;\n  const s3 = 4.4;\n\n  const s3Svg =\n    \"<svg width='1080' height='1920' viewBox='0 0 1080 1920' xmlns='http://www.w3.org/2000/svg'>\" +\n    \"<defs><radialGradient id='nsrCta' cx='0.5' cy='0.3' r='0.95'>\" +\n    \"<stop offset='0' stop-color='\" + inkGlow + \"'/><stop offset='1' stop-color='\" + ink + \"'/></radialGradient></defs>\" +\n    \"<rect width='1080' height='1920' fill='url(#nsrCta)'/>\" +\n    \"<rect x='48' y='48' width='984' height='1824' rx='48' fill='none' stroke='\" + rgba(paper, 0.16) + \"' stroke-width='1.5'/>\" +\n    \"<circle cx='540' cy='520' r='170' fill='none' stroke='\" + rgba(accentSoft, 0.22) + \"' stroke-width='1.5'/>\" +\n    \"<circle cx='540' cy='520' r='148' fill='none' stroke='\" + rgba(accentSoft, 0.75) + \"' stroke-width='2.5'/>\" +\n    \"<rect x='470' y='474' width='140' height='120' rx='20' fill='none' stroke='\" + accentSoft + \"' stroke-width='6'/>\" +\n    \"<path d='M502 456 V486 M578 456 V486' stroke='\" + accentSoft + \"' stroke-width='9' stroke-linecap='round'/>\" +\n    \"<path d='M470 516 H610' stroke='\" + accentSoft + \"' stroke-width='5'/>\" +\n    \"<path d='M504 550 L528 574 L578 528' fill='none' stroke='\" + accentSoft + \"' stroke-width='10' stroke-linecap='round' stroke-linejoin='round'/>\" +\n    \"<path d='M420 1560 H660' stroke='\" + rgba(paper, 0.22) + \"' stroke-width='1.5'/>\" +\n    '</svg>';\n\n  const s3Visuals = [\n    { type: 'SVG', width: 1080, height: 1920, track: 0, enterBegin: 0, exitEnd: s3, svg: s3Svg },\n    {\n      type: 'TEXT', x: 540, y: ctaHeadY, width: 900, anchor: 'top-center', track: 1,\n      enterBegin: 0.3, enterEnd: 0.85, enterAnimation: 'smoothup', exitEnd: s3,\n      style: { fontFamily: headFont, color: paper },\n      html: \"<div style='text-align:center; color:\" + paper + '; font-size:' + ctaSize + 'px; line-height:' + ctaLineH +\n        \"px; font-weight:700; letter-spacing:-1.5px;'>\" + esc(ctaHeadline) + '</div>',\n    },\n    {\n      type: 'TEXT', x: 540, y: ctaSubY, width: 820, anchor: 'top-center', track: 2,\n      enterBegin: 0.7, enterEnd: 1.15, enterAnimation: 'fade', exitEnd: s3,\n      style: { fontFamily: bodyFont, color: paperDim },\n      html: \"<div style='text-align:center; color:\" + paperDim + \"; font-size:32px; line-height:46px; font-weight:500;'>\" + esc(ctaSub) + '</div>',\n    },\n    {\n      type: 'TEXT', x: 540, y: pillY, width: 900, anchor: 'top-center', track: 3,\n      enterBegin: 1.05, enterEnd: 1.5, enterAnimation: 'circleopen', exitEnd: s3,\n      style: { fontFamily: bodyFont, color: paper },\n      html:\n        \"<div style='text-align:center;'><div style='display:inline-block; background:\" + accent + '; color:' + paper +\n        \"; border-radius:999px; padding:28px 68px; font-size:40px; font-weight:800; letter-spacing:-0.5px;'>\" + esc(ctaText) + '</div></div>',\n    },\n  ];\n\n  if (rebookLabel) {\n    s3Visuals.push({\n      type: 'TEXT', x: 540, y: domainY, width: 900, anchor: 'top-center', track: 4,\n      enterBegin: 1.4, enterEnd: 1.8, enterAnimation: 'fade', exitEnd: s3,\n      style: { fontFamily: bodyFont, color: paperDim },\n      html: \"<div style='text-align:center; color:\" + paperDim + \"; font-size:28px; font-weight:700; letter-spacing:4px;'>\" +\n        esc(rebookLabel.toUpperCase()) + '</div>',\n    });\n  }\n\n  s3Visuals.push({\n    type: 'TEXT', x: 540, y: 1620, width: 900, anchor: 'top-center', track: 5,\n    enterBegin: 1.7, enterEnd: 2.1, enterAnimation: 'fade', exitEnd: s3,\n    style: { fontFamily: bodyFont, color: paperDim },\n    html: \"<div style='text-align:center; color:\" + paperDim + \"; font-size:27px; font-weight:600; letter-spacing:2px;'>&mdash; \" +\n      esc(signOff) + '</div>',\n  });\n\n  // ---- assemble ------------------------------------------------------------\n  const scenes = [\n    { id: 'missed', duration: s1, backgroundColor: paper, transition: 'smoothup', transitionId: 'recap', transitionDuration: S1_TR, visuals: s1Visuals },\n    { id: 'recap', duration: s2, backgroundColor: paper, transition: 'fade', transitionId: 'rebook', transitionDuration: S2_TR, visuals: s2Visuals },\n    { id: 'rebook', duration: s3, backgroundColor: ink, visuals: s3Visuals },\n  ];\n\n  // The render API only allows letters, digits, spaces, _ and - in `name`.\n  const safeName = ('no-show-' + firstName + '-' + eventName)\n    .toLowerCase().normalize('NFKD').replace(/[\\u0300-\\u036f]/g, '')\n    .replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '').slice(0, 60);\n\n  const payload = {\n    name: safeName || 'no-show-reengagement',\n    resolution: 'instagram-reel',\n    frameRate: Number(cfg.frameRate ?? 30),\n    backgroundColor: paper,\n    outputFormat: 'mp4',\n    scenes,\n  };\n  // NOTE: no `track` on audios — the render API rejects audios[].track.\n  if (music && music.src) {\n    payload.audios = [{ src: music.src, volume: Number(cfg.musicVolume ?? 0.14) }];\n  }\n\n  const totalSeconds = round(s1 + s2 + s3 - S1_TR - S2_TR);\n  const lowestPixel = Math.max(cardY + cardH, footY + 40, 1660);\n\n  return {\n    payload,\n    meta: {\n      firstName,\n      eventName,\n      originalTime,\n      rebookUrl,\n      rebookLabel,\n      presenter,\n      brand,\n      highlightCount: highlights.length,\n      avatarMode: hasPhoto ? 'photo' : 'monogram',\n      hasMusic: Boolean(music && music.src),\n      sceneSeconds: [s1, s2, s3],\n      totalSeconds,\n      headSize,\n      headLines,\n      subLines,\n      titleSize,\n      titleLines,\n      hlSize,\n      hlCardH,\n      evSize,\n      evLines,\n      cardY,\n      cardH,\n      cardsY,\n      presLabelY,\n      footY,\n      ctaSize,\n      ctaLines,\n      lowestPixel,\n      visualCount: s1Visuals.length + s2Visuals.length + s3Visuals.length,\n    },\n  };\n}\n\n// === n8n glue ===\nconst cfg = $('Config').first().json;\nconst guard = $('Music guard').first().json;\nconst noShow = $input.first().json;\nconst built = buildProject(cfg, noShow, guard.music);\nbuilt.meta.rowNumber = noShow.rowNumber == null ? null : noShow.rowNumber;\nbuilt.meta.source = noShow.source || 'sheet';\nbuilt.meta.email = noShow.email || '';\nbuilt.meta.musicNote = guard.musicNote;\nreturn [{ json: built }];\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        880,
        220
      ],
      "id": "zvid-ns-0013",
      "name": "Build project JSON"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $('Config').first().json.apiUrl }}/api/render/validate/api-key",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ payload: $json.payload }) }}",
        "options": {
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": true
            }
          }
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1100,
        220
      ],
      "id": "zvid-ns-0014",
      "name": "Validate project (free)"
    },
    {
      "parameters": {
        "jsCode": "// The free validate endpoint runs the exact pipeline a render submission runs,\n// so a failure here is a payload bug — surface the field list instead of a\n// generic HTTP error.\n// The node ships with fullResponse on, so the item is the\n// { statusCode, headers, body } envelope. Tolerate the raw body too, in case\n// that option is ever turned off — but keep an explicit status guard so a 401,\n// 403 or 5xx throws something actionable instead of sliding through as an\n// empty result.\nconst res = $input.first().json || {};\nconst hasEnvelope = typeof res === 'object' && res !== null && 'statusCode' in res;\nconst status = hasEnvelope ? Number(res.statusCode) : 200;\nconst body = (hasEnvelope ? res.body : res) || {};\n\nif (status !== 200) {\n  const details = Array.isArray(body.details)\n    ? body.details.map((d) => '  - ' + d.field + ': ' + d.message).join('\\n')\n    : JSON.stringify(body).slice(0, 500);\n  throw new Error('Zvid rejected the project (HTTP ' + status + '): ' + (body.message || body.error) + '\\n' + details);\n}\n\nconst build = $('Build project JSON').first().json;\nreturn [{\n  json: {\n    ...build,\n    creditsRequired: body.creditsRequired,\n    warnings: body.warnings || [],\n    schemaVersion: body.schemaVersion,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1320,
        220
      ],
      "id": "zvid-ns-0015",
      "name": "Check validation"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "dry-run--cond",
              "leftValue": "={{ $('Config').first().json.dryRun }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        1540,
        220
      ],
      "id": "zvid-ns-0016",
      "name": "Dry run?"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $('Config').first().json.apiUrl }}/api/projects",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ name: $json.payload.name, payload: $json.payload }) }}",
        "options": {}
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1760,
        60
      ],
      "id": "zvid-ns-0017",
      "name": "Save draft to editor",
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "jsCode": "// Dry run: everything except the paid render. Open editorLink to watch the\n// exact video, then set dryRun to false in Config to go live. The sheet row is\n// NOT marked sent on a dry run, so the next real run picks the same person.\nconst cfg = $('Config').first().json;\nconst d = $('Check validation').first().json;\nconst guard = $('Music guard').first().json;\n\n// The draft save is best-effort (onError: continue) so a hiccup there never\n// hides the rest of the dry-run report.\nlet editorLink = null;\nlet draftId = null;\ntry {\n  const project = $('Save draft to editor').first().json.project;\n  if (project && project.id) {\n    draftId = project.id;\n    editorLink = String(cfg.editorUrl || 'https://editor.zvid.io').replace(/\\/+$/, '') + '/?project=' + project.id;\n  }\n} catch (e) { /* draft step skipped or failed */ }\n\nreturn [{\n  json: {\n    dryRun: true,\n    rendered: false,\n    videoUrl: null,\n    firstName: d.meta.firstName,\n    eventName: d.meta.eventName,\n    presenter: d.meta.presenter,\n    avatarMode: d.meta.avatarMode,\n    rebookLabel: d.meta.rebookLabel,\n    videoSeconds: d.meta.totalSeconds,\n    creditsRequired: d.creditsRequired,\n    warnings: d.warnings,\n    music: guard.musicNote,\n    sheetRow: d.meta.rowNumber,\n    sheetUpdated: false,\n    emailSent: false,\n    editorLink,\n    draftId,\n    nextStep: editorLink\n      ? 'Open editorLink to preview the draft, then set dryRun to false in Config to render for real.'\n      : 'Set dryRun to false in Config to render this for real.',\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1980,
        60
      ],
      "id": "zvid-ns-0018",
      "name": "Dry run summary"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $('Config').first().json.apiUrl }}/api/render/api-key",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ payload: $json.payload }) }}",
        "options": {}
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1760,
        400
      ],
      "id": "zvid-ns-0019",
      "name": "Submit render",
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "amount": "={{ $('Config').first().json.pollSeconds }}",
        "unit": "seconds"
      },
      "type": "n8n-nodes-base.wait",
      "typeVersion": 1.1,
      "position": [
        1980,
        400
      ],
      "id": "zvid-ns-0020",
      "name": "Wait",
      "webhookId": "d7c4b0e9-53a1-4f28-8b6d-0e9a3c15f472"
    },
    {
      "parameters": {
        "url": "={{ $('Config').first().json.apiUrl }}/api/jobs/{{ $('Submit render').first().json.jobId }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "options": {}
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2200,
        400
      ],
      "id": "zvid-ns-0021",
      "name": "Get render status",
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "render-finished--cond",
              "leftValue": "={{ $json.state === 'completed' }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        2420,
        400
      ],
      "id": "zvid-ns-0022",
      "name": "Render finished?"
    },
    {
      "parameters": {
        "jsCode": "// Fails fast on a failed render and stops the poll loop from running forever.\nconst cfg = $('Config').first().json;\nconst job = $input.first().json;\n\nif (job.state === 'failed') {\n  throw new Error('Render failed: ' + (job.failedReason || 'no reason given'));\n}\n\nconst maxPolls = Math.ceil((Number(cfg.timeoutMinutes || 20) * 60) / Number(cfg.pollSeconds || 10));\nif ($runIndex + 1 >= maxPolls) {\n  throw new Error('Render did not finish within ' + cfg.timeoutMinutes + ' minutes (job ' + job.id + ', state ' + job.state + ').');\n}\n\nreturn $input.all();"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2420,
        620
      ],
      "id": "zvid-ns-0023",
      "name": "Still rendering?"
    },
    {
      "parameters": {
        "jsCode": "// The render is done: pull the video URL out of the job and build the re-book\n// email from the Config templates here, in JS, where every value the invitee\n// supplied can be HTML-escaped before it lands in markup.\nconst cfg = $('Config').first().json;\nconst build = $('Check validation').first().json;\nconst job = $input.first().json;\n\nconst url = typeof job.result === 'string' ? job.result : (job.result && job.result.url) || null;\nif (!url) {\n  throw new Error('Render completed but returned no output URL: ' + JSON.stringify(job).slice(0, 300));\n}\n\nconst meta = build.meta || {};\nconst esc = (s) =>\n  String(s == null ? '' : s).replace(/[&<>\"']/g, (c) => ({\n    '&': '&amp;', '<': '&lt;', '>': '&gt;', '\"': '&quot;', \"'\": '&#39;',\n  }[c]));\n\nconst fill = (tpl, escapeValues) => {\n  const v = {\n    '{firstName}': meta.firstName,\n    '{eventName}': meta.eventName,\n    '{originalTime}': meta.originalTime,\n    '{presenter}': meta.presenter,\n    '{brand}': meta.brand,\n    '{videoUrl}': url,\n    '{rebookUrl}': meta.rebookUrl,\n  };\n  let out = String(tpl == null ? '' : tpl);\n  for (const key of Object.keys(v)) {\n    out = out.split(key).join(escapeValues ? esc(v[key]) : String(v[key] == null ? '' : v[key]));\n  }\n  return out;\n};\n\nreturn [{\n  json: {\n    videoUrl: url,\n    jobId: job.id,\n    emailSubject: fill(cfg.emailSubject, false),\n    emailHtml: fill(cfg.emailBody, true),\n    toEmail: meta.email || '',\n    firstName: meta.firstName,\n    eventName: meta.eventName,\n    rowNumber: meta.rowNumber,\n    videoSeconds: meta.totalSeconds,\n    creditsCharged: build.creditsRequired,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2640,
        280
      ],
      "id": "zvid-ns-0024",
      "name": "Video URL"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "send-email--cond",
              "leftValue": "={{ $('Config').first().json.sendEmail }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            },
            {
              "id": "has-email--cond",
              "leftValue": "={{ String($json.toEmail || \"\").includes(\"@\") }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        2860,
        280
      ],
      "id": "zvid-ns-0025",
      "name": "Send email?"
    },
    {
      "parameters": {
        "fromEmail": "={{ $('Config').first().json.emailFrom }}",
        "toEmail": "={{ $('Video URL').first().json.toEmail }}",
        "subject": "={{ $('Video URL').first().json.emailSubject }}",
        "emailFormat": "html",
        "html": "={{ $('Video URL').first().json.emailHtml }}",
        "options": {}
      },
      "type": "n8n-nodes-base.emailSend",
      "typeVersion": 2.1,
      "position": [
        3080,
        140
      ],
      "id": "zvid-ns-0026",
      "name": "Send re-book email (SMTP)",
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "sheet-row--cond",
              "leftValue": "={{ $('Video URL').first().json.rowNumber !== null }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        3300,
        280
      ],
      "id": "zvid-ns-0027",
      "name": "Sheet row?"
    },
    {
      "parameters": {
        "resource": "sheet",
        "operation": "update",
        "documentId": {
          "__rl": true,
          "mode": "list",
          "value": ""
        },
        "sheetName": {
          "__rl": true,
          "mode": "list",
          "value": ""
        },
        "columns": {
          "mappingMode": "defineBelow",
          "value": {
            "row_number": "={{ $('Video URL').first().json.rowNumber }}",
            "Status": "={{ $('Config').first().json.statusDoneValue }}",
            "VideoUrl": "={{ $('Video URL').first().json.videoUrl }}"
          },
          "matchingColumns": [
            "row_number"
          ],
          "schema": [
            {
              "id": "row_number",
              "displayName": "row_number",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "number",
              "canBeUsedToMatch": true,
              "readOnly": true,
              "removed": false
            },
            {
              "id": "Status",
              "displayName": "Status",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true,
              "removed": false
            },
            {
              "id": "VideoUrl",
              "displayName": "VideoUrl",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true,
              "removed": false
            }
          ]
        },
        "options": {}
      },
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.7,
      "position": [
        3520,
        140
      ],
      "id": "zvid-ns-0028",
      "name": "Mark row sent",
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "jsCode": "// Final report for a live run. By the time this executes the video exists, the\n// email has been sent (if sendEmail is on) and the sheet row is marked.\nconst cfg = $('Config').first().json;\nconst d = $('Video URL').first().json;\nconst guard = $('Music guard').first().json;\nconst meta = $('Check validation').first().json.meta || {};\n\n// The email node continues on error so a refused SMTP connection never costs\n// you the video — which means \"it ran\" is NOT the same as \"it was delivered\".\n// n8n puts the failure on the item as `error`, so read that (and any rejected\n// recipient) and report honestly.\nlet emailSent = false;\nlet emailError = null;\ntry {\n  if ($('Send re-book email (SMTP)').isExecuted) {\n    const out = $('Send re-book email (SMTP)').first().json || {};\n    const err = out.error;\n    const rejected = Array.isArray(out.rejected) ? out.rejected : [];\n    if (err) {\n      emailError = String((err && err.message) || err);\n    } else if (rejected.length) {\n      emailError = 'SMTP rejected: ' + rejected.join(', ');\n    }\n    emailSent = !emailError;\n  }\n} catch (e) { /* email branch not taken */ }\n\nreturn [{\n  json: {\n    rendered: true,\n    videoUrl: d.videoUrl,\n    jobId: d.jobId,\n    firstName: d.firstName,\n    eventName: d.eventName,\n    presenter: meta.presenter,\n    avatarMode: meta.avatarMode,\n    rebookLabel: meta.rebookLabel,\n    videoSeconds: d.videoSeconds,\n    creditsCharged: d.creditsCharged,\n    music: guard.musicNote,\n    source: meta.source,\n    sheetRow: d.rowNumber,\n    sheetStatus: d.rowNumber === null ? null : cfg.statusDoneValue,\n    emailSent,\n    emailError,\n    emailTo: emailSent ? d.toEmail : null,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3740,
        280
      ],
      "id": "zvid-ns-0029",
      "name": "Run summary"
    },
    {
      "parameters": {
        "respondWith": "firstIncomingItem",
        "options": {}
      },
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        3960,
        460
      ],
      "id": "zvid-ns-0030",
      "name": "Respond ok",
      "onError": "continueRegularOutput",
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "url": "={{ $json.videoUrl }}",
        "options": {
          "response": {
            "response": {
              "responseFormat": "file",
              "outputPropertyName": "data"
            }
          }
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        4180,
        280
      ],
      "id": "zvid-ns-0031",
      "name": "▶ Watch video",
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "width": 900,
        "height": 740,
        "color": 4,
        "content": "## Webinar / demo no-show → personal re-book video\n\nSomeone booked a session and did not show up. This workflow turns that into a\nwarm 16-second vertical video (1080x1920, made for a phone) that says \"we\nmissed you, {firstName}\", recaps what the session covered with the presenter's\nface on screen, and ends on a friendly \"grab another slot\" card — then emails\nit to them and marks the row done.\n\nTwo ways in, one pipeline:\n\n- **Sheet** (default) — every 15 minutes it takes the first row with an empty\n  Status column. Zero extra keys, works on n8n Cloud.\n- **Webhook** — POST a no-show to `/webhook/no-show` from Calendly, HubSpot,\n  Zapier or your own app. Set `source: \"webhook\"` in **Config** to use it.\n\n### Setup (about three minutes)\n\n1. **Zvid API key** — create one at https://app.zvid.io/api-keys\n   Add an n8n **Header Auth** credential: name `x-api-key`, value = your key.\n   Attach it to *Validate project (free)*, *Save draft to editor*,\n   *Submit render* and *Get render status*.\n2. **Google Sheets** — attach your Google credential to *Read no-shows sheet*\n   and *Mark row sent*, then pick your spreadsheet and tab in both. Columns are\n   in the green note.\n3. **Open `Config`** — set `brandName`, `presenterName`, `presenterRole`,\n   `rebookUrl` and the three `highlights` lines. Everything else has a\n   sensible default.\n4. **Run it — the first run renders for real and spends about 16 credits.**\n   `dryRun` is `false` out of the box. When it finishes, click\n   **▶ Watch video** to play the result right here in n8n.\n5. Want a free preview first? Set `dryRun: true` in **Config** before that\n   first run. The workflow validates the payload, quotes the exact credit cost\n   and saves the project as a **draft**, then reports an **editorLink** like\n   `https://editor.zvid.io/?project=prj_...` — open it to watch the exact\n   video and tweak anything by hand, with nothing spent. **The sheet row is not\n   marked sent and no email goes out on a dry run**, so the next real run picks\n   the same person. Set `dryRun` back to `false` to render for real.\n6. **Activate** — with `source: \"sheet\"` the schedule polls the sheet every\n   15 minutes. With `source: \"webhook\"` the schedule deliberately does\n   **nothing**: it ends on *Nothing to send* rather than rendering the sample\n   on a timer, so an activated workflow can never spend credits on a video\n   nobody asked for. Point your no-show automation at the production webhook\n   URL instead.\n\n### Cost per video\n\nA ~16 s re-book video is **about 16 Zvid credits**. *Validate project (free)*\nruns before every render and reports the exact figure as `creditsCharged`;\na `dryRun: true` pass gives you that number without spending anything."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -1840,
        -60
      ],
      "id": "zvid-ns-0040",
      "name": "Note 40"
    },
    {
      "parameters": {
        "width": 620,
        "height": 420,
        "color": 5,
        "content": "## Your sheet: seven columns\n\nHeader row (row 1), spelled exactly like this:\n\n| FirstName | Email | EventName | OriginalTime | RebookUrl | Status | VideoUrl |\n| --- | --- | --- | --- | --- | --- | --- |\n\n- **FirstName** + **EventName** — required. Everything else is optional.\n- **OriginalTime** — an ISO timestamp (`2026-08-13T18:00:00Z`) is formatted\n  using `timeZone` from Config: it reads \"Thu, Aug 13 · 6:00 PM UTC\" with the\n  shipped default, or \"Thu, Aug 13 · 2:00 PM EDT\" with\n  `timeZone: \"America/New_York\"`. Anything else — including a hand-typed\n  \"last Tuesday\" — is printed as you wrote it.\n- **RebookUrl** — per-person booking link. Falls back to `rebookUrl` in\n  **Config** when the cell is empty.\n- **Status** — leave EMPTY for people waiting on a video. The workflow takes\n  the first empty-Status row (top to bottom) and writes `sent` + the video\n  URL when the render finishes. Put anything in Status by hand to skip a row.\n- **Email** — only used when `sendEmail` is `true`, and only to send that\n  person their own video. Nothing is stored anywhere else."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -880,
        700
      ],
      "id": "zvid-ns-0041",
      "name": "Note 41"
    },
    {
      "parameters": {
        "width": 640,
        "height": 500,
        "color": 5,
        "content": "## The webhook contract\n\nSet `source: \"webhook\"` in **Config**, then POST JSON to this workflow's\nwebhook URL:\n\n```\n{\n  \"inviteeFirstName\": \"Sarah\",       ← required\n  \"inviteeEmail\": \"sarah@acme.com\",  ← optional, used by the email step\n  \"eventName\": \"Scaling Content Ops\",\n  \"originalTime\": \"2026-08-13T18:00:00Z\",\n  \"rebookUrl\": \"https://calendly.com/you/30min\"\n}\n```\n\nThe response is sent when the run finishes:\n`{ videoUrl, jobId, creditsCharged, emailSent, ... }`\n\n**Calendly (production):** create a Personal Access Token, call\n`GET https://api.calendly.com/users/me` for your `user` +\n`organization` URIs, then `POST https://api.calendly.com/webhook_subscriptions`\nwith `{ url, events, organization, user, scope: \"user\" }` — or\n`scope: \"organization\"` without `user` for the whole team.\n\nCalendly publishes `invitee.created`, `invitee.canceled` and\n`routing_form_submission.created`; there is **no documented no-show event**,\nso most teams fire this workflow from whatever actually marks the no-show — a\nrep in the CRM, a Zoom attendance report, Zapier/Make — or just use the sheet\npath. Calendly's own spellings (`name`, `email`, `reschedule_url`,\n`scheduled_event.name`, `scheduled_event.start_time`) are read too, so a\nforwarded Calendly invitee record needs no mapping.\n\n**A real render keeps the HTTP request open for one to three minutes** while\nthe video renders. Raise your caller's timeout, or turn on `sendEmail` and\ntreat the webhook as fire-and-forget.\n\nExecuting the workflow by hand always works: with no webhook body, Config's\n`samplePayload` is used instead, so *Execute workflow* always produces a real\nvideo.\n\n**Only a real POST or a hand-started run reaches the render chain in webhook\nmode.** A scheduled run has nothing posted to it, so it ends on *Nothing to\nsend* — the 15-minute schedule can never render `samplePayload` on a loop.\nAnd a POST that arrives while `source` is still `sheet` is answered with\nthat same friendly message instead of a stranger's video."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -160,
        -420
      ],
      "id": "zvid-ns-0042",
      "name": "Note 42"
    },
    {
      "parameters": {
        "width": 660,
        "height": 500,
        "color": 6,
        "content": "## Make it yours\n\nEverything lives in **Config**, and the layout adapts on its own:\n\n- `highlights` — the two or three things the session actually covered. Keep\n  them under ~90 characters each; the cards resize from 34 px down to 24 px as\n  they grow, and the scene gets longer so there is time to read them.\n- `presenterImageUrl` — a **square** photo (a 480x480 headshot is ideal),\n  served over https. Leave it empty and you get a clean monogram circle built\n  from `presenterName` instead — both looks are polished, neither is a\n  placeholder.\n- Copy knobs (`hookLine`, `hookSub`, `ctaHeadline`, `ctaSub`,\n  `signOff`, `emailSubject`, `emailBody`) may embed `{firstName}`,\n  `{eventName}`, `{originalTime}`, `{presenter}` and `{brand}`.\n  Values are HTML-escaped after substitution, so a name with an ampersand can\n  never break the render.\n- Long values are safe: the headline steps down from 96 px as the name grows,\n  a 60-plus-character event title still fits its card, and any single word too wide\n  for its box shrinks the whole line rather than running off the edge.\n- `paperColor` / `inkColor` / `mutedColor` / `brandAccent` recolour every\n  scene, chip, card and button at once.\n\nTone note: the copy never guilt-trips. \"We missed you\", \"no stress\", \"there is\nnothing to catch up on\" — that is what makes people re-book."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        640,
        -420
      ],
      "id": "zvid-ns-0043",
      "name": "Note 43"
    },
    {
      "parameters": {
        "width": 620,
        "height": 360,
        "color": 7,
        "content": "## Optional: email the video\n\n`sendEmail` in **Config** is **false** by default — the workflow renders the\nvideo and writes it to the sheet, and you send it however you already send\nmail. Turn it on and *Send re-book email (SMTP)* mails the person their own\nvideo, using `emailSubject` / `emailBody` from Config.\n\nThe node is n8n's core **Send Email** (SMTP) — attach any SMTP credential.\n**On n8n Cloud, swap it for the Gmail or Outlook node** in one click;\neverything upstream stays identical.\n\nThe step is skipped when the row has no email address, and it is set to\ncontinue on error so a bounced SMTP connection never costs you the video. When\na send does fail, *Run summary* reports `emailSent: false` plus the SMTP\nerror in `emailError` — the video and the sheet write-back still happen.\n\n**Privacy:** the address is used only to send that person their own video. It\nis never written into the video, the project name or the render payload."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        2960,
        -140
      ],
      "id": "zvid-ns-0045",
      "name": "Note 45"
    },
    {
      "parameters": {
        "width": 460,
        "height": 280,
        "color": 3,
        "content": "## ▶ Watch it right here\n\nClick **▶ Watch video** and n8n plays the finished MP4 in the output panel —\nthe node downloads it as binary `video/mp4`, which n8n renders as an inline\nplayer with a download button. No copying URLs out of a JSON blob.\n\nIt hangs off *Run summary*, which still carries `videoUrl`, `jobId`,\n`creditsCharged` and `emailSent` for piping onward — and *Respond ok*\nanswers the webhook caller either way. Only real renders reach this node;\na dry run never does."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        4120,
        460
      ],
      "id": "zvid-ns-0046",
      "name": "Note 46"
    },
    {
      "parameters": {
        "width": 620,
        "height": 280,
        "color": 6,
        "content": "## Official Zvid nodes\n\nThese HTTP Request nodes are deliberately core-only so the workflow runs\nanywhere, including n8n Cloud with nothing installed.\n\nOn self-hosted n8n you can install **`@zvid/n8n-nodes-zvid`**\n(Settings → Community nodes) and swap:\n\n| HTTP Request node | Native replacement |\n| --- | --- |\n| Validate project (free) | **Zvid** → Render → Validate |\n| Submit render | **Zvid** → Render → Create |\n| Get render status + Wait | **Zvid Trigger** (render webhook) |\n\nThe Zvid Trigger removes the polling loop entirely."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        1980,
        700
      ],
      "id": "zvid-ns-0047",
      "name": "Note 47"
    }
  ],
  "pinData": {},
  "connections": {
    "Test manually": {
      "main": [
        [
          {
            "node": "Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Every 15 minutes": {
      "main": [
        [
          {
            "node": "Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "No-show webhook": {
      "main": [
        [
          {
            "node": "Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Config": {
      "main": [
        [
          {
            "node": "Check music",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Check music": {
      "main": [
        [
          {
            "node": "Music guard",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Music guard": {
      "main": [
        [
          {
            "node": "Source?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Source?": {
      "main": [
        [
          {
            "node": "Webhook payload",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Read no-shows sheet",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook payload": {
      "main": [
        [
          {
            "node": "No-show found?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Read no-shows sheet": {
      "main": [
        [
          {
            "node": "Pick next no-show",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Pick next no-show": {
      "main": [
        [
          {
            "node": "No-show found?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "No-show found?": {
      "main": [
        [
          {
            "node": "Build project JSON",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Nothing to send",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Nothing to send": {
      "main": [
        [
          {
            "node": "Respond ok",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build project JSON": {
      "main": [
        [
          {
            "node": "Validate project (free)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate project (free)": {
      "main": [
        [
          {
            "node": "Check validation",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Check validation": {
      "main": [
        [
          {
            "node": "Dry run?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Dry run?": {
      "main": [
        [
          {
            "node": "Save draft to editor",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Submit render",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Save draft to editor": {
      "main": [
        [
          {
            "node": "Dry run summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Dry run summary": {
      "main": [
        [
          {
            "node": "Respond ok",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Submit render": {
      "main": [
        [
          {
            "node": "Wait",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Wait": {
      "main": [
        [
          {
            "node": "Get render status",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get render status": {
      "main": [
        [
          {
            "node": "Render finished?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Render finished?": {
      "main": [
        [
          {
            "node": "Video URL",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Still rendering?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Still rendering?": {
      "main": [
        [
          {
            "node": "Wait",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Video URL": {
      "main": [
        [
          {
            "node": "Send email?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Send email?": {
      "main": [
        [
          {
            "node": "Send re-book email (SMTP)",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Sheet row?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Send re-book email (SMTP)": {
      "main": [
        [
          {
            "node": "Sheet row?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Sheet row?": {
      "main": [
        [
          {
            "node": "Mark row sent",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Run summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Mark row sent": {
      "main": [
        [
          {
            "node": "Run summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run summary": {
      "main": [
        [
          {
            "node": "▶ Watch video",
            "type": "main",
            "index": 0
          },
          {
            "node": "Respond ok",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "versionId": "7c1f5a92-4d38-4b60-9e27-3a05b8d1f6c4",
  "meta": {
    "templateCredsSetupCompleted": false
  },
  "id": "ZvidNoShowReengagement",
  "tags": []
}
