{
  "name": "Turn 5-star reviews into testimonial videos (Zvid)",
  "nodes": [
    {
      "parameters": {},
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        -680,
        180
      ],
      "id": "zvid-rt-0001",
      "name": "Test manually"
    },
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "triggerAtHour": 9
            }
          ]
        }
      },
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        -680,
        380
      ],
      "id": "zvid-rt-0002",
      "name": "Every day at 9am"
    },
    {
      "parameters": {
        "mode": "raw",
        "jsonOutput": "{\n  \"apiUrl\": \"https://api.zvid.io\",\n  \"editorUrl\": \"https://editor.zvid.io\",\n  \"source\": \"sheet\",\n  \"judgemeShop\": \"\",\n  \"judgemeToken\": \"\",\n  \"minRating\": 4,\n  \"useLlm\": false,\n  \"llmModel\": \"openai/gpt-4.1-mini\",\n  \"maxQuoteChars\": 220,\n  \"brandName\": \"Northwind Goods\",\n  \"brandKicker\": \"CUSTOMER LOVE\",\n  \"verifiedLabel\": \"Verified customer\",\n  \"hookLine\": \"Real words from real customers.\",\n  \"ctaText\": \"Read the reviews\",\n  \"storeUrl\": \"https://northwindgoods.example\",\n  \"quoteFont\": \"Playfair Display\",\n  \"uiFont\": \"Outfit\",\n  \"paperColor\": \"#F5EDE6\",\n  \"inkColor\": \"#1C1512\",\n  \"accentColor\": \"#C2553D\",\n  \"starColor\": \"#D9892B\",\n  \"mutedOnInk\": \"#CFBAAE\",\n  \"mutedOnPaper\": \"#70594D\",\n  \"musicUrl\": \"https://cdn.pixabay.com/audio/2025/04/21/audio_ed6f0ed574.mp3\",\n  \"musicVolume\": 0.14,\n  \"maxMusicBytes\": 5242880,\n  \"statusDoneValue\": \"done\",\n  \"dryRun\": false,\n  \"pollSeconds\": 10,\n  \"timeoutMinutes\": 20\n}",
        "options": {}
      },
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [
        -460,
        280
      ],
      "id": "zvid-rt-0003",
      "name": "Config"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "source--cond",
              "leftValue": "={{ String($('Config').first().json.source || 'sheet').trim().toLowerCase() === 'sheet' }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        -240,
        280
      ],
      "id": "zvid-rt-0004",
      "name": "Source?"
    },
    {
      "parameters": {
        "resource": "sheet",
        "operation": "read",
        "documentId": {
          "__rl": true,
          "mode": "list",
          "value": ""
        },
        "sheetName": {
          "__rl": true,
          "mode": "list",
          "value": ""
        },
        "options": {}
      },
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.5,
      "position": [
        -20,
        120
      ],
      "id": "zvid-rt-0005",
      "name": "Read reviews sheet",
      "alwaysOutputData": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "jsCode": "// The Google Sheets node emits one item per row plus a row_number field.\n// Keep the FIRST row that has no Status yet AND clears minRating, so the\n// update node at the end can mark exactly that row done.\nconst cfg = $('Config').first().json;\nconst minRating = Number(cfg.minRating);\nconst floor = Number.isFinite(minRating) ? minRating : 4;\nconst isEmpty = (v) => v === undefined || v === null || String(v).trim() === '';\nconst num = (v) => {\n  const n = Number(String(v == null ? '' : v).replace(',', '.').replace(/[^0-9.]/g, ''));\n  return Number.isFinite(n) ? n : NaN;\n};\n\n// The sheet read runs with alwaysOutputData, so a header-only sheet arrives\n// here as a single blank placeholder item instead of nothing at all. Dropping\n// blank items keeps this node running (and reporting) on an empty sheet.\nconst rows = $input.all()\n  .map((i) => i.json)\n  .filter((r) => r && typeof r === \"object\" && Object.keys(r).some((k) => k !== \"row_number\" && !isEmpty(r[k])));\n\nif (!rows.length) {\n  return [{\n    json: {\n      found: false,\n      source: 'sheet',\n      checked: 0,\n      pending: 0,\n      minRating: floor,\n      reason: 'The sheet has no data rows yet. Add a review under the header row (Reviewer, Rating, ReviewText).',\n    },\n  }];\n}\n\nconst pending = rows.filter((r) => isEmpty(r.Status));\nconst row = pending.find((r) => {\n  const n = num(r.Rating);\n  return Number.isFinite(n) && n >= floor;\n});\n\nif (!row) {\n  return [{\n    json: {\n      found: false,\n      source: 'sheet',\n      checked: rows.length,\n      pending: pending.length,\n      minRating: floor,\n      reason: pending.length\n        ? 'Every pending row is rated below minRating (' + floor + '). Lower minRating in Config or add a higher-rated review.'\n        : 'No row with an empty Status column. Add fresh review rows to the sheet.',\n    },\n  }];\n}\n\nconst text = String(row.ReviewText || '').trim();\nconst reviewer = String(row.Reviewer || '').trim();\nif (!text || !reviewer) {\n  throw new Error('Row ' + row.row_number + ' is missing Reviewer or ReviewText. Fill both columns, or put anything in Status to skip the row.');\n}\n\nreturn [{\n  json: {\n    found: true,\n    source: 'sheet',\n    reviewer,\n    rating: num(row.Rating),\n    text,\n    product: String(row.Product || '').trim(),\n    imageUrl: String(row.ImageUrl || '').trim(),\n    rowNumber: row.row_number,\n    reviewId: null,\n    checked: rows.length,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        200,
        120
      ],
      "id": "zvid-rt-0006",
      "name": "Pick next review"
    },
    {
      "parameters": {
        "url": "https://judge.me/api/v1/reviews",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "api_token",
              "value": "={{ $('Config').first().json.judgemeToken }}"
            },
            {
              "name": "shop_domain",
              "value": "={{ $('Config').first().json.judgemeShop }}"
            },
            {
              "name": "per_page",
              "value": "10"
            }
          ]
        },
        "options": {
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": true
            }
          },
          "timeout": 20000
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        -20,
        440
      ],
      "id": "zvid-rt-0007",
      "name": "Fetch Judge.me reviews",
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "jsCode": "// Judge.me answers { reviews: [ { id, rating, body, reviewer: { name }, ... } ] }.\n// Field names follow the public Judge.me reviews API; anything missing\n// degrades to an empty string instead of throwing, and a bad HTTP status ends\n// the run with a friendly \"nothing to render\" instead of an error.\nconst cfg = $('Config').first().json;\nconst minRating = Number(cfg.minRating);\nconst floor = Number.isFinite(minRating) ? minRating : 4;\nconst staticData = $getWorkflowStaticData('global');\nconst seen = Array.isArray(staticData.seenReviewIds) ? staticData.seenReviewIds : [];\n\n// The fetch node ships with fullResponse on, so an item looks like\n// { statusCode, headers, body }. Accept the raw Judge.me body too, in case\n// someone turns fullResponse off - otherwise a perfectly good response would\n// be reported as \"unreachable\".\nconst res = ($input.first() && $input.first().json) || {};\nconst enveloped = Object.prototype.hasOwnProperty.call(res, 'statusCode') || Object.prototype.hasOwnProperty.call(res, 'body');\nconst body = (enveloped ? res.body : res) || {};\nconst status = enveloped\n  ? Number(res.statusCode)\n  : (Array.isArray(body.reviews) ? 200 : NaN);\n\nconst stop = (reason) => [{ json: { found: false, source: \"judgeme\", checked: 0, reason } }];\n\nif (!Number.isFinite(status) || status === 0) {\n  return stop('Judge.me was unreachable. Check judgemeShop and judgemeToken in Config.');\n}\nif (status === 401 || status === 403) {\n  return stop('Judge.me rejected the token (HTTP ' + status + '). Re-copy judgemeToken from the Judge.me dashboard.');\n}\nif (status !== 200) {\n  return stop('Judge.me answered HTTP ' + status + '.');\n}\n\nconst list = Array.isArray(body.reviews) ? body.reviews : [];\nif (!list.length) return stop('Judge.me returned no reviews for this shop.');\n\nconst reviewText = (r) => String((r && (r.body || r.title)) || '').trim();\n// A review that is hidden, unpublished or flagged as spam must never become a\n// public testimonial. Missing flags mean \"fine\" - Judge.me only sends them\n// when they are set.\nconst isPublic = (r) => r.hidden !== true && r.published !== false && String(r.curated || '').toLowerCase() !== 'spam';\n\nconst unseen = list\n  .filter((r) => Number(r.rating) >= floor)\n  .filter((r) => seen.indexOf(String(r.id)) === -1);\nconst visible = unseen.filter(isPublic);\n// A star-only review has nothing to put on screen. Skipping it HERE is what\n// keeps the queue moving: it is never written to seenReviewIds (that only\n// happens after a successful render), so if it were picked it would be picked\n// again on every future run and the run would fail every time.\nconst candidates = visible.filter((r) => reviewText(r).length > 0);\n\nif (!candidates.length) {\n  const skippedEmpty = visible.length - candidates.length;\n  const skippedHidden = unseen.length - visible.length;\n  const extra = (skippedEmpty ? ' Skipped ' + skippedEmpty + ' star-only review(s) with no written text.' : '')\n    + (skippedHidden ? ' Skipped ' + skippedHidden + ' hidden/unpublished review(s).' : '');\n  return stop('No usable unseen review at ' + floor + ' stars or better. Judge.me returned ' + list.length + ' review(s).' + extra);\n}\n\nconst r = candidates[0];\nconst reviewerName = (r.reviewer && (r.reviewer.name || r.reviewer.display_name))\n  || r.reviewer_name\n  || 'A customer';\n\n// The id is only remembered AFTER a successful render (see Run summary), so a\n// failed render retries the same review on the next run.\nreturn [{\n  json: {\n    found: true,\n    source: 'judgeme',\n    reviewer: String(reviewerName).trim(),\n    rating: Number(r.rating),\n    text: reviewText(r),\n    product: String(r.product_title || r.product_handle || '').trim(),\n    imageUrl: String((Array.isArray(r.pictures) && r.pictures[0] && (r.pictures[0].urls ? r.pictures[0].urls.original : r.pictures[0].url)) || '').trim(),\n    rowNumber: null,\n    reviewId: String(r.id),\n    checked: list.length,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        200,
        440
      ],
      "id": "zvid-rt-0008",
      "name": "Pick Judge.me review"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "review-found--cond",
              "leftValue": "={{ $json.found }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        420,
        280
      ],
      "id": "zvid-rt-0009",
      "name": "Review found?"
    },
    {
      "parameters": {
        "jsCode": "// No review is waiting. This is a normal, successful outcome - not an error.\nconst picked = $input.first().json;\nreturn [{\n  json: {\n    rendered: false,\n    source: picked.source,\n    reason: picked.reason || 'Nothing to render.',\n    reviewsChecked: picked.checked,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        640,
        460
      ],
      "id": "zvid-rt-0010",
      "name": "Nothing to render today"
    },
    {
      "parameters": {
        "jsCode": "// One place where untrusted review text is made safe and predictable before\n// it reaches the trimmer and the design: collapse whitespace, drop any HTML a\n// review platform let through, normalise curly quotes and ellipses to plain\n// ASCII so the wrap estimate and the trimmer count real characters, and clamp the\n// rating to 1..5. (The builder HTML-escapes on top of this.)\nconst cfg = $('Config').first().json;\nconst row = $input.first().json;\n\nconst strip = (s) => String(s == null ? \"\" : s)\n  .replace(/<br\\s*\\/?>/gi, \" \")\n  .replace(/<[^>]*>/g, \" \")\n  .replace(/&nbsp;/gi, \" \")\n  .replace(/&amp;/gi, \"&\")\n  .replace(/[\\u2018\\u2019\\u201A\\u201B]/g, \"'\")\n  .replace(/[\\u201C\\u201D\\u201E\\u201F]/g, '\"')\n  .replace(/\\u2026/g, \"...\")\n  .replace(/\\s+/g, \" \")\n  .trim();\n\nconst text = strip(row.text);\nconst reviewer = strip(row.reviewer);\nif (!text) throw new Error(\"The picked review has no text.\");\nif (!reviewer) throw new Error(\"The picked review has no reviewer name.\");\n\nconst rating = Number(row.rating);\nif (!Number.isFinite(rating) || rating <= 0) {\n  throw new Error(\"The picked review has no usable rating (got \\\"\" + String(row.rating) + \"\\\"). Use a number 1-5.\");\n}\n\nconst imageUrl = String(row.imageUrl || \"\").trim();\n\nreturn [{\n  json: {\n    ...row,\n    reviewer,\n    text,\n    rating: Math.min(5, Math.max(1, rating)),\n    product: strip(row.product),\n    // Only http(s) media reaches the renderer; anything else is treated as\n    // \"no image\" and the hook scene re-balances itself.\n    imageUrl: /^https?:\\/\\//i.test(imageUrl) ? imageUrl : \"\",\n    minRating: Number(cfg.minRating) || 4,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        640,
        200
      ],
      "id": "zvid-rt-0011",
      "name": "Clean review text"
    },
    {
      "parameters": {
        "method": "HEAD",
        "url": "={{ $('Config').first().json.musicUrl }}",
        "options": {
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": true
            }
          },
          "redirect": {
            "redirect": {}
          },
          "timeout": 15000
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        860,
        200
      ],
      "id": "zvid-rt-0012",
      "name": "Check music",
      "onError": "continueRegularOutput",
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "// The music bed is optional. An unreachable URL, an HTTP error or a file over\n// the plan's audio cap renders the video WITHOUT music instead of failing the\n// run - a missing soundtrack is a much smaller problem than a missing video.\nconst cfg = $('Config').first().json;\nconst review = $('Clean review text').first().json;\nconst res = $input.first().json;\n\nconst maxBytes = Number(cfg.maxMusicBytes) || 5242880;\nconst url = String(cfg.musicUrl || '').trim();\n\nlet musicUrl = url;\nlet reason = null;\nlet bytes = null;\n\nif (!url) {\n  musicUrl = '';\n  reason = 'musicUrl is empty in Config';\n} else {\n  const status = Number(res && res.statusCode);\n  const headers = (res && res.headers) || {};\n  const len = Number(headers['content-length'] || headers['Content-Length'] || 0);\n  bytes = Number.isFinite(len) && len > 0 ? len : null;\n  if (!Number.isFinite(status) || status === 0) {\n    musicUrl = '';\n    reason = 'the music URL was unreachable';\n  } else if (status >= 400) {\n    musicUrl = '';\n    reason = 'the music URL answered HTTP ' + status;\n  } else if (bytes && bytes > maxBytes) {\n    musicUrl = '';\n    reason = 'the music file is ' + Math.round(bytes / 1024) + ' KB, over the ' + Math.round(maxBytes / 1024) + ' KB cap';\n  }\n}\n\nreturn [{ json: { ...review, musicUrl, musicBytes: bytes, musicSkippedReason: reason } }];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1080,
        200
      ],
      "id": "zvid-rt-0013",
      "name": "Music guard"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "use-ai--cond",
              "leftValue": "={{ $('Config').first().json.useLlm }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        1300,
        200
      ],
      "id": "zvid-rt-0014",
      "name": "Use AI?"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://openrouter.ai/api/v1/chat/completions",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "openRouterApi",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ model: $('Config').first().json.llmModel, response_format: { type: \"json_object\" }, messages: [ { role: \"system\", content: \"You pick the single most quotable span from a customer review for a testimonial video. You return JSON only, shaped {\\\"quote\\\": \\\"...\\\"}.\\n\\nHARD RULES\\n- Use ONLY words that already appear in the review, in their original order and wording.\\n- Never add, reword, translate, paraphrase or fix the customer's writing.\\n- Never exceed the character budget you are given.\\n- Prefer a span that starts and ends on a sentence boundary.\\n- If the whole review already fits the budget, return it unchanged.\" }, { role: \"user\", content: JSON.stringify({ maxChars: $('Config').first().json.maxQuoteChars, rating: $json.rating, review: $json.text }) } ] }) }}",
        "options": {
          "timeout": 60000
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1520,
        80
      ],
      "id": "zvid-rt-0015",
      "name": "Trim quote with AI",
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "jsCode": "// The LLM may only SELECT a span of the customer's own words - it must never\n// write new ones. Guard: strip everything but letters and digits from both the\n// answer and the original; if the answer is not contained in the original, or\n// is over budget, or the call failed, fall back to the same rule-based trim the\n// default (LLM-free) path uses. A testimonial that puts words in a customer's\n// mouth is worse than no testimonial.\nconst cfg = $('Config').first().json;\nconst row = $('Music guard').first().json;\nconst res = $input.first().json;\n\nconst maxChars = Math.max(60, Math.min(400, Number(cfg.maxQuoteChars) || 220));\nconst normalise = (s) => String(s == null ? '' : s).replace(/\\s+/g, ' ').trim();\n\nfunction ruleTrim(text, max) {\n  const t = normalise(text);\n  if (!t) return { quote: \"\", truncated: false };\n  if (t.length <= max) return { quote: t, truncated: false };\n  const parts = t.match(/[^.!?\\u2026]+[.!?\\u2026]*\\s*/g) || [t];\n  let acc = '';\n  for (let i = 0; i < parts.length; i += 1) {\n    if ((acc + parts[i]).trim().length > max) break;\n    acc += parts[i];\n  }\n  acc = acc.trim();\n  if (acc.length >= Math.floor(max * 0.6)) return { quote: acc, truncated: true };\n  let cut = t.slice(0, max - 1);\n  const sp = cut.lastIndexOf(' ');\n  if (sp > max * 0.5) cut = cut.slice(0, sp);\n  return { quote: cut.replace(/[\\s,;:.!?\\-\\u2013\\u2014]+$/, \"\") + \"\\u2026\", truncated: true };\n}\n\nconst original = normalise(row.text);\nconst fold = (s) => String(s).toLowerCase().replace(/[^a-z0-9]+/g, \"\");\n\nlet quote = null;\nlet usedLlm = false;\nlet fallbackReason = null;\n\ntry {\n  const content = res.choices[0].message.content;\n  const parsed = typeof content === 'string' ? JSON.parse(content) : content;\n  const candidate = normalise(parsed.quote);\n  if (!candidate) fallbackReason = 'the model returned an empty quote';\n  else if (candidate.length > maxChars) fallbackReason = 'the model went over the ' + maxChars + '-character budget';\n  else if (fold(original).indexOf(fold(candidate)) === -1) fallbackReason = 'the model reworded the review instead of quoting it';\n  else { quote = candidate; usedLlm = true; }\n} catch (e) {\n  fallbackReason = 'the model call failed or returned non-JSON';\n}\n\nlet truncated = false;\nif (!quote) {\n  const res2 = ruleTrim(original, maxChars);\n  quote = res2.quote;\n  truncated = res2.truncated;\n} else {\n  truncated = quote.length < original.length;\n}\n\nreturn [{\n  json: {\n    ...row,\n    quote,\n    trimmedBy: usedLlm ? 'llm' : 'rule',\n    llmFallbackReason: fallbackReason,\n    truncated,\n    originalChars: original.length,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1740,
        80
      ],
      "id": "zvid-rt-0016",
      "name": "Use AI quote"
    },
    {
      "parameters": {
        "jsCode": "// Rule-based quote trimming — the DEFAULT path (useLlm is false out of the box,\n// so the template needs no LLM key at all).\n//\n// Long reviews do not fit on a reel. The rule: keep whole sentences while they\n// fit inside maxQuoteChars; if not even the first sentence fits, cut on a word\n// boundary and mark the cut with an ellipsis. Nothing is ever added, reworded\n// or paraphrased — the words on screen are the customer's own.\nconst cfg = $('Config').first().json;\nconst row = $input.first().json;\n\nconst maxChars = Math.max(60, Math.min(400, Number(cfg.maxQuoteChars) || 220));\n\nconst normalise = (s) => String(s == null ? '' : s)\n  .replace(/\\r/g, ' ')\n  .replace(/\\s+/g, ' ')\n  .trim();\n\nfunction trimQuote(text, max) {\n  const t = normalise(text);\n  if (!t) return { quote: '', truncated: false };\n  if (t.length <= max) return { quote: t, truncated: false };\n\n  // Longest run of WHOLE sentences that still fits.\n  const parts = t.match(/[^.!?…]+[.!?…]*\\s*/g) || [t];\n  let acc = '';\n  for (let i = 0; i < parts.length; i += 1) {\n    if ((acc + parts[i]).trim().length > max) break;\n    acc += parts[i];\n  }\n  acc = acc.trim();\n  // A single short sentence would waste the frame — only keep the sentence cut\n  // when it fills most of the budget.\n  if (acc.length >= Math.floor(max * 0.6)) return { quote: acc, truncated: true };\n\n  let cut = t.slice(0, max - 1);\n  const sp = cut.lastIndexOf(' ');\n  if (sp > max * 0.5) cut = cut.slice(0, sp);\n  return { quote: cut.replace(/[\\s,;:.!?\\-–—]+$/, '') + '…', truncated: true };\n}\n\nconst original = normalise(row.text);\nconst res = trimQuote(original, maxChars);\nif (!res.quote) {\n  throw new Error('The review has no text. Fill ReviewText (sheet) or check the Judge.me response.');\n}\n\nreturn [{\n  json: Object.assign({}, row, {\n    quote: res.quote,\n    trimmedBy: 'rule',\n    truncated: res.truncated,\n    originalChars: original.length,\n  }),\n}];\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1520,
        320
      ],
      "id": "zvid-rt-0017",
      "name": "Trim quote"
    },
    {
      "parameters": {
        "jsCode": "// === buildProject — identical to the frame-reviewed standalone builder ===\n// Turns ONE normalised review into a three-scene 1080x1920 testimonial reel.\n//\n//   scene 1  hook   — product photo as a full-bleed hero under a scrim (or a\n//                     designed ink gradient when the review carries no image),\n//                     brand, kicker, honest star row, rating label\n//   scene 2  quote  — paper card: ghost quotation glyph, the quote in serif\n//                     italic, accent rule, monogram + reviewer + verified tag\n//   scene 3  cta    — product name / hook line, CTA pill, store domain, brand\n//\n// The star row is DRAWN from the real rating (rounded to the nearest whole\n// star, clamped 1..5): filled stars for the score, outlined stars for the rest.\n// A 4-star review renders four stars. Nothing here can inflate a rating.\n//\n// RENDERER NOTE: `resize` on a media element is resolved against the PROJECT\n// canvas, not the element's own box — it is only safe on full-bleed layers.\n// Inset imagery would be stretched, so this design never insets a photo.\nfunction buildProject(config, data) {\n  const cfg = config || {};\n  const clean = function (v) { return String(v == null ? '' : v).replace(/\\s+/g, ' ').trim(); };\n  const esc = function (s) {\n    return String(s).replace(/[&<>\"']/g, function (c) {\n      return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '\"': '&quot;', \"'\": '&#39;' }[c];\n    });\n  };\n  const round = function (n) { return Math.round(n * 100) / 100; };\n\n  // ---- inputs -------------------------------------------------------------\n  const reviewer = clean(data.reviewer);\n  const quote = clean(data.quote || data.text);\n  const product = clean(data.product);\n  const imageUrl = clean(data.imageUrl);\n  if (!quote) throw new Error('The review has no text to quote.');\n  if (!reviewer) throw new Error('The review has no reviewer name.');\n\n  const ratingRaw = Number(data.rating);\n  if (!Number.isFinite(ratingRaw) || ratingRaw <= 0) {\n    throw new Error('The review has no usable Rating (got \"' + String(data.rating) + '\"). Use a number 1-5.');\n  }\n  // Honest stars: round to the nearest whole star, never above 5, never below 1.\n  const rating = Math.min(5, Math.max(1, ratingRaw));\n  const starsFilled = Math.min(5, Math.max(1, Math.round(rating)));\n  const ratingLabel = (Math.round(rating * 10) / 10).toFixed(1) + ' out of 5';\n\n  // ---- brand knobs --------------------------------------------------------\n  const brandName = clean(cfg.brandName) || 'Your brand';\n  const kicker = (clean(cfg.brandKicker) || 'CUSTOMER LOVE').toUpperCase();\n  const verifiedLabel = clean(cfg.verifiedLabel) || 'Verified customer';\n  const hookLine = clean(cfg.hookLine) || 'Real words from real customers.';\n  const ctaText = clean(cfg.ctaText) || 'Read the reviews';\n  const quoteFont = clean(cfg.quoteFont) || 'Playfair Display';\n  const uiFont = clean(cfg.uiFont) || 'Outfit';\n  const paper = clean(cfg.paperColor) || '#F5EDE6';\n  const ink = clean(cfg.inkColor) || '#1C1512';\n  const accent = clean(cfg.accentColor) || '#C2553D';\n  const star = clean(cfg.starColor) || '#E0A33C';\n  const mutedOnInk = clean(cfg.mutedOnInk) || '#CFBAAE';\n  const mutedOnPaper = clean(cfg.mutedOnPaper) || '#70594D';\n\n  // storeUrl -> bare host, so the CTA card never shows a raw https:// prefix.\n  const storeRaw = clean(cfg.storeUrl);\n  const storeDomain = storeRaw\n    .replace(/^[a-zA-Z][a-zA-Z0-9+.-]*:\\/\\//, '')\n    .replace(/^www\\./, '')\n    .split('/')[0]\n    .replace(/[?#].*$/, '');\n\n  // ---- colour helpers -----------------------------------------------------\n  const hexToRgb = function (hex) {\n    let h = String(hex).trim().replace('#', '');\n    if (h.length === 3) h = h[0] + h[0] + h[1] + h[1] + h[2] + h[2];\n    const n = parseInt(h, 16);\n    if (!Number.isFinite(n) || h.length !== 6) return { r: 194, g: 85, b: 61 };\n    return { r: (n >> 16) & 255, g: (n >> 8) & 255, b: n & 255 };\n  };\n  const rgba = function (hex, a) {\n    const c = hexToRgb(hex);\n    return 'rgba(' + c.r + ',' + c.g + ',' + c.b + ',' + a + ')';\n  };\n  const hx = function (v) { return ('0' + Math.max(0, Math.min(255, Math.round(v))).toString(16)).slice(-2); };\n  const lighten = function (hex, t) {\n    const c = hexToRgb(hex);\n    const f = function (v) { return v + (255 - v) * t; };\n    return '#' + hx(f(c.r)) + hx(f(c.g)) + hx(f(c.b));\n  };\n  const darken = function (hex, t) {\n    const c = hexToRgb(hex);\n    return '#' + hx(c.r * (1 - t)) + hx(c.g * (1 - t)) + hx(c.b * (1 - t));\n  };\n  // Flatten `over` at opacity `a` onto `base` — the same maths the renderer\n  // does when a translucent bloom is painted over the field.\n  const flatten = function (base, over, a) {\n    const b = hexToRgb(base);\n    const o = hexToRgb(over);\n    return '#' + hx(b.r + (o.r - b.r) * a) + hx(b.g + (o.g - b.g) * a) + hx(b.b + (o.b - b.b) * a);\n  };\n\n  // ---- contrast guard -----------------------------------------------------\n  // The platform's own contrast lint compares `style.color` with the SCENE's\n  // flat backgroundColor — it cannot see the radial fields these scenes paint,\n  // so a tone that \"passes\" the lint can still land at 2.7:1 on the lit part of\n  // a gradient. The builder therefore measures every secondary tone against the\n  // BRIGHTEST pixel its field can reach and lifts it until it clears WCAG AA,\n  // whatever brand colours the importer sets.\n  const AA = 4.5;\n  const relLum = function (hex) {\n    const c = hexToRgb(hex);\n    const ch = function (v) {\n      const s = v / 255;\n      return s <= 0.03928 ? s / 12.92 : Math.pow((s + 0.055) / 1.055, 2.4);\n    };\n    return 0.2126 * ch(c.r) + 0.7152 * ch(c.g) + 0.0722 * ch(c.b);\n  };\n  const contrast = function (a, b) {\n    const la = relLum(a);\n    const lb = relLum(b);\n    return (Math.max(la, lb) + 0.05) / (Math.min(la, lb) + 0.05);\n  };\n  // Walks the colour away from the background (lighter on dark fields, darker\n  // on light ones) in 4% steps until the ratio is met.\n  const ensureContrast = function (fg, bg, min) {\n    if (contrast(fg, bg) >= min) return fg;\n    const up = relLum(fg) >= relLum(bg);\n    for (let t = 0.04; t <= 1.0001; t += 0.04) {\n      const c = up ? lighten(fg, t) : darken(fg, t);\n      if (contrast(c, bg) >= min) return c;\n    }\n    return up ? '#FFFFFF' : '#000000';\n  };\n\n  // ---- the two fields, and the tones that have to survive them ------------\n  // FIELD_LIFT is how far the dark scenes lift at their centre; the blooms are\n  // the soft glows stacked on top. Keeping these low is half the contrast fix —\n  // the other half is lifting the type.\n  const FIELD_LIFT = 0.14;\n  const HALO_PEAK = 0.18;\n  const BLOOM_PEAK = 0.16;\n  const PAPER_BLOOM = 0.16;\n  const fieldTop = lighten(ink, FIELD_LIFT);\n  const darkPeakStar = flatten(fieldTop, star, HALO_PEAK);\n  const darkPeakAccent = flatten(fieldTop, accent, BLOOM_PEAK);\n  const darkPeak = relLum(darkPeakStar) >= relLum(darkPeakAccent) ? darkPeakStar : darkPeakAccent;\n  const paperLow = flatten(paper, accent, PAPER_BLOOM);\n\n  const mutedInk = ensureContrast(mutedOnInk, darkPeak, AA);\n  const mutedPaper = ensureContrast(mutedOnPaper, paperLow, AA);\n  const kickerInk = ensureContrast(lighten(accent, 0.3), darkPeak, AA);\n\n  // Solid accent surfaces (CTA pill, monogram chip): pick the label colour that\n  // already clears AA on the accent, and only push the surface darker when\n  // neither paper nor ink does.\n  let chipBg = accent;\n  let chipText = contrast(paper, accent) >= AA ? paper : ink;\n  if (contrast(chipText, chipBg) < AA) {\n    chipText = paper;\n    chipBg = ensureContrast(accent, paper, AA);\n  }\n  // The chip's gradient sweep always runs AWAY from the label colour, so every\n  // pixel of the chip is at least as readable as its darkest/lightest end.\n  const chipEdge = relLum(chipText) > relLum(chipBg) ? darken(chipBg, 0.16) : lighten(chipBg, 0.16);\n\n  // ---- star row -----------------------------------------------------------\n  // Five 5-point stars drawn as polygons, so the count is DATA and not a glyph\n  // string. Inner/outer radius ratio 0.382 = the classic star proportion.\n  const starPoints = function (cx, cy, ro, ri) {\n    const pts = [];\n    for (let i = 0; i < 10; i += 1) {\n      const r = i % 2 === 0 ? ro : ri;\n      const a = (Math.PI / 5) * i - Math.PI / 2;\n      pts.push(Math.round((cx + r * Math.cos(a)) * 10) / 10 + ',' + Math.round((cy + r * Math.sin(a)) * 10) / 10);\n    }\n    return pts.join(' ');\n  };\n  const starRow = function (filled, size, gap, onColor, offColor) {\n    const w = 5 * size + 4 * gap;\n    let body = '';\n    for (let i = 0; i < 5; i += 1) {\n      const cx = i * (size + gap) + size / 2;\n      const pts = starPoints(cx, size / 2, size * 0.5, size * 0.191);\n      body += i < filled\n        ? \"<polygon points='\" + pts + \"' fill='\" + onColor + \"'/>\"\n        : \"<polygon points='\" + pts + \"' fill='none' stroke='\" + offColor + \"' stroke-width='\" + Math.max(2, Math.round(size * 0.055)) + \"' stroke-linejoin='round'/>\";\n    }\n    return {\n      w: w,\n      h: size,\n      svg: \"<svg width='\" + w + \"' height='\" + size + \"' viewBox='0 0 \" + w + \" \" + size + \"' xmlns='http://www.w3.org/2000/svg'>\" + body + '</svg>',\n    };\n  };\n\n  // ---- greedy wrap estimate ----------------------------------------------\n  // Calibrated against the rendered source design: Playfair Display italic\n  // averages ~0.50em per glyph, the sans UI face ~0.52em.\n  const wrapLines = function (text, fontPx, boxPx, avgEm) {\n    const perLine = Math.max(6, Math.floor(boxPx / (fontPx * avgEm)));\n    let lines = 1;\n    let used = 0;\n    const words = String(text).split(' ');\n    for (let i = 0; i < words.length; i += 1) {\n      const w = words[i];\n      const need = used === 0 ? w.length : used + 1 + w.length;\n      if (need <= perLine) { used = need; continue; }\n      lines += 1;\n      used = Math.min(w.length, perLine);\n    }\n    return lines;\n  };\n\n  // ---- timing -------------------------------------------------------------\n  // A scene `transition` overlaps its two scenes, so every non-last scene\n  // stores duration = visible seconds + transitionDuration.\n  const TD = 0.5;\n  const hookSeconds = 3.8;\n  const qLen = quote.length;\n  const quoteSeconds = round(Math.min(Math.max(3.6 + qLen * 0.026, 5.4), 8.6));\n  const ctaSeconds = 4.2;\n  const totalSeconds = round(hookSeconds + quoteSeconds + ctaSeconds);\n\n  const hasImage = /^https?:\\/\\//i.test(imageUrl);\n\n  // =========================================================================\n  // SCENE 1 — hook\n  // =========================================================================\n  const s1Dur = round(hookSeconds + TD);\n  const bigStars = starRow(starsFilled, hasImage ? 112 : 136, hasImage ? 30 : 36, star, rgba(paper, 0.36));\n\n  // A soft-edged accent bloom — hard-edged circles read cheap on a dark field.\n  const bloom = function (id, hex, peak) {\n    return \"<radialGradient id='\" + id + \"' cx='0.5' cy='0.5' r='0.5'>\"\n      + \"<stop offset='0' stop-color='\" + hex + \"' stop-opacity='\" + peak + \"'/>\"\n      + \"<stop offset='0.6' stop-color='\" + hex + \"' stop-opacity='\" + round(peak * 0.45) + \"'/>\"\n      + \"<stop offset='1' stop-color='\" + hex + \"' stop-opacity='0'/></radialGradient>\";\n  };\n\n  // With a product photo the type block sits in the lower third over a scrim;\n  // without one the same block is centred on a designed gradient.\n  const kickerY = hasImage ? 1120 : 660;\n  const starsY1 = hasImage ? 1206 : 766;\n  const ratingY = hasImage ? 1382 : 966;\n\n  const hookBg = hasImage\n    ? \"<svg width='1080' height='1920' viewBox='0 0 1080 1920' xmlns='http://www.w3.org/2000/svg'>\"\n      // Heavy top + bottom scrim: customer photos are unpredictable, and the\n      // type has to clear 4.5:1 on a white wall as well as on a dark one.\n      + \"<defs><linearGradient id='rtvScrim' x1='0' y1='0' x2='0' y2='1'>\"\n      + \"<stop offset='0' stop-color='\" + ink + \"' stop-opacity='0.72'/>\"\n      + \"<stop offset='0.22' stop-color='\" + ink + \"' stop-opacity='0.3'/>\"\n      + \"<stop offset='0.44' stop-color='\" + ink + \"' stop-opacity='0.36'/>\"\n      + \"<stop offset='0.56' stop-color='\" + ink + \"' stop-opacity='0.88'/>\"\n      + \"<stop offset='1' stop-color='\" + ink + \"' stop-opacity='0.97'/></linearGradient></defs>\"\n      + \"<rect width='1080' height='1920' fill='url(#rtvScrim)'/>\"\n      + \"<rect x='64' y='64' width='952' height='1792' rx='36' fill='none' stroke='\" + rgba(paper, 0.22) + \"' stroke-width='1.5'/>\"\n      + '</svg>'\n    : \"<svg width='1080' height='1920' viewBox='0 0 1080 1920' xmlns='http://www.w3.org/2000/svg'>\"\n      + \"<defs><radialGradient id='rtvHook' cx='0.5' cy='0.34' r='0.94'>\"\n      + \"<stop offset='0' stop-color='\" + fieldTop + \"'/>\"\n      + \"<stop offset='1' stop-color='\" + ink + \"'/></radialGradient>\"\n      + bloom('rtvHookA', accent, BLOOM_PEAK) + bloom('rtvHookB', accent, round(BLOOM_PEAK * 0.66)) + '</defs>'\n      + \"<rect width='1080' height='1920' fill='url(#rtvHook)'/>\"\n      + \"<circle cx='900' cy='230' r='420' fill='url(#rtvHookA)'/>\"\n      + \"<circle cx='140' cy='1680' r='380' fill='url(#rtvHookB)'/>\"\n      + \"<rect x='64' y='64' width='952' height='1792' rx='36' fill='none' stroke='\" + rgba(paper, 0.18) + \"' stroke-width='1.5'/>\"\n      + '</svg>';\n\n  const s1 = {\n    id: 'hook',\n    duration: s1Dur,\n    backgroundColor: ink,\n    transition: 'smoothup',\n    transitionId: 'quote',\n    transitionDuration: TD,\n    visuals: [],\n  };\n\n  if (hasImage) {\n    // Full-bleed only: `resize` is canvas-relative, so this is the one safe way\n    // to place a customer photo of unknown aspect without stretching it.\n    s1.visuals.push({\n      type: 'IMAGE', src: imageUrl, width: 1080, height: 1920,\n      position: 'center-center', resize: 'cover', zoom: true, track: 0,\n      enterBegin: 0, exitEnd: s1Dur,\n    });\n  }\n\n  s1.visuals.push({\n    type: 'SVG', width: 1080, height: 1920, track: 1, enterBegin: 0, exitEnd: s1Dur,\n    svg: hookBg,\n  });\n  s1.visuals.push({\n    type: 'TEXT', x: 540, y: 140, width: 880, anchor: 'top-center', track: 3,\n    enterBegin: 0, enterEnd: 0.45, enterAnimation: 'fade', exitEnd: s1Dur,\n    style: { fontFamily: uiFont, color: hasImage ? paper : mutedInk },\n    html: \"<div style=\\\"text-align:center; color:\" + (hasImage ? paper : mutedInk) + \"; font-size:26px; font-weight:700; letter-spacing:9px;\\\">\" + esc(brandName.toUpperCase()) + '</div>',\n  });\n  s1.visuals.push({\n    type: 'TEXT', x: 540, y: kickerY, width: 880, anchor: 'top-center', track: 3,\n    enterBegin: 0.15, enterEnd: 0.62, enterAnimation: 'smoothdown', exitEnd: s1Dur,\n    style: { fontFamily: uiFont, color: kickerInk },\n    html: \"<div style=\\\"text-align:center; color:\" + kickerInk + \"; font-size:30px; font-weight:800; letter-spacing:11px;\\\">\" + esc(kicker) + '</div>',\n  });\n  s1.visuals.push({\n    type: 'SVG', x: 540, y: starsY1, width: bigStars.w, height: bigStars.h, anchor: 'top-center', track: 3,\n    enterBegin: 0.35, enterEnd: 0.95, enterAnimation: 'circleopen', exitEnd: s1Dur,\n    svg: bigStars.svg,\n  });\n  s1.visuals.push({\n    type: 'TEXT', x: 540, y: ratingY, width: 880, anchor: 'top-center', track: 3,\n    enterBegin: 0.85, enterEnd: 1.22, enterAnimation: 'fade', exitEnd: s1Dur,\n    style: { fontFamily: uiFont, color: paper },\n    html: \"<div style=\\\"text-align:center; color:\" + paper + \"; font-size:38px; font-weight:600; letter-spacing:4px;\\\">\" + esc(ratingLabel) + '</div>',\n  });\n\n  if (hasImage) {\n    if (product) {\n      s1.visuals.push({\n        type: 'TEXT', x: 540, y: ratingY + 90, width: 880, anchor: 'top-center', track: 3,\n        enterBegin: 1.15, enterEnd: 1.55, enterAnimation: 'fade', exitEnd: s1Dur,\n        style: { fontFamily: uiFont, color: mutedInk },\n        html: \"<div style=\\\"text-align:center; color:\" + mutedInk + \"; font-size:29px; font-weight:600; letter-spacing:5px;\\\">\" + esc(product.toUpperCase()) + '</div>',\n      });\n    }\n  } else {\n    s1.visuals.push({\n      // `fade`, not `smoothup`: an upward wipe on a two-line serif block shows\n      // the second line before the first for a beat, which reads as a glitch.\n      type: 'TEXT', x: 540, y: 1098, width: 860, anchor: 'top-center', track: 3,\n      enterBegin: 1.0, enterEnd: 1.55, enterAnimation: 'fade', exitEnd: s1Dur,\n      style: { fontFamily: quoteFont, color: paper },\n      html: \"<div style=\\\"text-align:center; color:\" + paper + \"; font-size:64px; line-height:1.22; font-weight:600;\\\">\" + esc(hookLine) + '</div>',\n    });\n  }\n\n  // =========================================================================\n  // SCENE 2 — the quote (paper card)\n  // =========================================================================\n  const s2Dur = round(quoteSeconds + TD);\n  const quoteSize = qLen <= 60 ? 84 : qLen <= 100 ? 72 : qLen <= 150 ? 62 : qLen <= 190 ? 54 : 48;\n  const quoteBoxW = 840;\n  const quoteLines = wrapLines(quote, quoteSize, quoteBoxW, 0.5);\n  const quoteBlockH = Math.round(quoteLines * quoteSize * 1.28);\n\n  const headStars = starRow(starsFilled, 40, 11, star, rgba(ink, 0.26));\n  const reviewerSize = reviewer.length <= 18 ? 40 : reviewer.length <= 26 ? 35 : 31;\n  const metaText = verifiedLabel + (product ? ' · ' + product : '');\n\n  // Centre quote + rule + reviewer as ONE block so short and long quotes both\n  // sit balanced under the header row. The wrap estimate errs long by design:\n  // over-estimating a line only adds whitespace, under-estimating collides.\n  const blockH = quoteBlockH + 48 + 4 + 56 + 104;\n  const quoteY = Math.max(430, Math.round(1030 - blockH / 2));\n  const ruleY = quoteY + quoteBlockH + 48;\n  const reviewerY = ruleY + 56;\n\n  const quoteBg =\n    \"<svg width='1080' height='1920' viewBox='0 0 1080 1920' xmlns='http://www.w3.org/2000/svg'>\"\n    + \"<defs><linearGradient id='rtvPaper' x1='0' y1='0' x2='0' y2='1'>\"\n    + \"<stop offset='0' stop-color='\" + lighten(paper, 0.5) + \"'/>\"\n    + \"<stop offset='1' stop-color='\" + paper + \"'/></linearGradient>\"\n    + bloom('rtvPaperA', accent, PAPER_BLOOM) + bloom('rtvPaperB', accent, round(PAPER_BLOOM * 0.62)) + '</defs>'\n    + \"<rect width='1080' height='1920' fill='url(#rtvPaper)'/>\"\n    + \"<circle cx='980' cy='1780' r='400' fill='url(#rtvPaperA)'/>\"\n    + \"<circle cx='100' cy='1840' r='240' fill='url(#rtvPaperB)'/>\"\n    + \"<text x='96' y='\" + (quoteY + 110) + \"' font-family='Georgia, serif' font-size='400' fill='\" + rgba(accent, 0.14) + \"'>&#8220;</text>\"\n    + '</svg>';\n\n  const monoSize = 100;\n  const monogram = (reviewer.match(/[\\p{L}\\p{N}]/u) || ['?'])[0].toUpperCase();\n\n  const s2 = {\n    id: 'quote',\n    duration: s2Dur,\n    backgroundColor: paper,\n    transition: 'smoothup',\n    transitionId: 'cta',\n    transitionDuration: TD,\n    visuals: [\n      {\n        type: 'SVG', width: 1080, height: 1920, track: 0, enterBegin: 0, exitEnd: s2Dur,\n        svg: quoteBg,\n      },\n      {\n        // The hook's wordmark is still on screen for the whole TD-long cross\n        // dissolve. Bringing this one in at 0 painted the SAME words a second\n        // time, offset and in another colour — pure ghosting. The paper header\n        // therefore waits until the dissolve has finished (t = TD) and the\n        // wordmark simply re-forms in its new home.\n        type: 'TEXT', x: 120, y: 150, width: 560, anchor: 'top-left', track: 2,\n        enterBegin: TD, enterEnd: TD + 0.4, enterAnimation: 'fade', exitEnd: s2Dur,\n        style: { fontFamily: uiFont, color: mutedPaper },\n        html: \"<div style=\\\"color:\" + mutedPaper + \"; font-size:26px; font-weight:700; letter-spacing:9px;\\\">\" + esc(brandName.toUpperCase()) + '</div>',\n      },\n      {\n        type: 'SVG', x: 960, y: 144, width: headStars.w, height: headStars.h, anchor: 'top-right', track: 2,\n        enterBegin: TD + 0.12, enterEnd: TD + 0.55, enterAnimation: 'smoothleft', exitEnd: s2Dur,\n        svg: headStars.svg,\n      },\n      {\n        // `fade`, never `smoothup`: an upward wipe unmasks the element box from\n        // its bottom edge, so a multi-line quote showed its LAST line — the\n        // punchline — a third of a second before its first. A quote always\n        // wraps, so this block always fades. It also waits for t = TD so no\n        // paper-scene type paints while the hook is still cross-dissolving.\n        type: 'TEXT', x: 120, y: quoteY, width: quoteBoxW, anchor: 'top-left', track: 3,\n        enterBegin: TD, enterEnd: TD + 0.7, enterAnimation: 'fade', exitEnd: s2Dur,\n        style: { fontFamily: quoteFont, color: ink },\n        html: \"<div style=\\\"color:\" + ink + '; font-size:' + quoteSize + \"px; line-height:1.28; font-weight:500; font-style:italic;\\\">&#8220;\" + esc(quote) + '&#8221;</div>',\n      },\n      {\n        type: 'SVG', x: 120, y: ruleY, width: 200, height: 4, anchor: 'top-left', track: 2,\n        enterBegin: 1.0, enterEnd: 1.4, enterAnimation: 'smoothleft', exitEnd: s2Dur,\n        svg: \"<svg width='200' height='4' viewBox='0 0 200 4' xmlns='http://www.w3.org/2000/svg'><rect width='200' height='4' rx='2' fill='\" + accent + \"'/></svg>\",\n      },\n      {\n        type: 'SVG', x: 120, y: reviewerY, width: monoSize, height: monoSize, anchor: 'top-left', track: 2,\n        enterBegin: 1.15, enterEnd: 1.6, enterAnimation: 'circleopen', exitEnd: s2Dur,\n        svg: \"<svg width='\" + monoSize + \"' height='\" + monoSize + \"' viewBox='0 0 \" + monoSize + ' ' + monoSize + \"' xmlns='http://www.w3.org/2000/svg'>\"\n          + \"<defs><linearGradient id='rtvMono' x1='0' y1='0' x2='1' y2='1'>\"\n          + \"<stop offset='0' stop-color='\" + chipBg + \"'/><stop offset='1' stop-color='\" + chipEdge + \"'/></linearGradient></defs>\"\n          + \"<circle cx='\" + monoSize / 2 + \"' cy='\" + monoSize / 2 + \"' r='\" + monoSize / 2 + \"' fill='url(#rtvMono)'/>\"\n          + \"<text x='\" + monoSize / 2 + \"' y='\" + (monoSize / 2 + 16) + \"' font-family='Georgia, serif' font-size='46' font-weight='700' fill='\" + chipText + \"' text-anchor='middle'>\" + esc(monogram) + '</text>'\n          + '</svg>',\n      },\n      {\n        type: 'TEXT', x: 252, y: reviewerY + 8, width: 700, anchor: 'top-left', track: 2,\n        enterBegin: 1.25, enterEnd: 1.7, enterAnimation: 'smoothleft', exitEnd: s2Dur,\n        style: { fontFamily: uiFont, color: ink },\n        html: \"<div style=\\\"color:\" + ink + '; font-size:' + reviewerSize + \"px; font-weight:700;\\\">\" + esc(reviewer) + '</div>'\n          + \"<div style=\\\"margin-top:8px; color:\" + mutedPaper + \"; font-size:25px; font-weight:600;\\\">\" + esc(metaText) + '</div>',\n      },\n    ],\n  };\n\n  // Quiet editorial footer — anchors the bottom of an otherwise airy page.\n  if (storeDomain) {\n    s2.visuals.push({\n      type: 'TEXT', x: 120, y: 1720, width: 840, anchor: 'top-left', track: 2,\n      enterBegin: 1.5, enterEnd: 1.9, enterAnimation: 'fade', exitEnd: s2Dur,\n      style: { fontFamily: uiFont, color: mutedPaper },\n      html: \"<div style=\\\"color:\" + mutedPaper + \"; font-size:24px; font-weight:600; letter-spacing:5px;\\\">\" + esc(storeDomain.toUpperCase()) + '</div>',\n    });\n  }\n\n  // =========================================================================\n  // SCENE 3 — CTA\n  // =========================================================================\n  const headline = product || hookLine;\n  const headSize = headline.length <= 22 ? 82 : headline.length <= 34 ? 70 : headline.length <= 50 ? 60 : 52;\n  const headLines = wrapLines(headline, headSize, 880, 0.52);\n  const headH = Math.round(headLines * headSize * 1.16);\n  const ctaStars = starRow(starsFilled, 52, 16, star, rgba(paper, 0.32));\n\n  const BRAND_H = 34;\n  const PILL_H = 92;\n  const DOMAIN_H = 36;\n  const blockH3 = ctaStars.h + 78 + headH + 44 + BRAND_H + 92 + PILL_H + (storeDomain ? 56 + DOMAIN_H : 0);\n  const starsY3 = Math.max(340, Math.round(960 - blockH3 / 2));\n  const headY = starsY3 + ctaStars.h + 78;\n  const brandY = headY + headH + 44;\n  const pillY = brandY + BRAND_H + 92;\n  const domainY = pillY + PILL_H + 56;\n  const ringCy = starsY3 + Math.round(ctaStars.h / 2);\n\n  // A soft halo behind the stars instead of hairline rings: rings of any radius\n  // that clear the star row also cut straight through the headline below it.\n  const ctaBg =\n    \"<svg width='1080' height='1920' viewBox='0 0 1080 1920' xmlns='http://www.w3.org/2000/svg'>\"\n    + \"<defs><radialGradient id='rtvCta' cx='0.5' cy='0.5' r='0.95'>\"\n    + \"<stop offset='0' stop-color='\" + fieldTop + \"'/>\"\n    + \"<stop offset='1' stop-color='\" + ink + \"'/></radialGradient>\"\n    + bloom('rtvCtaHalo', star, HALO_PEAK) + bloom('rtvCtaB', accent, BLOOM_PEAK) + '</defs>'\n    + \"<rect width='1080' height='1920' fill='url(#rtvCta)'/>\"\n    + \"<circle cx='540' cy='\" + ringCy + \"' r='300' fill='url(#rtvCtaHalo)'/>\"\n    + \"<circle cx='900' cy='1780' r='420' fill='url(#rtvCtaB)'/>\"\n    + \"<rect x='64' y='64' width='952' height='1792' rx='36' fill='none' stroke='\" + rgba(paper, 0.16) + \"' stroke-width='1.5'/>\"\n    + '</svg>';\n\n  const s3 = {\n    id: 'cta',\n    duration: ctaSeconds,\n    backgroundColor: ink,\n    visuals: [\n      {\n        type: 'SVG', width: 1080, height: 1920, track: 0, enterBegin: 0, exitEnd: ctaSeconds,\n        svg: ctaBg,\n      },\n      {\n        type: 'SVG', x: 540, y: starsY3, width: ctaStars.w, height: ctaStars.h, anchor: 'top-center', track: 2,\n        enterBegin: 0.15, enterEnd: 0.6, enterAnimation: 'fade', exitEnd: ctaSeconds,\n        svg: ctaStars.svg,\n      },\n      {\n        // Same wipe trap as the quote: a two-line product name unmasked from the\n        // bottom spelled out its tail (\"Long Sleeve\") on its own for ~0.3 s.\n        // One-line headlines keep the upward move; wrapped ones fade.\n        type: 'TEXT', x: 540, y: headY, width: 880, anchor: 'top-center', track: 2,\n        enterBegin: 0.35, enterEnd: 0.92, enterAnimation: headLines > 1 ? 'fade' : 'smoothup', exitEnd: ctaSeconds,\n        style: { fontFamily: quoteFont, color: paper },\n        html: \"<div style=\\\"text-align:center; color:\" + paper + '; font-size:' + headSize + \"px; line-height:1.16; font-weight:600;\\\">\" + esc(headline) + '</div>',\n      },\n      {\n        type: 'TEXT', x: 540, y: brandY, width: 880, anchor: 'top-center', track: 2,\n        enterBegin: 0.7, enterEnd: 1.05, enterAnimation: 'fade', exitEnd: ctaSeconds,\n        style: { fontFamily: uiFont, color: mutedInk },\n        html: \"<div style=\\\"text-align:center; color:\" + mutedInk + \"; font-size:28px; font-weight:600; letter-spacing:7px;\\\">\" + esc(brandName.toUpperCase()) + '</div>',\n      },\n      {\n        // No `width` on purpose: the renderer sizes a width-less text box to\n        // fit-content, and that is what centres an inline-block pill on x=540.\n        type: 'TEXT', x: 540, y: pillY, anchor: 'top-center', track: 2,\n        enterBegin: 0.95, enterEnd: 1.45, enterAnimation: 'circleopen', exitEnd: ctaSeconds,\n        style: { fontFamily: uiFont, color: chipText },\n        html: \"<div style=\\\"display:inline-block; background:\" + chipBg + '; color:' + chipText + \"; border-radius:999px; padding:26px 62px; font-size:38px; font-weight:700;\\\">\" + esc(ctaText) + '</div>',\n      },\n    ],\n  };\n\n  if (storeDomain) {\n    s3.visuals.push({\n      // The domain is the conversion element on the terminal card — it gets the\n      // full-strength paper tone, not the secondary one.\n      type: 'TEXT', x: 540, y: domainY, width: 880, anchor: 'top-center', track: 2,\n      enterBegin: 1.3, enterEnd: 1.68, enterAnimation: 'fade', exitEnd: ctaSeconds,\n      style: { fontFamily: uiFont, color: paper },\n      html: \"<div style=\\\"text-align:center; color:\" + paper + \"; font-size:29px; font-weight:600; letter-spacing:3px;\\\">\" + esc(storeDomain) + '</div>',\n    });\n  }\n\n  // ---- payload ------------------------------------------------------------\n  // The render API only allows letters, digits, spaces, _ and - in `name`.\n  const safeName = ('review-' + (product || brandName) + '-' + reviewer)\n    .toLowerCase().normalize('NFKD').replace(/[\\u0300-\\u036f]/g, '')\n    .replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '').slice(0, 60).replace(/-+$/, '');\n\n  const payload = {\n    name: safeName || 'customer-review',\n    resolution: 'instagram-reel',\n    frameRate: 30,\n    backgroundColor: ink,\n    outputFormat: 'mp4',\n    scenes: [s1, s2, s3],\n  };\n\n  // NOTE: no `track` here — the render API rejects audios[].track.\n  const musicUrl = clean(data.musicUrl);\n  if (musicUrl) {\n    payload.audios = [{ src: musicUrl, volume: Number(cfg.musicVolume == null ? 0.14 : cfg.musicVolume) }];\n  }\n\n  return {\n    payload: payload,\n    meta: {\n      reviewer: reviewer,\n      rating: rating,\n      starsFilled: starsFilled,\n      ratingLabel: ratingLabel,\n      product: product || null,\n      hasImage: hasImage,\n      quoteChars: qLen,\n      quoteSize: quoteSize,\n      quoteLines: quoteLines,\n      quote: quote,\n      hookSeconds: hookSeconds,\n      quoteSeconds: quoteSeconds,\n      ctaSeconds: ctaSeconds,\n      totalSeconds: totalSeconds,\n      musicUrl: musicUrl || null,\n      storeDomain: storeDomain || null,\n      // Resolved tones + the ratios they hold on the worst pixel of each field,\n      // so a run can be contrast-audited from its own output.\n      contrast: {\n        darkPeak: darkPeak,\n        paperLow: paperLow,\n        mutedOnInk: mutedInk + ' ' + (Math.round(contrast(mutedInk, darkPeak) * 100) / 100) + ':1',\n        kicker: kickerInk + ' ' + (Math.round(contrast(kickerInk, darkPeak) * 100) / 100) + ':1',\n        mutedOnPaper: mutedPaper + ' ' + (Math.round(contrast(mutedPaper, paperLow) * 100) / 100) + ':1',\n        chip: chipText + ' on ' + chipBg + ' ' + (Math.round(contrast(chipText, chipBg) * 100) / 100) + ':1',\n      },\n    },\n  };\n}\n\n// === n8n glue ===\nconst config = $('Config').first().json;\nconst row = $input.first().json;\nconst built = buildProject(config, {\n  reviewer: row.reviewer,\n  rating: row.rating,\n  text: row.text,\n  quote: row.quote,\n  product: row.product,\n  imageUrl: row.imageUrl,\n  musicUrl: row.musicUrl,\n});\nbuilt.meta.rowNumber = row.rowNumber == null ? null : row.rowNumber;\nbuilt.meta.source = row.source || 'sheet';\nbuilt.meta.trimmedBy = row.trimmedBy || 'rule';\nbuilt.meta.originalChars = row.originalChars == null ? built.meta.quoteChars : row.originalChars;\nbuilt.meta.musicSkippedReason = row.musicSkippedReason || null;\nbuilt.meta.reviewId = row.reviewId == null ? null : row.reviewId;\nreturn [{ json: built }];\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1960,
        200
      ],
      "id": "zvid-rt-0018",
      "name": "Build project JSON"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $('Config').first().json.apiUrl }}/api/render/validate/api-key",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ payload: $json.payload }) }}",
        "options": {
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": true
            }
          }
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2180,
        200
      ],
      "id": "zvid-rt-0019",
      "name": "Validate project (free)"
    },
    {
      "parameters": {
        "jsCode": "// The free validate endpoint runs the exact pipeline a render submission runs,\n// so a failure here is a payload bug - surface the field list instead of a\n// generic HTTP error.\nconst res = $input.first().json;\nconst status = res.statusCode;\nconst body = res.body || {};\n\nif (status !== 200) {\n  const details = Array.isArray(body.details)\n    ? body.details.map((d) => '  - ' + d.field + ': ' + d.message).join('\\n')\n    : JSON.stringify(body).slice(0, 500);\n  throw new Error('Zvid rejected the project (HTTP ' + status + '): ' + (body.message || body.error) + '\\n' + details);\n}\n\nconst build = $('Build project JSON').first().json;\nreturn [{\n  json: {\n    ...build,\n    creditsRequired: body.creditsRequired,\n    warnings: body.warnings || [],\n    schemaVersion: body.schemaVersion,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2400,
        200
      ],
      "id": "zvid-rt-0020",
      "name": "Check validation"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "dry-run--cond",
              "leftValue": "={{ $('Config').first().json.dryRun }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        2620,
        200
      ],
      "id": "zvid-rt-0021",
      "name": "Dry run?"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $('Config').first().json.apiUrl }}/api/projects",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ name: $json.payload.name, payload: $json.payload }) }}",
        "options": {}
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2840,
        40
      ],
      "id": "zvid-rt-0022",
      "name": "Save draft to editor",
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "jsCode": "// Dry run: everything except the paid render. Open editorLink to preview the\n// exact video, then set dryRun to false in Config to go live. The sheet row is\n// NOT marked done on a dry run, so the next real run picks the same review.\nconst cfg = $('Config').first().json;\nconst d = $('Check validation').first().json;\n\n// The draft save is best-effort (onError: continue) so a hiccup there never\n// hides the rest of the dry-run report.\nlet editorLink = null;\nlet draftId = null;\ntry {\n  const project = $('Save draft to editor').first().json.project;\n  if (project && project.id) {\n    draftId = project.id;\n    editorLink = String(cfg.editorUrl || 'https://editor.zvid.io').replace(/\\/+$/, '') + '/?project=' + project.id;\n  }\n} catch (e) { /* draft step skipped or failed */ }\n\nreturn [{\n  json: {\n    dryRun: true,\n    reviewer: d.meta.reviewer,\n    rating: d.meta.rating,\n    stars: d.meta.starsFilled,\n    quote: d.meta.quote,\n    quoteChars: d.meta.quoteChars,\n    trimmedBy: d.meta.trimmedBy,\n    product: d.meta.product,\n    sheetRow: d.meta.rowNumber,\n    editorLink,\n    draftId,\n    creditsRequired: d.creditsRequired,\n    videoSeconds: d.meta.totalSeconds,\n    warnings: d.warnings,\n    sheetUpdated: false,\n    nextStep: editorLink\n      ? 'Open editorLink to preview the draft, then set dryRun to false in Config to render and mark the row done.'\n      : 'Set dryRun to false in Config to render this for real.',\n    payload: d.payload,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3060,
        40
      ],
      "id": "zvid-rt-0023",
      "name": "Dry run summary"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $('Config').first().json.apiUrl }}/api/render/api-key",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ payload: $json.payload }) }}",
        "options": {}
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2840,
        360
      ],
      "id": "zvid-rt-0024",
      "name": "Submit render",
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "amount": "={{ $('Config').first().json.pollSeconds }}",
        "unit": "seconds"
      },
      "type": "n8n-nodes-base.wait",
      "typeVersion": 1.1,
      "position": [
        3060,
        360
      ],
      "id": "zvid-rt-0025",
      "name": "Wait",
      "webhookId": "b6f2d418-7c39-4a51-9e07-3d84c1f62a95"
    },
    {
      "parameters": {
        "url": "={{ $('Config').first().json.apiUrl }}/api/jobs/{{ $('Submit render').first().json.jobId }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "options": {}
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        3280,
        360
      ],
      "id": "zvid-rt-0026",
      "name": "Get render status",
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "render-finished--cond",
              "leftValue": "={{ $json.state === 'completed' }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        3500,
        360
      ],
      "id": "zvid-rt-0027",
      "name": "Render finished?"
    },
    {
      "parameters": {
        "jsCode": "// Fails fast on a failed render and stops the poll loop from running forever.\nconst cfg = $('Config').first().json;\nconst job = $input.first().json;\n\nif (job.state === 'failed') {\n  throw new Error('Render failed: ' + (job.failedReason || 'no reason given'));\n}\n\nconst maxPolls = Math.ceil((Number(cfg.timeoutMinutes || 20) * 60) / Number(cfg.pollSeconds || 10));\nif ($runIndex + 1 >= maxPolls) {\n  throw new Error('Render did not finish within ' + cfg.timeoutMinutes + ' minutes (job ' + job.id + ', state ' + job.state + ').');\n}\n\nreturn $input.all();"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3500,
        580
      ],
      "id": "zvid-rt-0028",
      "name": "Still rendering?"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "sheet-mode--cond",
              "leftValue": "={{ String($('Config').first().json.source || 'sheet').trim().toLowerCase() === 'sheet' }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        3720,
        280
      ],
      "id": "zvid-rt-0029",
      "name": "Sheet mode?"
    },
    {
      "parameters": {
        "resource": "sheet",
        "operation": "update",
        "documentId": {
          "__rl": true,
          "mode": "list",
          "value": ""
        },
        "sheetName": {
          "__rl": true,
          "mode": "list",
          "value": ""
        },
        "columns": {
          "mappingMode": "defineBelow",
          "value": {
            "row_number": "={{ $('Build project JSON').first().json.meta.rowNumber }}",
            "Status": "={{ $('Config').first().json.statusDoneValue }}",
            "VideoUrl": "={{ typeof $json.result === 'string' ? $json.result : ($json.result ? $json.result.url : '') }}"
          },
          "matchingColumns": [
            "row_number"
          ],
          "schema": [
            {
              "id": "row_number",
              "displayName": "row_number",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "number",
              "canBeUsedToMatch": true,
              "readOnly": true,
              "removed": false
            },
            {
              "id": "Status",
              "displayName": "Status",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true,
              "removed": false
            },
            {
              "id": "VideoUrl",
              "displayName": "VideoUrl",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true,
              "removed": false
            }
          ]
        },
        "options": {}
      },
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.7,
      "position": [
        3940,
        180
      ],
      "id": "zvid-rt-0030",
      "name": "Mark row done",
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "jsCode": "// Final report for a live run. In sheet mode the row is already marked done by\n// this point; in Judge.me mode the review id is remembered HERE - only after a\n// successful render - so a failed render retries the same review next time.\nconst cfg = $('Config').first().json;\nconst build = $('Check validation').first().json;\nconst job = $('Get render status').first().json;\nconst url = typeof job.result === 'string' ? job.result : (job.result ? job.result.url : null);\n\nconst source = String(cfg.source || 'sheet').trim().toLowerCase();\nif (source === 'judgeme' && build.meta.reviewId) {\n  const staticData = $getWorkflowStaticData('global');\n  const seen = Array.isArray(staticData.seenReviewIds) ? staticData.seenReviewIds : [];\n  staticData.seenReviewIds = [String(build.meta.reviewId)].concat(seen.filter((id) => id !== String(build.meta.reviewId))).slice(0, 200);\n}\n\nreturn [{\n  json: {\n    rendered: true,\n    videoUrl: url,\n    jobId: job.id,\n    source,\n    reviewer: build.meta.reviewer,\n    rating: build.meta.rating,\n    stars: build.meta.starsFilled,\n    product: build.meta.product,\n    quote: build.meta.quote,\n    quoteChars: build.meta.quoteChars,\n    originalChars: build.meta.originalChars,\n    trimmedBy: build.meta.trimmedBy,\n    hadProductImage: build.meta.hasImage,\n    music: build.meta.musicUrl ? \"on\" : (\"off - \" + (build.meta.musicSkippedReason || \"no musicUrl\")),\n    videoSeconds: build.meta.totalSeconds,\n    creditsCharged: build.creditsRequired,\n    sheetRow: build.meta.rowNumber,\n    sheetStatus: source === 'sheet' ? cfg.statusDoneValue : null,\n    reviewId: build.meta.reviewId,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        4160,
        280
      ],
      "id": "zvid-rt-0031",
      "name": "Run summary"
    },
    {
      "parameters": {
        "url": "={{ $json.videoUrl }}",
        "options": {
          "response": {
            "response": {
              "responseFormat": "file",
              "outputPropertyName": "data"
            }
          }
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        4380,
        280
      ],
      "id": "zvid-rt-0032",
      "name": "▶ Watch video",
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "width": 880,
        "height": 660,
        "color": 4,
        "content": "## Turn 5-star reviews into testimonial videos\n\nTakes ONE customer review - from a Google Sheet out of the box, or from\nJudge.me in production - and renders it as a 1080x1920 testimonial reel\n(~15 s): a star row drawn from the real rating, the quote set in serif on a\npaper card with the reviewer and a verified tag, then a CTA card. The video\nURL is written back to the sheet row and the row is marked done.\n\n**No store and no AI key needed.** `source` is `sheet` and `useLlm` is\n`false` out of the box, so a Zvid key plus a Google account is the whole\nshopping list. Long reviews are trimmed by a plain-JavaScript rule.\n\n### Setup (about three minutes)\n\n1. **Zvid API key** - create one at https://app.zvid.io/api-keys\n   Add an n8n **Header Auth** credential: name `x-api-key`, value = your key.\n   Attach it to *Validate project (free)*, *Save draft to editor*,\n   *Submit render* and *Get render status*.\n2. **Google Sheets** - attach your Google credential to *Read reviews sheet*\n   and *Mark row done*, then pick your spreadsheet and tab in both nodes.\n   The columns are in the yellow note next to the sheet node.\n3. **Open `Config`** - set `brandName`, `ctaText` and `storeUrl`.\n4. **Run it - the first run renders for real and spends about 16 credits.**\n   When it finishes, click **▶ Watch video** to play the reel here in n8n.\n5. Free preview first? Set `dryRun: true` in **Config**: you get the credit\n   quote plus an **editorLink** to watch the draft, nothing spent and the\n   sheet untouched. Set it back to `false` to render.\n6. **Activate** - it turns one review into a video every day at 9am."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -700,
        -420
      ],
      "id": "zvid-rt-0040",
      "name": "Note 40"
    },
    {
      "parameters": {
        "width": 660,
        "height": 380,
        "color": 5,
        "content": "## Your sheet: seven columns\n\nHeader row (row 1), spelled exactly like this:\n\n| Reviewer | Rating | ReviewText | Product | ImageUrl | Status | VideoUrl |\n| --- | --- | --- | --- | --- | --- | --- |\n\n- **Reviewer**, **Rating** (1-5) and **ReviewText** are required.\n- **Product** is optional - it becomes the line under the stars, the tag next\n  to the reviewer and the headline on the CTA card.\n- **ImageUrl** is optional. With one, the hook scene becomes a full-bleed\n  photo under a scrim; without one it re-balances to a centred type layout.\n  Must be a public http(s) URL (Shopify / Judge.me CDN URLs are).\n- **Status** - leave EMPTY for reviews waiting to be rendered. The workflow\n  takes the first empty-Status row that also clears `minRating`, and writes\n  `done` + the video URL when the render finishes. Put anything in Status by\n  hand to skip a row."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -60,
        -300
      ],
      "id": "zvid-rt-0041",
      "name": "Note 41"
    },
    {
      "parameters": {
        "width": 660,
        "height": 440,
        "color": 6,
        "content": "## Production path: Judge.me\n\nThe sheet is the demo path so the template runs the minute it is imported.\nTo pull real reviews instead:\n\n1. Judge.me dashboard -> **Settings -> Integrations -> Judge.me API**, copy\n   your **private API token** (the free plan includes API access).\n2. In **Config** set `source: \"judgeme\"`, `judgemeToken` and `judgemeShop`\n   (`your-store.myshopify.com`).\n3. That is it - no credential to attach: Judge.me authenticates with the\n   token as a query parameter, which is why it lives in Config.\n\nStar-only reviews (a rating with no words) and anything hidden, unpublished\nor flagged as spam are skipped automatically - the picker moves on to the\nnext review instead of stalling on one it cannot put on screen.\n\nReviews already turned into a video are remembered in workflow static data\n(`seenReviewIds`, last 200) and only AFTER a successful render, so a failed\nrender retries the same review. n8n only persists static data for production\nruns, so manual test runs can re-pick a review. Nothing is written back to\nJudge.me."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -60,
        620
      ],
      "id": "zvid-rt-0042",
      "name": "Note 42"
    },
    {
      "parameters": {
        "width": 640,
        "height": 340,
        "color": 6,
        "content": "## Long reviews, honest quotes\n\nA 300-character review does not fit on a reel. *Trim quote* keeps whole\nsentences while they fit inside `maxQuoteChars` (220 by default); if not even\nthe first sentence fits it cuts on a word boundary and marks the cut with an\nellipsis. **Nothing is ever reworded** - the words on screen are the\ncustomer's own.\n\nSet `useLlm: true` to let a model pick the most quotable span instead\n(*Trim quote with AI*, needs an OpenRouter credential). The answer is still\nchecked: strip everything but letters and digits, and if the result is not\ncontained in the original review - or is over budget, or the call failed -\nthe rule-based trim is used instead and `trimmedBy` reports `rule`.\n\n**Stars are drawn from the real rating** (rounded to the nearest whole star).\nA 4-star review renders four filled stars and one outlined one. There is no\ncode path that can show five."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        1280,
        -300
      ],
      "id": "zvid-rt-0043",
      "name": "Note 43"
    },
    {
      "parameters": {
        "width": 620,
        "height": 300,
        "color": 4,
        "content": "## Free preview: the optional dry run\n\n**`dryRun` is `false` in Config, so a run renders for real.** Set it to\n`true` whenever you want a free pass instead: the workflow validates the\npayload, quotes the exact credit cost and saves the project as a **draft**,\nthen reports an **editorLink** like `https://editor.zvid.io/?project=prj_...`\n\nOpen it to watch the exact video and tweak anything by hand, with nothing\nspent. **The sheet row is not marked done on a dry run**, so the next real\nrun renders the same review.\n\nSet `dryRun` back to `false` to render and mark the row done."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        2600,
        -280
      ],
      "id": "zvid-rt-0044",
      "name": "Note 44"
    },
    {
      "parameters": {
        "width": 660,
        "height": 320,
        "color": 7,
        "content": "## Publishing the video (optional tail)\n\nThe workflow ends with the video URL in the sheet - a clean handoff you can\nextend after **Mark row done**:\n\n- **Instagram / TikTok / everywhere** - pass `videoUrl` to a scheduler like\n  Blotato, Buffer or Metricool via their HTTP API; they accept a public video\n  URL directly, no download step needed.\n- **YouTube Shorts** - add an *HTTP Request* node (GET the `videoUrl`,\n  response format `File`) followed by the native **YouTube** node\n  (Video -> Upload, binary `data`). Needs a YouTube OAuth2 credential.\n- **Product page** - drop `videoUrl` into your CMS/theme via its own API.\n\nThey are left out of the required path on purpose so the template imports and\nruns with a Zvid key and a Google account, nothing else."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        3900,
        -160
      ],
      "id": "zvid-rt-0045",
      "name": "Note 45"
    },
    {
      "parameters": {
        "width": 420,
        "height": 240,
        "color": 3,
        "content": "## ▶ Watch video\n\nClick this node after a run: n8n renders the downloaded MP4 as an inline\nvideo player with a download button - the finished testimonial, no URL to\ncopy.\n\n*Run summary* before it still carries `videoUrl` (and `jobId`,\n`creditsCharged`, `trimmedBy`) if you want to pipe the result onward."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        4340,
        460
      ],
      "id": "zvid-rt-0046",
      "name": "Note 46"
    }
  ],
  "pinData": {},
  "connections": {
    "Test manually": {
      "main": [
        [
          {
            "node": "Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Every day at 9am": {
      "main": [
        [
          {
            "node": "Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Config": {
      "main": [
        [
          {
            "node": "Source?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Source?": {
      "main": [
        [
          {
            "node": "Read reviews sheet",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Fetch Judge.me reviews",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Read reviews sheet": {
      "main": [
        [
          {
            "node": "Pick next review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Pick next review": {
      "main": [
        [
          {
            "node": "Review found?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch Judge.me reviews": {
      "main": [
        [
          {
            "node": "Pick Judge.me review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Pick Judge.me review": {
      "main": [
        [
          {
            "node": "Review found?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Review found?": {
      "main": [
        [
          {
            "node": "Clean review text",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Nothing to render today",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Clean review text": {
      "main": [
        [
          {
            "node": "Check music",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Check music": {
      "main": [
        [
          {
            "node": "Music guard",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Music guard": {
      "main": [
        [
          {
            "node": "Use AI?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Use AI?": {
      "main": [
        [
          {
            "node": "Trim quote with AI",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Trim quote",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Trim quote with AI": {
      "main": [
        [
          {
            "node": "Use AI quote",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Use AI quote": {
      "main": [
        [
          {
            "node": "Build project JSON",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Trim quote": {
      "main": [
        [
          {
            "node": "Build project JSON",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build project JSON": {
      "main": [
        [
          {
            "node": "Validate project (free)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate project (free)": {
      "main": [
        [
          {
            "node": "Check validation",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Check validation": {
      "main": [
        [
          {
            "node": "Dry run?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Dry run?": {
      "main": [
        [
          {
            "node": "Save draft to editor",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Submit render",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Save draft to editor": {
      "main": [
        [
          {
            "node": "Dry run summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Submit render": {
      "main": [
        [
          {
            "node": "Wait",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Wait": {
      "main": [
        [
          {
            "node": "Get render status",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get render status": {
      "main": [
        [
          {
            "node": "Render finished?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Render finished?": {
      "main": [
        [
          {
            "node": "Sheet mode?",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Still rendering?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Still rendering?": {
      "main": [
        [
          {
            "node": "Wait",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Sheet mode?": {
      "main": [
        [
          {
            "node": "Mark row done",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Run summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Mark row done": {
      "main": [
        [
          {
            "node": "Run summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run summary": {
      "main": [
        [
          {
            "node": "▶ Watch video",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "versionId": "a7d21f36-58be-4c04-9f7a-2e6b83d51c9f",
  "meta": {
    "templateCredsSetupCompleted": false
  },
  "id": "ZvidReviewTestimonialVideo",
  "tags": []
}
