{
  "name": "Monthly customer-testimonial compilation video (Zvid)",
  "nodes": [
    {
      "parameters": {},
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        -1120,
        120
      ],
      "id": "zvid-tc-0001",
      "name": "Test manually"
    },
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "months",
              "triggerAtDayOfMonth": 1,
              "triggerAtHour": 9
            }
          ]
        }
      },
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        -1120,
        320
      ],
      "id": "zvid-tc-0002",
      "name": "On the 1st of each month"
    },
    {
      "parameters": {
        "mode": "raw",
        "jsonOutput": "{\n  \"apiUrl\": \"https://api.zvid.io\",\n  \"editorUrl\": \"https://editor.zvid.io\",\n  \"source\": \"sheet\",\n  \"airtableBaseId\": \"\",\n  \"airtableTable\": \"Testimonials\",\n  \"airtableView\": \"\",\n  \"maxQuotes\": 5,\n  \"minQuotes\": 2,\n  \"maxQuoteChars\": 260,\n  \"brandName\": \"Northwind Logistics\",\n  \"kickerText\": \"Customer stories\",\n  \"introTitle\": \"What our customers say\",\n  \"verifiedLabel\": \"Verified customer\",\n  \"ctaText\": \"Join them\",\n  \"ctaUrl\": \"northwind.example/stories\",\n  \"logoUrl\": \"\",\n  \"monthLabelMode\": \"current\",\n  \"monthLabelOverride\": \"\",\n  \"quoteFont\": \"Playfair Display\",\n  \"uiFont\": \"Outfit\",\n  \"paperColor\": \"#F4F1EA\",\n  \"inkColor\": \"#12161B\",\n  \"accentColor\": \"#2F6155\",\n  \"accentOnDark\": \"#84C6B2\",\n  \"starColor\": \"#9A6B14\",\n  \"starOnDark\": \"#E8B94B\",\n  \"mutedOnPaper\": \"#5C5548\",\n  \"mutedOnInk\": \"#96A1AC\",\n  \"musicUrl\": \"https://cdn.pixabay.com/audio/2025/03/19/audio_56ae1dae5f.mp3\",\n  \"musicVolume\": 0.12,\n  \"maxMusicBytes\": 5242880,\n  \"statusDoneValue\": \"compiled\",\n  \"dryRun\": false,\n  \"pollSeconds\": 10,\n  \"timeoutMinutes\": 20\n}",
        "options": {}
      },
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [
        -900,
        220
      ],
      "id": "zvid-tc-0003",
      "name": "Config"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "source-sheet--cond",
              "leftValue": "={{ String($('Config').first().json.source || 'sheet').toLowerCase() === 'sheet' }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        -680,
        220
      ],
      "id": "zvid-tc-0004",
      "name": "Source?"
    },
    {
      "parameters": {
        "resource": "sheet",
        "operation": "read",
        "documentId": {
          "__rl": true,
          "mode": "list",
          "value": ""
        },
        "sheetName": {
          "__rl": true,
          "mode": "list",
          "value": ""
        },
        "options": {}
      },
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.5,
      "position": [
        -460,
        60
      ],
      "id": "zvid-tc-0005",
      "name": "Read testimonials sheet",
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "jsCode": "// The Google Sheets node emits one item per row plus a row_number field.\n// Keep every row that is marked Featured and has not been compiled yet, in\n// sheet order, up to maxQuotes. row_number is what the write-back matches on.\nconst cfg = $('Config').first().json;\nconst rows = $input.all().map((i) => i.json);\n\nconst isEmpty = (v) => v === undefined || v === null || String(v).trim() === '';\nconst truthy = (v) => {\n  const s = String(v == null ? '' : v).trim().toLowerCase();\n  return s === 'true' || s === 'yes' || s === 'y' || s === '1' || s === 'x' || s === '✓' || s === 'featured';\n};\nconst text = (v) => String(v == null ? '' : v).trim();\n\nconst maxQuotes = Math.max(2, Math.min(12, Number(cfg.maxQuotes) || 5));\nconst minQuotes = Math.max(2, Math.min(maxQuotes, Number(cfg.minQuotes) || 2));\n\nconst picked = [];\nconst skipped = { notFeatured: 0, alreadyCompiled: 0, noQuote: 0 };\n\nfor (const r of rows) {\n  if (!truthy(r.Featured)) { skipped.notFeatured += 1; continue; }\n  if (!isEmpty(r.Status)) { skipped.alreadyCompiled += 1; continue; }\n  const quote = text(r.Quote);\n  if (!quote) { skipped.noQuote += 1; continue; }\n  picked.push({\n    quote,\n    author: text(r.Author),\n    role: text(r.Role),\n    company: text(r.Company),\n    rating: text(r.Rating),\n    rowNumber: r.row_number,\n    recordId: '',\n  });\n  if (picked.length >= maxQuotes) break;\n}\n\nif (picked.length < minQuotes) {\n  return [{ json: { found: false, source: 'sheet', picked: picked.length, minQuotes, checked: rows.length, skipped } }];\n}\n\nreturn [{ json: { found: true, source: 'sheet', testimonials: picked, checked: rows.length, skipped } }];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -240,
        60
      ],
      "id": "zvid-tc-0006",
      "name": "Pick this month's"
    },
    {
      "parameters": {
        "url": "=https://api.airtable.com/v0/{{ $('Config').first().json.airtableBaseId }}/{{ encodeURIComponent($('Config').first().json.airtableTable) }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpBearerAuth",
        "sendQuery": true,
        "specifyQuery": "json",
        "jsonQuery": "={{ JSON.stringify($('Config').first().json.airtableView ? { maxRecords: '100', view: $('Config').first().json.airtableView } : { maxRecords: '100' }) }}",
        "options": {
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": true
            }
          },
          "timeout": 20000
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        -460,
        400
      ],
      "id": "zvid-tc-0007",
      "name": "Fetch Airtable records",
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "jsCode": "// Same filter as the sheet path, against Airtable's { records: [{ id, fields }] }.\n// Field names must match the sheet header exactly: Quote, Author, Role,\n// Company, Rating, Featured, Status. Airtable has no row numbers, so the sheet\n// write-back is skipped in this mode (see the Run summary node).\nconst cfg = $('Config').first().json;\nconst res = $input.first().json;\nconst status = Number(res && res.statusCode);\nconst body = (res && res.body) || {};\n\nif (!Number.isFinite(status) || status === 0) {\n  throw new Error('Airtable did not answer. Check airtableBaseId / airtableTable in Config and that the Bearer Auth credential holds a personal access token.');\n}\nif (status >= 400) {\n  const e = body && body.error;\n  const msg = e ? (e.message || e.type || JSON.stringify(e)) : JSON.stringify(body).slice(0, 300);\n  throw new Error('Airtable answered HTTP ' + status + ': ' + msg);\n}\n\nconst records = Array.isArray(body.records) ? body.records : [];\nconst isEmpty = (v) => v === undefined || v === null || String(v).trim() === '';\nconst truthy = (v) => {\n  if (v === true) return true;\n  const s = String(v == null ? '' : v).trim().toLowerCase();\n  return s === 'true' || s === 'yes' || s === 'y' || s === '1' || s === 'x' || s === '✓' || s === 'featured';\n};\nconst text = (v) => (Array.isArray(v) ? v.join(' ') : String(v == null ? '' : v)).trim();\n\nconst maxQuotes = Math.max(2, Math.min(12, Number(cfg.maxQuotes) || 5));\nconst minQuotes = Math.max(2, Math.min(maxQuotes, Number(cfg.minQuotes) || 2));\n\nconst picked = [];\nconst skipped = { notFeatured: 0, alreadyCompiled: 0, noQuote: 0 };\n\nfor (const rec of records) {\n  const f = (rec && rec.fields) || {};\n  if (!truthy(f.Featured)) { skipped.notFeatured += 1; continue; }\n  if (!isEmpty(f.Status)) { skipped.alreadyCompiled += 1; continue; }\n  const quote = text(f.Quote);\n  if (!quote) { skipped.noQuote += 1; continue; }\n  picked.push({\n    quote,\n    author: text(f.Author),\n    role: text(f.Role),\n    company: text(f.Company),\n    rating: text(f.Rating),\n    rowNumber: 0,\n    recordId: String(rec.id || ''),\n  });\n  if (picked.length >= maxQuotes) break;\n}\n\nif (picked.length < minQuotes) {\n  return [{ json: { found: false, source: 'airtable', picked: picked.length, minQuotes, checked: records.length, skipped } }];\n}\n\nreturn [{ json: { found: true, source: 'airtable', testimonials: picked, checked: records.length, skipped } }];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -240,
        400
      ],
      "id": "zvid-tc-0008",
      "name": "Pick Airtable records"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "enough-testimonials--cond",
              "leftValue": "={{ $json.found }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        -20,
        220
      ],
      "id": "zvid-tc-0009",
      "name": "Enough testimonials?"
    },
    {
      "parameters": {
        "jsCode": "// Fewer than minQuotes rows are waiting. A compilation of one quote is not a\n// compilation, so this is a normal, successful outcome — not an error.\nconst picked = $input.first().json;\nreturn [{\n  json: {\n    rendered: false,\n    reason: 'Found ' + picked.picked + ' testimonial(s) marked Featured with an empty Status; this compilation needs at least ' + picked.minQuotes + '.',\n    source: picked.source,\n    rowsChecked: picked.checked,\n    skipped: picked.skipped,\n    fix: 'Tick Featured on more rows (and clear their Status), or lower minQuotes in Config.',\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        200,
        420
      ],
      "id": "zvid-tc-0010",
      "name": "Nothing to compile"
    },
    {
      "parameters": {
        "jsCode": "// Adds the month label and hands the builder one tidy item. The label is\n// DATA, not a design decision — monthLabelOverride pins it, monthLabelMode\n// picks the current month (default) or the one that just ended, which is what\n// you usually want from a run on the 1st.\nconst cfg = $('Config').first().json;\nconst picked = $input.first().json;\n\nconst MONTHS = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'];\n\nlet monthLabel = String(cfg.monthLabelOverride || '').trim();\nif (!monthLabel) {\n  const now = new Date();\n  const d = new Date(Date.UTC(now.getUTCFullYear(), now.getUTCMonth(), 1));\n  if (String(cfg.monthLabelMode || 'current').toLowerCase() === 'previous') {\n    d.setUTCMonth(d.getUTCMonth() - 1);\n  }\n  monthLabel = MONTHS[d.getUTCMonth()] + ' ' + d.getUTCFullYear();\n}\n\nconst list = Array.isArray(picked.testimonials) ? picked.testimonials : [];\n\nreturn [{\n  json: {\n    found: true,\n    source: picked.source,\n    monthLabel,\n    testimonials: list.map((t) => ({\n      quote: t.quote,\n      author: t.author,\n      role: t.role,\n      company: t.company,\n      rating: t.rating,\n    })),\n    rows: list.map((t) => ({\n      rowNumber: t.rowNumber || 0,\n      recordId: t.recordId || '',\n      author: t.author,\n    })),\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        200,
        160
      ],
      "id": "zvid-tc-0011",
      "name": "Prepare compilation"
    },
    {
      "parameters": {
        "method": "HEAD",
        "url": "={{ $('Config').first().json.musicUrl }}",
        "options": {
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": true
            }
          },
          "redirect": {
            "redirect": {}
          },
          "timeout": 15000
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        420,
        160
      ],
      "id": "zvid-tc-0012",
      "name": "Check music",
      "onError": "continueRegularOutput",
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "// The music bed is optional. An unreachable URL, an HTTP error or a file over\n// the plan's audio cap renders the compilation WITHOUT music instead of failing\n// the run — a silent testimonial reel still sells, a failed run does not.\nconst cfg = $('Config').first().json;\nconst prepared = $('Prepare compilation').first().json;\nconst res = $input.first().json;\n\nconst maxBytes = Number(cfg.maxMusicBytes) || 5242880;\nconst url = String(cfg.musicUrl || '').trim();\n\nlet musicUrl = url;\nlet reason = null;\nlet bytes = null;\n\nif (!url) {\n  musicUrl = '';\n  reason = 'musicUrl is empty in Config';\n} else {\n  const status = Number(res && res.statusCode);\n  const headers = (res && res.headers) || {};\n  const len = Number(headers['content-length'] || headers['Content-Length'] || 0);\n  bytes = Number.isFinite(len) && len > 0 ? len : null;\n  if (!Number.isFinite(status) || status === 0) {\n    musicUrl = '';\n    reason = 'the music URL was unreachable';\n  } else if (status >= 400) {\n    musicUrl = '';\n    reason = 'the music URL answered HTTP ' + status;\n  } else if (bytes && bytes > maxBytes) {\n    musicUrl = '';\n    reason = 'the music file is ' + Math.round(bytes / 1024) + ' KB, over the ' + Math.round(maxBytes / 1024) + ' KB cap';\n  }\n}\n\nreturn [{ json: { ...prepared, musicUrl, musicBytes: bytes, musicSkippedReason: reason } }];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        640,
        160
      ],
      "id": "zvid-tc-0013",
      "name": "Music guard"
    },
    {
      "parameters": {
        "jsCode": "// === buildProject — identical to the frame-reviewed standalone builder ===\n// Emits a Zvid TEMPLATE project: ONE testimonial scene carrying `iterate`,\n// plus the data in `variables`. Zvid resolves `iterate` into one scene per\n// testimonial before validation, so the scene count follows the sheet and the\n// payload stays the same size whether there are two quotes or twelve.\nfunction buildProject(config, data) {\n  const cfg = config || {};\n\n  // Sheet / Airtable text is untrusted twice over: it lands inside HTML, and\n  // it lands inside a template that resolves {{placeholders}}. Collapse\n  // whitespace, defuse braces, then HTML-escape at the variable boundary.\n  const clean = (v) => {\n    let s = String(v == null ? '' : v).replace(/\\s+/g, ' ');\n    // Collapse repeatedly: \"{{{{\" would otherwise fold back into \"{{\" and\n    // reopen a placeholder. Each pass strictly shortens the string.\n    while (s.indexOf('{{') !== -1 || s.indexOf('}}') !== -1) {\n      s = s.replace(/\\{\\{/g, '{').replace(/\\}\\}/g, '}');\n    }\n    return s.trim();\n  };\n  const esc = (s) =>\n    String(s).replace(\n      /[&<>\"']/g,\n      (c) =>\n        ({\n          '&': '&amp;',\n          '<': '&lt;',\n          '>': '&gt;',\n          '\"': '&quot;',\n          \"'\": '&#39;',\n        })[c]\n    );\n  const round = (n) => Math.round(n * 100) / 100;\n  const httpUrl = (u) => (/^https?:\\/\\//i.test(u) ? u : '');\n\n  // ---- config ------------------------------------------------------------\n  const brandName = clean(cfg.brandName) || 'Your brand';\n  const kickerText = (clean(cfg.kickerText) || 'CUSTOMER STORIES').toUpperCase().slice(0, 38);\n  const introTitle = clean(cfg.introTitle) || 'What our customers say';\n  const ctaText = clean(cfg.ctaText) || 'Join them';\n  const ctaUrl = clean(cfg.ctaUrl);\n  const logoUrl = httpUrl(clean(cfg.logoUrl));\n  const verifiedLabel = (clean(cfg.verifiedLabel) || 'VERIFIED CUSTOMER').toUpperCase();\n  const quoteFont = clean(cfg.quoteFont) || 'Playfair Display';\n  const uiFont = clean(cfg.uiFont) || 'Outfit';\n  const paper = clean(cfg.paperColor) || '#F4F1EA';\n  const ink = clean(cfg.inkColor) || '#12161B';\n  const accent = clean(cfg.accentColor) || '#2F6155';\n  const accentDark = clean(cfg.accentOnDark) || '#84C6B2';\n  const star = clean(cfg.starColor) || '#9A6B14';\n  const starDark = clean(cfg.starOnDark) || '#E8B94B';\n  const mutedPaper = clean(cfg.mutedOnPaper) || '#5C5548';\n  const mutedInk = clean(cfg.mutedOnInk) || '#96A1AC';\n  const maxQuotes = Math.max(2, Math.min(12, Number(cfg.maxQuotes) || 5));\n  const maxQuoteChars = Math.max(80, Math.min(400, Number(cfg.maxQuoteChars) || 260));\n\n  // ---- data --------------------------------------------------------------\n  const list = Array.isArray(data.testimonials) ? data.testimonials.slice(0, maxQuotes) : [];\n  if (list.length < 2) {\n    throw new Error(\n      'A compilation needs at least 2 testimonials, got ' +\n        list.length +\n        '. Mark more rows Featured (and leave their Status empty).'\n    );\n  }\n\n  const monthLabel = clean(data.monthLabel);\n\n  // ---- geometry ----------------------------------------------------------\n  // 1080x1080. Everything below the quote sits on a fixed baseline grid, so\n  // the cut from one testimonial to the next never moves the author block —\n  // only the quote itself re-flows.\n  const MARGIN = 96;\n  const BOX = 888; // 1080 - 2*96\n  const QUOTE_TOP = 330;\n  const QUOTE_BOTTOM = 748;\n  const QUOTE_H = QUOTE_BOTTOM - QUOTE_TOP;\n  const BADGE_Y = 780;\n  const RULE_Y = 860;\n  const RING_CY = 940;\n  const TD = 0.55; // testimonial -> testimonial transition\n\n  // Greedy word-wrap estimate, calibrated the same way the quote-reel template\n  // was: Playfair Display at 600 weight averages ~0.52em per glyph.\n  const wrapLines = (text, fontPx, boxPx, avgEm) => {\n    const perLine = Math.max(6, Math.floor(boxPx / (fontPx * avgEm)));\n    let lines = 1;\n    let used = 0;\n    for (const w of String(text).split(' ')) {\n      const need = used === 0 ? w.length : used + 1 + w.length;\n      if (need <= perLine) {\n        used = need;\n        continue;\n      }\n      lines += 1;\n      used = Math.min(w.length, perLine);\n    }\n    return lines;\n  };\n\n  // Pick the largest size whose wrapped block still fits the quote window, so\n  // a 40-character quote sets big and a 260-character one steps down instead\n  // of running into the star row.\n  const fitBlock = (text, sizes, boxPx, avgEm, lineHeight, maxH) => {\n    let chosen = null;\n    for (const px of sizes) {\n      const lines = wrapLines(text, px, boxPx, avgEm);\n      const height = Math.round(lines * px * lineHeight);\n      chosen = { size: px, lines, height };\n      if (height <= maxH) return chosen;\n    }\n    return chosen;\n  };\n\n  // Long quotes are trimmed on a word boundary rather than clipped on screen.\n  const truncate = (text, max) => {\n    if (text.length <= max) return { text, truncated: false };\n    const cut = text.slice(0, max);\n    const at = cut.lastIndexOf(' ');\n    return {\n      text: (at > max * 0.6 ? cut.slice(0, at) : cut).replace(/[,;:.–—-]+$/, '') + '…',\n      truncated: true,\n    };\n  };\n\n  const initialsOf = (name) => {\n    const parts = String(name)\n      .split(' ')\n      .filter((p) => /[A-Za-z0-9]/.test(p));\n    const letters = parts\n      .slice(0, 2)\n      .map((p) => p.replace(/[^A-Za-z0-9]/g, '').charAt(0))\n      .join('');\n    return (letters || String(name).trim().charAt(0) || 'A').toUpperCase();\n  };\n\n  const pad2 = (n) => (n < 10 ? '0' + n : String(n));\n\n  // Letterspaced caps blow past their box long before a normal word-wrap\n  // estimate says they should, so tracked labels get their own ramp: pick the\n  // first (size, tracking) pair whose measured run fits the box on one line.\n  const trackedFit = (text, boxPx, steps) => {\n    const n = String(text).length;\n    for (const s of steps) {\n      if (n * (s[0] * 0.62 + s[1]) <= boxPx) return { size: s[0], tracking: s[1] };\n    }\n    const last = steps[steps.length - 1];\n    return { size: last[0], tracking: last[1] };\n  };\n  const plainFit = (text, boxPx, sizes, em) => {\n    const n = String(text).length;\n    for (const px of sizes) {\n      if (n * px * em <= boxPx) return px;\n    }\n    return sizes[sizes.length - 1];\n  };\n\n  // A five-point star as pure geometry — no font is involved, so the row looks\n  // identical on every renderer.\n  const starPath = (cx, cy, r) => {\n    const pts = [];\n    for (let i = 0; i < 10; i++) {\n      const rad = i % 2 === 0 ? r : r * 0.42;\n      const a = (Math.PI / 5) * i - Math.PI / 2;\n      pts.push((cx + rad * Math.cos(a)).toFixed(1) + ' ' + (cy + rad * Math.sin(a)).toFixed(1));\n    }\n    return 'M ' + pts.join(' L ') + ' Z';\n  };\n\n  const starsSvg = (filled, color) => {\n    let out =\n      \"<svg width='212' height='40' viewBox='0 0 212 40' xmlns='http://www.w3.org/2000/svg'>\";\n    for (let i = 0; i < 5; i++) {\n      out +=\n        \"<path d='\" +\n        starPath(18 + i * 44, 20, 18) +\n        \"' fill='\" +\n        color +\n        \"' fill-opacity='\" +\n        (i < filled ? '1' : '0.22') +\n        \"'/>\";\n    }\n    return out + '</svg>';\n  };\n\n  // Chapter rail across the top frame edge: one segment per testimonial,\n  // filled up to the current one. Baked per scene, so it is deterministic.\n  const railSvg = (index, total, fillColor, lineColor) => {\n    const gap = 8;\n    const seg = (1000 - (total - 1) * gap) / total;\n    let out = '';\n    for (let k = 0; k < total; k++) {\n      const x = 40 + k * (seg + gap);\n      // 4px tall and centred on the frame's 1.4px top stroke, so the rail\n      // covers the hairline instead of half-blending with it.\n      out +=\n        \"<rect x='\" +\n        x.toFixed(1) +\n        \"' y='38' width='\" +\n        seg.toFixed(1) +\n        \"' height='4' fill='\" +\n        (k <= index ? fillColor : lineColor) +\n        \"' fill-opacity='\" +\n        (k <= index ? '1' : '0.18') +\n        \"'/>\";\n    }\n    return out;\n  };\n\n  const FRAME = \"<rect x='40' y='40' width='1000' height='1000' fill='none' \";\n\n  // The marginal rule beside the quote: it tracks the quote block, so a short\n  // testimonial and a long one both read as \"this bit is the quotation\".\n  const markSvg = (color, y, h) =>\n    \"<rect x='38' y='\" + y + \"' width='5' height='\" + h + \"' fill='\" + color + \"'/>\";\n\n  // Treatment A — warm paper card, accent tick on the left frame edge.\n  const panelPaper = (index, total, markY, markH) =>\n    \"<svg width='1080' height='1080' viewBox='0 0 1080 1080' xmlns='http://www.w3.org/2000/svg'>\" +\n    \"<defs><linearGradient id='tcpa' x1='0' y1='0' x2='0.55' y2='1'>\" +\n    \"<stop offset='0' stop-color='#FCFAF6'/><stop offset='1' stop-color='#E9E3D5'/></linearGradient>\" +\n    \"<radialGradient id='tcpg' cx='0.06' cy='0.04' r='0.75'>\" +\n    \"<stop offset='0' stop-color='\" +\n    accent +\n    \"' stop-opacity='0.13'/>\" +\n    \"<stop offset='1' stop-color='\" +\n    accent +\n    \"' stop-opacity='0'/></radialGradient></defs>\" +\n    \"<rect width='1080' height='1080' fill='url(#tcpa)'/>\" +\n    \"<rect width='1080' height='1080' fill='url(#tcpg)'/>\" +\n    FRAME +\n    \"stroke='\" +\n    ink +\n    \"' stroke-opacity='0.14' stroke-width='1.4'/>\" +\n    railSvg(index, total, accent, ink) +\n    markSvg(accent, markY, markH) +\n    \"<circle cx='138' cy='\" +\n    RING_CY +\n    \"' r='42' fill='none' stroke='\" +\n    accent +\n    \"' stroke-opacity='0.5' stroke-width='2'/>\" +\n    '</svg>';\n\n  // Treatment B — deep ink card, accent corner wedge bottom-right.\n  const panelInk = (index, total, markY, markH) =>\n    \"<svg width='1080' height='1080' viewBox='0 0 1080 1080' xmlns='http://www.w3.org/2000/svg'>\" +\n    \"<defs><linearGradient id='tcia' x1='0' y1='0' x2='0.5' y2='1'>\" +\n    \"<stop offset='0' stop-color='#1B242B'/><stop offset='1' stop-color='#0C1013'/></linearGradient>\" +\n    \"<radialGradient id='tcig' cx='0.94' cy='0.92' r='0.8'>\" +\n    \"<stop offset='0' stop-color='\" +\n    accentDark +\n    \"' stop-opacity='0.17'/>\" +\n    \"<stop offset='1' stop-color='\" +\n    accentDark +\n    \"' stop-opacity='0'/></radialGradient></defs>\" +\n    \"<rect width='1080' height='1080' fill='url(#tcia)'/>\" +\n    \"<rect width='1080' height='1080' fill='url(#tcig)'/>\" +\n    \"<path d='M 1040 700 L 1040 1040 L 700 1040 Z' fill='\" +\n    accentDark +\n    \"' fill-opacity='0.08'/>\" +\n    FRAME +\n    \"stroke='\" +\n    paper +\n    \"' stroke-opacity='0.16' stroke-width='1.4'/>\" +\n    railSvg(index, total, accentDark, paper) +\n    markSvg(accentDark, markY, markH) +\n    \"<circle cx='138' cy='\" +\n    RING_CY +\n    \"' r='42' fill='none' stroke='\" +\n    accentDark +\n    \"' stroke-opacity='0.5' stroke-width='2'/>\" +\n    '</svg>';\n\n  // Intro / outro share one title panel so the film is bookended.\n  const panelTitle =\n    \"<svg width='1080' height='1080' viewBox='0 0 1080 1080' xmlns='http://www.w3.org/2000/svg'>\" +\n    \"<defs><linearGradient id='tcta' x1='0.1' y1='0' x2='0.9' y2='1'>\" +\n    \"<stop offset='0' stop-color='#1D272E'/><stop offset='1' stop-color='#0B0F12'/></linearGradient>\" +\n    \"<radialGradient id='tctg' cx='0.5' cy='0.4' r='0.62'>\" +\n    \"<stop offset='0' stop-color='\" +\n    accentDark +\n    \"' stop-opacity='0.2'/>\" +\n    \"<stop offset='1' stop-color='\" +\n    accentDark +\n    \"' stop-opacity='0'/></radialGradient></defs>\" +\n    \"<rect width='1080' height='1080' fill='url(#tcta)'/>\" +\n    \"<rect width='1080' height='1080' fill='url(#tctg)'/>\" +\n    FRAME +\n    \"stroke='\" +\n    paper +\n    \"' stroke-opacity='0.16' stroke-width='1.4'/>\" +\n    \"<circle cx='540' cy='540' r='424' fill='none' stroke='\" +\n    accentDark +\n    \"' stroke-opacity='0.12' stroke-width='1.4'/>\" +\n    '</svg>';\n\n  const ruleSvg = (color) =>\n    \"<svg width='120' height='3' viewBox='0 0 120 3' xmlns='http://www.w3.org/2000/svg'>\" +\n    \"<rect width='120' height='3' fill='\" +\n    color +\n    \"'/></svg>\";\n\n  const dividerSvg = (color, opacity) =>\n    \"<svg width='\" +\n    BOX +\n    \"' height='2' viewBox='0 0 \" +\n    BOX +\n    \" 2' xmlns='http://www.w3.org/2000/svg'>\" +\n    \"<rect width='\" +\n    BOX +\n    \"' height='2' fill='\" +\n    color +\n    \"' fill-opacity='\" +\n    opacity +\n    \"'/></svg>\";\n\n  // ---- per-testimonial variables ----------------------------------------\n  const total = list.length;\n  const items = [];\n  let truncatedCount = 0;\n  let starredCount = 0;\n\n  list.forEach((raw, i) => {\n    const rawQuote = clean(raw.quote);\n    const author = clean(raw.author) || 'Anonymous';\n    if (!rawQuote) {\n      throw new Error(\n        'Testimonial ' + (i + 1) + ' (' + author + ') has no quote text — fill its Quote cell.'\n      );\n    }\n    const t = truncate(rawQuote, maxQuoteChars);\n    if (t.truncated) truncatedCount += 1;\n    const quote = t.text;\n\n    const role = clean(raw.role);\n    const company = clean(raw.company);\n    // The author block is optically centred on the ring, so the role/company\n    // line is capped at two lines — past that it is trimmed on a word boundary.\n    const metaRaw = [role, company].filter(Boolean).join(' · ') || 'Customer';\n    const metaLine = truncate(metaRaw, 118).text;\n    const metaLines = Math.min(2, wrapLines(metaLine, 25, 760, 0.5));\n    const authorName = truncate(author, 44).text;\n    const authorSize = plainFit(authorName, 760, [38, 34, 30, 27], 0.55);\n    const authorY = Math.round(\n      RING_CY - (Math.round(authorSize * 1.15) + 8 + metaLines * 33) / 2\n    );\n\n    const ratingNum = Number(String(raw.rating == null ? '' : raw.rating).replace(/[^0-9.]/g, ''));\n    const filled =\n      Number.isFinite(ratingNum) && ratingNum >= 1 ? Math.min(5, Math.round(ratingNum)) : 0;\n    const hasRating = filled >= 1;\n    if (hasRating) starredCount += 1;\n\n    const dark = i % 2 === 1;\n\n    const qLen = quote.length;\n    const fit = fitBlock(quote, [66, 60, 54, 48, 43, 38, 34], BOX, 0.52, 1.3, QUOTE_H);\n    const quoteY = Math.round(QUOTE_TOP + Math.max(0, (QUOTE_H - fit.height) / 2));\n    // The opening glyph and the marginal rule travel with the quote block so\n    // the three read as one group at every quote length.\n    const glyphY = Math.max(150, quoteY - 105);\n    const markH = Math.max(140, fit.height);\n\n    // Reading time, floored so a two-word quote still lands, capped so one\n    // long testimonial cannot swallow the compilation.\n    const seconds = round(Math.min(9.5, Math.max(5, 3.4 + qLen * 0.03)));\n\n    items.push({\n      quote: esc(quote),\n      quoteSize: fit.size + 'px',\n      quoteY,\n      glyphY,\n      author: esc(authorName),\n      metaLine: esc(metaLine),\n      authorSize: authorSize + 'px',\n      authorY,\n      initials: esc(initialsOf(author)),\n      counter: pad2(i + 1) + ' / ' + pad2(total),\n      hasRating,\n      noRating: !hasRating,\n      rating: filled,\n      duration: round(seconds + TD),\n      seconds,\n      sceneBg: dark ? ink : paper,\n      textColor: dark ? paper : ink,\n      mutedColor: dark ? mutedInk : mutedPaper,\n      accentColor: dark ? accentDark : accent,\n      panelSvg: dark\n        ? panelInk(i, total, quoteY, markH)\n        : panelPaper(i, total, quoteY, markH),\n      starsSvg: starsSvg(filled, dark ? starDark : star),\n      dividerSvg: dividerSvg(dark ? paper : ink, dark ? '0.2' : '0.16'),\n      treatment: dark ? 'ink' : 'paper',\n      quoteChars: qLen,\n      quoteLines: fit.lines,\n    });\n  });\n\n  // ---- intro / outro copy -------------------------------------------------\n  const titleFit = fitBlock(introTitle, [82, 72, 62, 54, 46], 880, 0.53, 1.16, 280);\n  const titleY = Math.round(380 + Math.max(0, (280 - titleFit.height) / 2));\n  const introSub =\n    total + (total === 1 ? ' story' : ' stories') + ' from the people who use ' + brandName;\n  const monogram = initialsOf(brandName);\n  const introSeconds = 3.9;\n  const outroSeconds = 4.2;\n  const footerLine =\n    [monthLabel, total + (total === 1 ? ' customer story' : ' customer stories')]\n      .filter(Boolean)\n      .join(' · ');\n\n  // ---- scenes -------------------------------------------------------------\n  const introDuration = round(introSeconds + 0.6);\n  const introScene = {\n    id: 'intro',\n    duration: introDuration,\n    backgroundColor: ink,\n    transition: 'smoothleft',\n    transitionId: 'say',\n    transitionDuration: 0.6,\n    visuals: [\n      {\n        type: 'SVG',\n        width: 1080,\n        height: 1080,\n        track: 0,\n        enterBegin: 0,\n        exitEnd: introDuration,\n        svg: '{{panelTitle}}',\n      },\n      {\n        type: 'TEXT',\n        x: 540,\n        y: 286,\n        width: 880,\n        anchor: 'top-center',\n        track: 1,\n        opacity: 0.05,\n        enterBegin: 0,\n        enterEnd: 1.1,\n        enterAnimation: 'circleopen',\n        exitEnd: introDuration,\n        style: { fontFamily: '{{quoteFont}}', color: '{{paperColor}}' },\n        html: '<div style=\"text-align:center; color:{{paperColor}}; font-size:440px; line-height:1; font-weight:700;\">{{monogram}}</div>',\n      },\n      {\n        type: 'TEXT',\n        x: 540,\n        y: 152,\n        width: 880,\n        anchor: 'top-center',\n        track: 2,\n        enterBegin: 0.2,\n        enterEnd: 0.7,\n        enterAnimation: 'fade',\n        exitEnd: introDuration,\n        style: { fontFamily: '{{uiFont}}', color: '{{paperColor}}' },\n        html: '<div style=\"text-align:center; color:{{paperColor}}; font-size:{{brandChipSize}}; font-weight:700; letter-spacing:{{brandChipTracking}};\">{{brandChip}}</div>',\n      },\n      {\n        type: 'TEXT',\n        x: 540,\n        y: 216,\n        width: 880,\n        anchor: 'top-center',\n        track: 3,\n        enterBegin: 0.35,\n        enterEnd: 0.85,\n        enterAnimation: 'fade',\n        exitEnd: introDuration,\n        style: { fontFamily: '{{uiFont}}', color: '{{accentOnDark}}' },\n        html: '<div style=\"text-align:center;\"><span style=\"display:inline-block; border:1.5px solid {{accentOnDark}}; color:{{accentOnDark}}; border-radius:999px; padding:9px 26px; font-size:22px; font-weight:700; letter-spacing:5px;\">{{monthChip}}</span></div>',\n      },\n      {\n        type: 'TEXT',\n        x: 540,\n        y: '{{introTitleY}}',\n        width: 880,\n        anchor: 'top-center',\n        track: 4,\n        enterBegin: 0.55,\n        enterEnd: 1.25,\n        enterAnimation: 'smoothup',\n        exitEnd: introDuration,\n        style: { fontFamily: '{{quoteFont}}', color: '{{paperColor}}' },\n        html: '<div style=\"text-align:center; color:{{paperColor}}; font-size:{{introTitleSize}}; line-height:1.16; font-weight:600;\">{{introTitle}}</div>',\n      },\n      {\n        type: 'SVG',\n        x: 540,\n        y: 700,\n        width: 120,\n        height: 3,\n        anchor: 'top-center',\n        track: 5,\n        enterBegin: 1.1,\n        enterEnd: 1.6,\n        enterAnimation: 'wiperight',\n        exitEnd: introDuration,\n        svg: '{{introRuleSvg}}',\n      },\n      {\n        type: 'TEXT',\n        x: 540,\n        y: 748,\n        width: 800,\n        anchor: 'top-center',\n        track: 6,\n        enterBegin: 1.25,\n        enterEnd: 1.8,\n        enterAnimation: 'fade',\n        exitEnd: introDuration,\n        style: { fontFamily: '{{uiFont}}', color: '{{mutedOnInk}}' },\n        html: '<div style=\"text-align:center; color:{{mutedOnInk}}; font-size:28px; font-weight:500; line-height:1.4;\">{{introSub}}</div>',\n      },\n    ],\n  };\n\n  // ONE scene definition; `iterate` turns it into one scene per testimonial.\n  const sayScene = {\n    id: 'say',\n    iterate: 'testimonials',\n    iterateAs: 'item',\n    duration: '{{item.duration}}',\n    backgroundColor: '{{item.sceneBg}}',\n    transition: 'smoothleft',\n    transitionId: 'outro',\n    transitionDuration: TD,\n    visuals: [\n      {\n        type: 'SVG',\n        width: 1080,\n        height: 1080,\n        track: 0,\n        enterBegin: 0,\n        exitEnd: '{{item.duration}}',\n        svg: '{{item.panelSvg}}',\n      },\n      {\n        type: 'TEXT',\n        x: MARGIN,\n        y: 96,\n        width: 560,\n        anchor: 'top-left',\n        track: 1,\n        enterBegin: 0.15,\n        enterEnd: 0.6,\n        enterAnimation: 'fade',\n        exitEnd: '{{item.duration}}',\n        style: { fontFamily: '{{uiFont}}', color: '{{item.accentColor}}' },\n        html: '<div style=\"color:{{item.accentColor}}; font-size:{{kickerSize}}; font-weight:700; letter-spacing:{{kickerTracking}};\">{{kickerText}}</div>',\n      },\n      {\n        type: 'TEXT',\n        x: 984,\n        y: 96,\n        width: 300,\n        anchor: 'top-right',\n        track: 2,\n        enterBegin: 0.2,\n        enterEnd: 0.65,\n        enterAnimation: 'fade',\n        exitEnd: '{{item.duration}}',\n        style: { fontFamily: '{{uiFont}}', color: '{{item.mutedColor}}' },\n        html: '<div style=\"text-align:right; color:{{item.mutedColor}}; font-size:23px; font-weight:600; letter-spacing:5px;\">{{item.counter}}</div>',\n      },\n      {\n        type: 'TEXT',\n        x: 88,\n        y: '{{item.glyphY}}',\n        width: 400,\n        anchor: 'top-left',\n        track: 3,\n        opacity: 0.2,\n        enterBegin: 0.1,\n        enterEnd: 0.75,\n        enterAnimation: 'circleopen',\n        exitEnd: '{{item.duration}}',\n        style: { fontFamily: '{{quoteFont}}', color: '{{item.accentColor}}' },\n        html: '<div style=\"color:{{item.accentColor}}; font-size:168px; line-height:1; font-weight:700;\">&#8220;</div>',\n      },\n      {\n        type: 'TEXT',\n        x: MARGIN,\n        y: '{{item.quoteY}}',\n        width: BOX,\n        anchor: 'top-left',\n        track: 4,\n        enterBegin: 0.35,\n        enterEnd: 1.05,\n        enterAnimation: 'smoothup',\n        exitEnd: '{{item.duration}}',\n        style: { fontFamily: '{{quoteFont}}', color: '{{item.textColor}}' },\n        html: '<div style=\"color:{{item.textColor}}; font-size:{{item.quoteSize}}; line-height:1.3; font-weight:600;\">{{item.quote}}</div>',\n      },\n      {\n        type: 'SVG',\n        condition: '{{item.hasRating}}',\n        x: MARGIN,\n        y: BADGE_Y,\n        width: 212,\n        height: 40,\n        anchor: 'top-left',\n        track: 5,\n        enterBegin: 0.85,\n        enterEnd: 1.3,\n        enterAnimation: 'fade',\n        exitEnd: '{{item.duration}}',\n        svg: '{{item.starsSvg}}',\n      },\n      {\n        type: 'TEXT',\n        condition: '{{item.noRating}}',\n        x: MARGIN,\n        y: BADGE_Y - 6,\n        width: 560,\n        anchor: 'top-left',\n        track: 5,\n        enterBegin: 0.85,\n        enterEnd: 1.3,\n        enterAnimation: 'fade',\n        exitEnd: '{{item.duration}}',\n        style: { fontFamily: '{{uiFont}}', color: '{{item.accentColor}}' },\n        html: '<div><span style=\"display:inline-block; border:1.5px solid {{item.accentColor}}; color:{{item.accentColor}}; border-radius:999px; padding:9px 24px; font-size:21px; font-weight:700; letter-spacing:4px;\">{{verifiedLabel}}</span></div>',\n      },\n      {\n        type: 'SVG',\n        x: MARGIN,\n        y: RULE_Y,\n        width: BOX,\n        height: 2,\n        anchor: 'top-left',\n        track: 6,\n        enterBegin: 0.6,\n        enterEnd: 1.15,\n        enterAnimation: 'wiperight',\n        exitEnd: '{{item.duration}}',\n        svg: '{{item.dividerSvg}}',\n      },\n      {\n        type: 'TEXT',\n        x: MARGIN,\n        y: 922,\n        width: 84,\n        anchor: 'top-left',\n        track: 7,\n        enterBegin: 1,\n        enterEnd: 1.4,\n        enterAnimation: 'fade',\n        exitEnd: '{{item.duration}}',\n        style: { fontFamily: '{{uiFont}}', color: '{{item.textColor}}' },\n        html: '<div style=\"text-align:center; color:{{item.textColor}}; font-size:30px; line-height:1.2; font-weight:700; letter-spacing:1px;\">{{item.initials}}</div>',\n      },\n      {\n        type: 'TEXT',\n        x: 204,\n        y: '{{item.authorY}}',\n        width: 760,\n        anchor: 'top-left',\n        track: 8,\n        enterBegin: 1.05,\n        enterEnd: 1.5,\n        enterAnimation: 'smoothleft',\n        exitEnd: '{{item.duration}}',\n        style: { fontFamily: '{{uiFont}}', color: '{{item.textColor}}' },\n        html:\n          '<div style=\"color:{{item.textColor}}; font-size:{{item.authorSize}}; line-height:1.15; font-weight:700;\">{{item.author}}</div>' +\n          '<div style=\"margin-top:8px; color:{{item.mutedColor}}; font-size:25px; line-height:1.3; font-weight:500;\">{{item.metaLine}}</div>',\n      },\n    ],\n  };\n\n  const outroScene = {\n    id: 'outro',\n    duration: outroSeconds,\n    backgroundColor: ink,\n    visuals: [\n      {\n        type: 'SVG',\n        width: 1080,\n        height: 1080,\n        track: 0,\n        enterBegin: 0,\n        exitEnd: outroSeconds,\n        svg: '{{panelTitle}}',\n      },\n      {\n        type: 'IMAGE',\n        condition: '{{hasLogo}}',\n        src: '{{logoUrl}}',\n        x: 540,\n        y: 300,\n        width: 260,\n        height: 190,\n        anchor: 'top-center',\n        resize: 'contain',\n        track: 1,\n        enterBegin: 0.15,\n        enterEnd: 0.7,\n        enterAnimation: 'fade',\n        exitEnd: outroSeconds,\n      },\n      {\n        type: 'TEXT',\n        condition: '{{noLogo}}',\n        x: 540,\n        y: 296,\n        width: 880,\n        anchor: 'top-center',\n        track: 1,\n        enterBegin: 0.15,\n        enterEnd: 0.8,\n        enterAnimation: 'circleopen',\n        exitEnd: outroSeconds,\n        style: { fontFamily: '{{quoteFont}}', color: '{{accentOnDark}}' },\n        html: '<div style=\"text-align:center; color:{{accentOnDark}}; font-size:150px; line-height:1.3; font-weight:700; letter-spacing:4px;\">{{monogram}}</div>',\n      },\n      {\n        type: 'SVG',\n        x: 540,\n        y: 524,\n        width: 120,\n        height: 3,\n        anchor: 'top-center',\n        track: 2,\n        enterBegin: 0.3,\n        enterEnd: 0.85,\n        enterAnimation: 'wiperight',\n        exitEnd: outroSeconds,\n        svg: '{{introRuleSvg}}',\n      },\n      {\n        type: 'TEXT',\n        x: 540,\n        y: '{{ctaY}}',\n        width: 880,\n        anchor: 'top-center',\n        track: 3,\n        enterBegin: 0.45,\n        enterEnd: 1.1,\n        enterAnimation: 'smoothup',\n        exitEnd: outroSeconds,\n        style: { fontFamily: '{{quoteFont}}', color: '{{paperColor}}' },\n        html: '<div style=\"text-align:center; color:{{paperColor}}; font-size:{{ctaSize}}; line-height:1.18; font-weight:600;\">{{ctaText}}</div>',\n      },\n      {\n        // The pill IS this element: its own width/height + backgroundColor and\n        // borderRadius on `style`, label centred with flex. Sizing a card with\n        // CSS padding instead would push it past the declared box (the\n        // renderer is content-box), and a style-less pill also reads as\n        // dark-on-dark to the contrast lint, which only sees `style`.\n        type: 'TEXT',\n        condition: '{{hasCtaUrl}}',\n        x: 540,\n        y: '{{ctaUrlY}}',\n        width: '{{ctaPillW}}',\n        height: '{{ctaPillH}}',\n        anchor: 'top-center',\n        track: 4,\n        enterBegin: 0.9,\n        enterEnd: 1.4,\n        enterAnimation: 'circleopen',\n        exitEnd: outroSeconds,\n        style: {\n          fontFamily: '{{uiFont}}',\n          color: '{{inkColor}}',\n          backgroundColor: '{{accentOnDark}}',\n          borderRadius: '999px',\n          display: 'flex',\n          alignItems: 'center',\n          justifyContent: 'center',\n        },\n        html: '<div style=\"text-align:center; color:{{inkColor}}; font-size:{{ctaUrlSize}}; line-height:1.2; font-weight:700;\">{{ctaUrl}}</div>',\n      },\n      {\n        type: 'TEXT',\n        x: 540,\n        y: 872,\n        width: 880,\n        anchor: 'top-center',\n        track: 5,\n        enterBegin: 1.2,\n        enterEnd: 1.7,\n        enterAnimation: 'fade',\n        exitEnd: outroSeconds,\n        style: { fontFamily: '{{uiFont}}', color: '{{mutedOnInk}}' },\n        html: '<div style=\"text-align:center; color:{{mutedOnInk}}; font-size:{{footerSize}}; font-weight:600; letter-spacing:{{footerTracking}};\">{{footerLine}}</div>',\n      },\n    ],\n  };\n\n  // ---- template payload ---------------------------------------------------\n  const ctaFit = fitBlock(ctaText, [62, 54, 46, 40], 880, 0.53, 1.18, 150);\n  const ctaUrlText = truncate(ctaUrl, 56).text;\n  // The CTA pill is one element, so its box has to be the pill: derive the\n  // width from the SAME em constant the size ramp uses, so the label can never\n  // be wider than the capsule it sits in.\n  const CTA_URL_EM = 0.56;\n  const ctaUrlPx = plainFit(ctaUrlText, 780, [30, 26, 22, 20], CTA_URL_EM);\n  const ctaPillW = Math.min(\n    880,\n    Math.max(300, Math.round(ctaUrlText.length * ctaUrlPx * CTA_URL_EM) + 96)\n  );\n  const ctaPillH = Math.round(ctaUrlPx * 1.2) + 40;\n  const brandChip = brandName.toUpperCase().slice(0, 46);\n  // Tracked caps: fit to the box the label actually owns (the testimonial\n  // kicker shares its row with the counter, so it only gets 556px).\n  const kickerFit = trackedFit(kickerText, 556, [\n    [23, 7],\n    [21, 5.5],\n    [19, 4.5],\n    [17, 3.5],\n  ]);\n  const brandFit = trackedFit(brandChip, 840, [\n    [26, 9],\n    [23, 6.5],\n    [20, 4.5],\n    [18, 3.5],\n  ]);\n  const footerFit = trackedFit(footerLine, 840, [\n    [24, 4],\n    [22, 3],\n    [20, 2],\n    [18, 1.5],\n  ]);\n  const variables = {\n    brandName: esc(brandName),\n    brandChip: esc(brandChip),\n    brandChipSize: brandFit.size + 'px',\n    brandChipTracking: brandFit.tracking + 'px',\n    monogram: esc(monogram),\n    kickerText: esc(kickerText),\n    kickerSize: kickerFit.size + 'px',\n    kickerTracking: kickerFit.tracking + 'px',\n    footerSize: footerFit.size + 'px',\n    footerTracking: footerFit.tracking + 'px',\n    ctaUrlSize: ctaUrlPx + 'px',\n    ctaPillW,\n    ctaPillH,\n    introTitle: esc(introTitle),\n    introTitleSize: titleFit.size + 'px',\n    introTitleY: titleY,\n    introSub: esc(introSub),\n    monthChip: esc((monthLabel || kickerText).slice(0, 26)),\n    ctaText: esc(ctaText),\n    ctaSize: ctaFit.size + 'px',\n    ctaY: Math.max(556, Math.round(600 - ctaFit.height / 2)),\n    ctaUrlY: Math.min(\n      752,\n      Math.max(690, Math.max(556, Math.round(600 - ctaFit.height / 2)) + ctaFit.height + 70)\n    ),\n    ctaUrl: esc(ctaUrlText),\n    hasCtaUrl: Boolean(ctaUrlText),\n    logoUrl: logoUrl,\n    hasLogo: Boolean(logoUrl),\n    noLogo: !logoUrl,\n    verifiedLabel: esc(verifiedLabel),\n    footerLine: esc(footerLine),\n    quoteFont,\n    uiFont,\n    paperColor: paper,\n    inkColor: ink,\n    accentOnDark: accentDark,\n    mutedOnInk: mutedInk,\n    panelTitle,\n    introRuleSvg: ruleSvg(accentDark),\n    testimonials: items,\n  };\n\n  const totalSeconds = round(\n    introDuration +\n      items.reduce((sum, it) => sum + it.duration, 0) +\n      outroScene.duration -\n      0.6 -\n      TD * items.length\n  );\n\n  // The render API only allows letters, digits, spaces, _ and - in `name`.\n  const safeName = ('testimonials-' + (monthLabel || 'compilation'))\n    .toLowerCase()\n    .normalize('NFKD')\n    .replace(/[̀-ͯ]/g, '')\n    .replace(/[^a-z0-9]+/g, '-')\n    .replace(/^-+|-+$/g, '')\n    .slice(0, 60);\n\n  const payload = {\n    name: safeName || 'testimonial-compilation',\n    resolution: 'instagram-post',\n    frameRate: 30,\n    backgroundColor: ink,\n    outputFormat: 'mp4',\n    variables,\n    scenes: [introScene, sayScene, outroScene],\n  };\n\n  const musicUrl = httpUrl(clean(data.musicUrl));\n  if (musicUrl) {\n    // NOTE: no `track` here — the render API rejects audios[].track.\n    const vol = Number(cfg.musicVolume);\n    payload.audios = [{ src: musicUrl, volume: Number.isFinite(vol) ? vol : 0.12 }];\n  }\n\n  return {\n    payload,\n    meta: {\n      monthLabel,\n      quoteCount: items.length,\n      sceneCount: items.length + 2,\n      totalSeconds,\n      truncatedCount,\n      starredCount,\n      maxQuoteChars,\n      musicUrl,\n      musicSkippedReason: data.musicSkippedReason || null,\n      brandName,\n      monogram,\n      quoteSizes: items.map((it) => it.quoteSize),\n      quoteLines: items.map((it) => it.quoteLines),\n      treatments: items.map((it) => it.treatment),\n      sceneSeconds: items.map((it) => it.duration),\n      rows: Array.isArray(data.rows) ? data.rows : [],\n      source: data.source || 'sheet',\n    },\n  };\n}\n\n// === n8n glue ===\nconst config = $('Config').first().json;\nconst data = $input.first().json;\nconst built = buildProject(config, data);\nreturn [{ json: built }];\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        860,
        160
      ],
      "id": "zvid-tc-0014",
      "name": "Build project JSON"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $('Config').first().json.apiUrl }}/api/render/validate/api-key",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ payload: $json.payload }) }}",
        "options": {
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": true
            }
          }
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1080,
        160
      ],
      "id": "zvid-tc-0015",
      "name": "Validate project (free)"
    },
    {
      "parameters": {
        "jsCode": "// The free validate endpoint runs the exact pipeline a render submission runs\n// — including resolving the template's variables and expanding `iterate` into\n// one scene per testimonial — so a failure here is a payload bug. Surface the\n// field list instead of a generic HTTP error.\nconst res = $input.first().json;\nconst status = res.statusCode;\nconst body = res.body || {};\n\nif (status !== 200) {\n  const details = Array.isArray(body.details)\n    ? body.details.map((d) => '  - ' + d.field + ': ' + d.message).join('\\n')\n    : JSON.stringify(body).slice(0, 500);\n  throw new Error('Zvid rejected the project (HTTP ' + status + '): ' + (body.message || body.error) + '\\n' + details);\n}\n\nconst build = $('Build project JSON').first().json;\nreturn [{\n  json: {\n    ...build,\n    creditsRequired: body.creditsRequired,\n    warnings: body.warnings || [],\n    schemaVersion: body.schemaVersion,\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1300,
        160
      ],
      "id": "zvid-tc-0016",
      "name": "Check validation"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "dry-run--cond",
              "leftValue": "={{ $('Config').first().json.dryRun }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        1520,
        160
      ],
      "id": "zvid-tc-0017",
      "name": "Dry run?"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $('Config').first().json.apiUrl }}/api/projects",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ name: $json.payload.name, payload: $json.payload }) }}",
        "options": {}
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1740,
        0
      ],
      "id": "zvid-tc-0018",
      "name": "Save draft to editor",
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "jsCode": "// Dry run: everything except the paid render. Open editorLink to watch the\n// exact compilation, then set dryRun to false in Config to go live. No sheet\n// row is marked compiled on a dry run, so the next real run uses the same rows.\nconst cfg = $('Config').first().json;\nconst d = $('Check validation').first().json;\nconst guard = $('Music guard').first().json;\n\n// The draft save is best-effort (onError: continue) so a hiccup there never\n// hides the rest of the dry-run report.\nlet editorLink = null;\nlet draftId = null;\ntry {\n  const project = $('Save draft to editor').first().json.project;\n  if (project && project.id) {\n    draftId = project.id;\n    editorLink = String(cfg.editorUrl || 'https://editor.zvid.io').replace(/\\/+$/, '') + '/?project=' + project.id;\n  }\n} catch (e) { /* draft step skipped or failed */ }\n\nreturn [{\n  json: {\n    dryRun: true,\n    rendered: false,\n    videoUrl: null,\n    source: d.meta.source,\n    monthLabel: d.meta.monthLabel,\n    quotes: d.meta.quoteCount,\n    scenes: d.meta.sceneCount,\n    starred: d.meta.starredCount,\n    trimmedQuotes: d.meta.truncatedCount,\n    videoSeconds: d.meta.totalSeconds,\n    creditsRequired: d.creditsRequired,\n    warnings: d.warnings,\n    music: guard.musicUrl ? 'on' : ('off - ' + (guard.musicSkippedReason || 'no musicUrl')),\n    authors: (d.meta.rows || []).map((r) => r.author),\n    sheetRows: (d.meta.rows || []).map((r) => r.rowNumber),\n    sheetUpdated: false,\n    editorLink,\n    draftId,\n    nextStep: editorLink\n      ? 'Open editorLink to preview the draft, then set dryRun to false in Config to render for real.'\n      : 'Set dryRun to false in Config to render this for real.',\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1960,
        0
      ],
      "id": "zvid-tc-0019",
      "name": "Dry run summary"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $('Config').first().json.apiUrl }}/api/render/api-key",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ payload: $json.payload }) }}",
        "options": {}
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1740,
        320
      ],
      "id": "zvid-tc-0020",
      "name": "Submit render",
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "amount": "={{ $('Config').first().json.pollSeconds }}",
        "unit": "seconds"
      },
      "type": "n8n-nodes-base.wait",
      "typeVersion": 1.1,
      "position": [
        1960,
        320
      ],
      "id": "zvid-tc-0021",
      "name": "Wait",
      "webhookId": "b6f2d418-73ac-4e51-9f08-2d47c1a6e935"
    },
    {
      "parameters": {
        "url": "={{ $('Config').first().json.apiUrl }}/api/jobs/{{ $('Submit render').first().json.jobId }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "options": {}
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2180,
        320
      ],
      "id": "zvid-tc-0022",
      "name": "Get render status",
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "render-finished--cond",
              "leftValue": "={{ $json.state === 'completed' }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        2400,
        320
      ],
      "id": "zvid-tc-0023",
      "name": "Render finished?"
    },
    {
      "parameters": {
        "jsCode": "// Fails fast on a failed render and stops the poll loop from running forever.\nconst cfg = $('Config').first().json;\nconst job = $input.first().json;\n\nif (job.state === 'failed') {\n  throw new Error('Render failed: ' + (job.failedReason || 'no reason given'));\n}\n\nconst maxPolls = Math.ceil((Number(cfg.timeoutMinutes || 20) * 60) / Number(cfg.pollSeconds || 10));\nif ($runIndex + 1 >= maxPolls) {\n  throw new Error('Render did not finish within ' + cfg.timeoutMinutes + ' minutes (job ' + job.id + ', state ' + job.state + ').');\n}\n\nreturn $input.all();"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2400,
        540
      ],
      "id": "zvid-tc-0024",
      "name": "Still rendering?"
    },
    {
      "parameters": {
        "jsCode": "// One item per testimonial that made it into the film, each carrying the row\n// the write-back should stamp. Every used row gets the same video URL, because\n// they are all in the same compilation.\nconst cfg = $('Config').first().json;\nconst build = $('Check validation').first().json;\nconst job = $('Get render status').first().json;\nconst url = typeof job.result === 'string' ? job.result : (job.result ? job.result.url : null);\n\nconst rows = Array.isArray(build.meta.rows) ? build.meta.rows : [];\nreturn rows.map((r, i) => ({\n  json: {\n    rowNumber: r.rowNumber,\n    recordId: r.recordId,\n    author: r.author,\n    position: (i + 1) + ' / ' + rows.length,\n    status: cfg.statusDoneValue,\n    videoUrl: url,\n    jobId: job.id,\n  },\n}));"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2620,
        200
      ],
      "id": "zvid-tc-0025",
      "name": "Rows to mark"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "sheet-mode--cond",
              "leftValue": "={{ String($('Config').first().json.source || 'sheet').toLowerCase() === 'sheet' }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "looseTypeValidation": true,
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        2840,
        200
      ],
      "id": "zvid-tc-0026",
      "name": "Sheet mode?"
    },
    {
      "parameters": {
        "resource": "sheet",
        "operation": "update",
        "documentId": {
          "__rl": true,
          "mode": "list",
          "value": ""
        },
        "sheetName": {
          "__rl": true,
          "mode": "list",
          "value": ""
        },
        "columns": {
          "mappingMode": "defineBelow",
          "value": {
            "row_number": "={{ $json.rowNumber }}",
            "Status": "={{ $json.status }}",
            "VideoUrl": "={{ $json.videoUrl }}"
          },
          "matchingColumns": [
            "row_number"
          ],
          "schema": [
            {
              "id": "row_number",
              "displayName": "row_number",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "number",
              "canBeUsedToMatch": true,
              "readOnly": true,
              "removed": false
            },
            {
              "id": "Status",
              "displayName": "Status",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true,
              "removed": false
            },
            {
              "id": "VideoUrl",
              "displayName": "VideoUrl",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true,
              "removed": false
            }
          ]
        },
        "options": {}
      },
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.7,
      "position": [
        3060,
        80
      ],
      "id": "zvid-tc-0027",
      "name": "Mark rows compiled",
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "jsCode": "// Final report for a live run. In sheet mode every row used by the\n// compilation is already stamped by this point; in Airtable mode there is no\n// row_number to match on, so the records are reported here and left untouched.\nconst cfg = $('Config').first().json;\nconst build = $('Check validation').first().json;\nconst job = $('Get render status').first().json;\nconst guard = $('Music guard').first().json;\nconst url = typeof job.result === 'string' ? job.result : (job.result ? job.result.url : null);\n\nconst source = String(cfg.source || 'sheet').toLowerCase();\nconst rows = Array.isArray(build.meta.rows) ? build.meta.rows : [];\n\nreturn [{\n  json: {\n    rendered: true,\n    videoUrl: url,\n    jobId: job.id,\n    source,\n    monthLabel: build.meta.monthLabel,\n    quotes: build.meta.quoteCount,\n    scenes: build.meta.sceneCount,\n    starred: build.meta.starredCount,\n    trimmedQuotes: build.meta.truncatedCount,\n    videoSeconds: build.meta.totalSeconds,\n    creditsCharged: build.creditsRequired,\n    music: guard.musicUrl ? 'on' : ('off - ' + (guard.musicSkippedReason || 'no musicUrl')),\n    authors: rows.map((r) => r.author),\n    sheetRows: source === 'sheet' ? rows.map((r) => r.rowNumber) : [],\n    sheetStatus: source === 'sheet' ? cfg.statusDoneValue : null,\n    airtableRecords: source === 'sheet' ? [] : rows.map((r) => r.recordId),\n  },\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3280,
        200
      ],
      "id": "zvid-tc-0028",
      "name": "Run summary"
    },
    {
      "parameters": {
        "url": "={{ $json.videoUrl }}",
        "options": {
          "response": {
            "response": {
              "responseFormat": "file",
              "outputPropertyName": "data"
            }
          }
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        3500,
        200
      ],
      "id": "zvid-tc-0029",
      "name": "▶ Watch video",
      "onError": "continueRegularOutput",
      "alwaysOutputData": true,
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "width": 920,
        "height": 700,
        "color": 4,
        "content": "## Monthly customer-testimonial compilation\n\nOnce a month this workflow collects the testimonials you have ticked\n**Featured** in a spreadsheet and cuts them into ONE polished 1080x1080 square\nvideo — an intro card, one designed scene per quote (serif quote, star rating,\nauthor + role/company, chapter rail across the top), and a closing CTA card.\nSquare because that is what LinkedIn actually shows at full size in the feed.\n\n**The scene count follows your data.** The payload is a Zvid *template*: one\ntestimonial scene carrying `iterate`, plus the quotes as variables. Zvid\nexpands it server-side, so three quotes make five scenes and five quotes make\nseven — same payload, no branching, one render, one credit charge.\n\n### Setup (about three minutes)\n\n1. **Zvid API key** — create one at https://app.zvid.io/api-keys\n   Add an n8n **Header Auth** credential: name `x-api-key`, value = your key.\n   Attach it to *Validate project (free)*, *Save draft to editor*,\n   *Submit render* and *Get render status*.\n2. **Google Sheets** — attach your Google credential to *Read testimonials\n   sheet* and *Mark rows compiled*, then pick your spreadsheet and tab in both.\n   The columns are in the green note.\n3. **Open `Config`** — set `brandName`, `introTitle`, `ctaText` and\n   `ctaUrl`. Colours and fonts are there too; everything has a default.\n4. **Run it — the first run renders for real and spends about 28 credits**\n   for a three-quote compilation. `dryRun` is `false` out of the box. When\n   it finishes, click **▶ Watch video** to play the result right here in n8n.\n5. Want a free preview first? Set `dryRun: true` in **Config**: you get the\n   credit quote plus an **editorLink** to watch the video in the editor, with\n   nothing spent and the sheet untouched.\n6. **Activate** — it runs on the 1st of each month at 9am.\n\n### Cost per video\n\nLength scales with the quotes: roughly **1 credit per second**. A three-quote\ncompilation is ~27 s (**28 credits**); five quotes with one very long one is\n~47 s (**47 credits**). *Validate project (free)* runs before every render and\nreports the exact figure as `creditsCharged`; a `dryRun: true` pass gives\nyou that number without spending anything."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -2100,
        -160
      ],
      "id": "zvid-tc-0040",
      "name": "Note 40"
    },
    {
      "parameters": {
        "width": 660,
        "height": 420,
        "color": 5,
        "content": "## Your sheet: eight columns\n\nHeader row (row 1), spelled exactly like this:\n\n| Quote | Author | Role | Company | Rating | Featured | Status | VideoUrl |\n| --- | --- | --- | --- | --- | --- | --- | --- |\n\n- **Quote** and **Author** are the only ones that really matter. Role and\n  Company become the small line under the name (either one alone is fine).\n- **Rating** — `1`–`5`. Leave it EMPTY and that scene shows a\n  `Verified customer` chip instead of stars. Mixed is fine.\n- **Featured** — tick the ones you want in this month's cut: `TRUE`,\n  `yes`, `1`, `x` or `✓` all count.\n- **Status** — leave EMPTY for quotes waiting to be used. After a successful\n  render every row that made it in gets `compiled` + the video URL, so next\n  month picks up fresh ones.\n- The run takes the first `maxQuotes` (5) matching rows top to bottom and\n  needs at least `minQuotes` (2) — otherwise it reports \"nothing to compile\"\n  and stops cleanly."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -1120,
        560
      ],
      "id": "zvid-tc-0041",
      "name": "Note 41"
    },
    {
      "parameters": {
        "width": 620,
        "height": 380,
        "color": 3,
        "content": "## Airtable instead of a sheet (optional)\n\nSet `source: \"airtable\"` in **Config** and fill `airtableBaseId` +\n`airtableTable`. Then:\n\n1. Create a personal access token at\n   https://airtable.com/create/tokens with the `data.records:read` scope\n   and access to your base.\n2. Add an n8n **Bearer Auth** credential holding that token and attach it to\n   *Fetch Airtable records*.\n3. The base id is the `app…` segment of your Airtable URL.\n4. Name the fields exactly as the sheet columns: **Quote, Author, Role,\n   Company, Rating, Featured** (checkbox), **Status**.\n5. Optional: put `airtableView` to a view name to pre-filter.\n\n**Airtable rows are never written back** — there is no row number to match on,\nso the record ids are reported in *Run summary* and you decide what to do with\nthem. The Google Sheets path is the one that closes the loop automatically."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -420,
        620
      ],
      "id": "zvid-tc-0042",
      "name": "Note 42"
    },
    {
      "parameters": {
        "width": 640,
        "height": 380,
        "color": 6,
        "content": "## One payload, N scenes\n\n*Build project JSON* does **not** emit a finished project. It emits a Zvid\n**template**: a `variables` block holding your quotes, and a single scene\nmarked `iterate: \"testimonials\"`. Zvid expands that into one scene per\nquote before it validates or renders — which is why this workflow has no\nloops, no merge nodes and one render job no matter how many testimonials you\nfeatured.\n\nThe design adapts to the data as well:\n\n- Quote type ramps **66 px → 34 px** so a 23-character quote and a\n  260-character one both fill the same window without clipping; longer ones\n  are trimmed on a word boundary at `maxQuoteChars`.\n- Scenes alternate **paper / ink** so two quotes never look the same in a row.\n- The chapter rail across the top fills as the film advances.\n- Scene length follows reading time (5 – 9.5 s), so the total is honest."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        780,
        -260
      ],
      "id": "zvid-tc-0043",
      "name": "Note 43"
    },
    {
      "parameters": {
        "width": 620,
        "height": 300,
        "color": 4,
        "content": "## Free preview: the optional dry run\n\n**`dryRun` is `false` in Config, so a run renders for real.** Set it to\n`true` whenever you want a free pass instead: the workflow validates the\npayload, quotes the exact credit cost and saves the project as a **draft**,\nthen reports an **editorLink** like `https://editor.zvid.io/?project=prj_...`\n\nOpen it to watch the exact compilation and tweak anything by hand, with\nnothing spent. **No sheet row is marked `compiled` on a dry run**, so the\nnext real run uses the same quotes.\n\nSet `dryRun` back to `false` to render and stamp the rows."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        1460,
        -280
      ],
      "id": "zvid-tc-0044",
      "name": "Note 44"
    },
    {
      "parameters": {
        "width": 660,
        "height": 320,
        "color": 7,
        "content": "## Publishing the compilation (optional tail)\n\nThe workflow ends with the video URL in your sheet — a clean handoff you can\nextend after **Mark rows compiled**:\n\n- **LinkedIn** — the square 1080x1080 output is made for it. Pass `videoUrl`\n  to a scheduler such as Blotato, Buffer or Metricool over their HTTP API;\n  they accept a public video URL directly, no download step.\n- **YouTube** — an *HTTP Request* node (GET `videoUrl`, response format\n  *File*) followed by the native **YouTube** node (Video → Upload, binary\n  `data`). Needs a YouTube OAuth2 credential.\n- **Human in the loop** — a Slack or Email node that sends `videoUrl` to\n  whoever posts.\n\nThey are left out of the required path on purpose so the template imports and\nruns with a Zvid key and a Google account, nothing else."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        3020,
        -180
      ],
      "id": "zvid-tc-0045",
      "name": "Note 45"
    },
    {
      "parameters": {
        "width": 440,
        "height": 240,
        "color": 3,
        "content": "## ▶ Watch video\n\nClick this node after a run: n8n renders the downloaded MP4 as an inline\nvideo player with a download button — this month's compilation, no URL to\ncopy.\n\n*Run summary* before it still carries `videoUrl` (plus `authors`,\n`sheetRows` and `creditsCharged`) if you want to pipe the result onward."
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        3460,
        380
      ],
      "id": "zvid-tc-0046",
      "name": "Note 46"
    }
  ],
  "pinData": {},
  "connections": {
    "Test manually": {
      "main": [
        [
          {
            "node": "Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "On the 1st of each month": {
      "main": [
        [
          {
            "node": "Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Config": {
      "main": [
        [
          {
            "node": "Source?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Source?": {
      "main": [
        [
          {
            "node": "Read testimonials sheet",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Fetch Airtable records",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Read testimonials sheet": {
      "main": [
        [
          {
            "node": "Pick this month's",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Pick this month's": {
      "main": [
        [
          {
            "node": "Enough testimonials?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch Airtable records": {
      "main": [
        [
          {
            "node": "Pick Airtable records",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Pick Airtable records": {
      "main": [
        [
          {
            "node": "Enough testimonials?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Enough testimonials?": {
      "main": [
        [
          {
            "node": "Prepare compilation",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Nothing to compile",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare compilation": {
      "main": [
        [
          {
            "node": "Check music",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Check music": {
      "main": [
        [
          {
            "node": "Music guard",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Music guard": {
      "main": [
        [
          {
            "node": "Build project JSON",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build project JSON": {
      "main": [
        [
          {
            "node": "Validate project (free)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate project (free)": {
      "main": [
        [
          {
            "node": "Check validation",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Check validation": {
      "main": [
        [
          {
            "node": "Dry run?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Dry run?": {
      "main": [
        [
          {
            "node": "Save draft to editor",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Submit render",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Save draft to editor": {
      "main": [
        [
          {
            "node": "Dry run summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Submit render": {
      "main": [
        [
          {
            "node": "Wait",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Wait": {
      "main": [
        [
          {
            "node": "Get render status",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get render status": {
      "main": [
        [
          {
            "node": "Render finished?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Render finished?": {
      "main": [
        [
          {
            "node": "Rows to mark",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Still rendering?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Still rendering?": {
      "main": [
        [
          {
            "node": "Wait",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Rows to mark": {
      "main": [
        [
          {
            "node": "Sheet mode?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Sheet mode?": {
      "main": [
        [
          {
            "node": "Mark rows compiled",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Run summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Mark rows compiled": {
      "main": [
        [
          {
            "node": "Run summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run summary": {
      "main": [
        [
          {
            "node": "▶ Watch video",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "versionId": "5a9e37c1-64d2-4b08-91f7-8c3e0d5a6b29",
  "meta": {
    "templateCredsSetupCompleted": false
  },
  "id": "ZvidTestimonialCompilation",
  "tags": []
}
